# PROJE DURUM — Görsel Hafıza Teknikleriyle JSPS

> Bu dosya projenin "seyir defteri"dir. Yeni bir Claude sohbeti açtığında bunu yapıştır → kaldığın yerden devam.
> **KURAL: Her iş/düzeltme sonrası bu dosya güncellenir (farz).** Ne yapıldı, hangi commit, yeni karar/sorun eklenir.
> Son güncelleme: 9 Temmuz 2026
>
> ### ▶ 9 Tem (7) — 🎖️ İSİM = TAKDİR BELGESİ ANINDA SOR (build 24 iptal → build 25)
> Başkan daha iyi fikir: "Apple ile girende belgede saçma/boş isim görünmesin — belge çıkınca 'gerçek adın bu mu?' diye sor; Apple buna karşı çıkmaz." DOĞRU: belge-anı isim isteği giriş akışı DEĞİL, özelliğe-özel → Apple serbest. **Build 24 (onboarding'de opsiyonel isim) İPTAL** (buluta ulaşmadan TaskStop + icerik:tam). Yeni tasarım (commit 458a94f, tsc 0): **onboarding tekrar sade** (yalnız branş/rütbe, isim/telefon YOK → Guideline 4/5.1.1 riski sıfır). **Takdir Belgesi** (`takdir-belgesi.tsx`) artık `isim` prop'u alır → "Sn. Ad Soyad" + 3. şahıs metin. **YENİ `takdir-belge-alani.tsx`:** belgeyi gösterir + belge İLK çıkınca isim onay modalı ("Belgende adın nasıl yazılsın?" Ad/Soyad, Apple `tamAd` ön-dolu, düzeltilebilir/boş bırakılabilir) → onayınca profiles'a yaz + `belge-isim.ts` cihaz-yerel bayrak (bir daha sorma). `sinav.tsx` TakdirBelgesi→TakdirBelgeAlani. auth-context saglayici/tamAd ön-doldurma için. Not: belge sadece kanunun TÜM testleri %100 olunca çıkar (reviewer görmeyebilir ama görse de Apple-uyumlu). **BUILD 25** (b4znlhqit) → ✅ **RESUBMIT WAITING_FOR_REVIEW** (build 25 bağlı). Resubmit tuzağı: eski submission `canceled:true` yetmiyor; item hâlâ sürümü tutuyordu (`ITEM_PART_OF_ANOTHER_SUBMISSION`) → eski submission COMPLETE olunca sürüm serbest kaldı, item yeni submission'a eklenip submit geçti (item DELETE 409 verdi ama cancel-COMPLETE bıraktı). İzleme (ScheduleWakeup 1800s) build 25 için yeniden kuruldu. **DERS:** kişisel bilgiyi giriş akışında değil, GEREKTİĞİ ÖZELLİKTE topla. **AYRI KONU (araştırılacak):** %30 indirim kodu `SUEM2020OZEL30` 9+ kişi kullandı ama satın alanlar TAM FİYAT `musterek_omurboyu` aldı (indirimli `_i30` SKU değil) → indirim ömür boyuna yansımıyor olabilir (Play'de `musterek_omurboyu_i30` kurulu mu? paywall indirim mantığı?). İlk gün Google: 73 kullanıcı / 7 gerçek ömür boyu satış (hepsi Jandarma, 6 subay 1 astsubay).
>
> ### ▶ 9 Tem (6) — 🔧 İSİM/SOYİSİM GERİ GELDİ (opsiyonel, e-posta) → build 24
> Başkan: "yeni güncellemede de isim/soyisim olmalı (takdir/ceza belgeleri için); Apple sadece telefona takılmamış mıydı?" — HAKLI. Apple'ın isim itirazı SADECE Guideline 4 (Apple ile Giriş sonrası isim istenmesi); isim genel yasak DEĞİL, 5.1.1 sadece telefonu saymıştı. **Build 23'teki "tümünü kaldır" fazla agresifti.** Düzeltme (commit 03b2158, tsc 0): **auth-context** Kullanici tipine `saglayici` (app_metadata.provider) + `tamAd` (user_metadata.full_name) eklendi (`kullaniciYap` helper, iki setKullanici). **onboarding:** Apple ile girene isim SORULMAZ (Guideline 4 — `tamAd`'dan otomatik profiles'a yazılır); e-posta ile girene **OPSİYONEL Ad/Soyad** (İSTEĞE BAĞLI, boş geçilebilir → 5.1.1 uyumlu; Takdir Belgesi + Sicil'de görünür). Telefon/doğum/cinsiyet hâlâ yok. **BUILD 24** başlatıldı (bg0lek2rf). Build 23 (isimsiz) TestFlight'ta kalır ama resubmit build 24 ile olacak. **DERS:** Apple ret maddesini DAR yorumla — "isim after Apple-signin" ≠ "isim tamamen yasak". saglayici tespiti için app_metadata.provider.
>
> ### ▶ 9 Tem (5) — 🔁 APPLE 2. RET (build 22) → onboarding kişisel alanlar KALDIRILDI + EULA linki → build 23
> Apple build 22'yi hızlı inceleyip yine reddetti (3 madde, reviewer metni okundu). **(1) Guideline 4.0 (Apple ile Giriş) + 5.1.1(v) (telefon):** ESKİ — bizim "opsiyonel yaptık" fix'i YETMEDİ. Apple, isim/mail/telefon alanlarının Apple-giriş sonrası **görünmesini bile** istemiyor. → **ProfilAdim'daki ad/soyad/telefon/doğum/cinsiyet TAMAMEN kaldırıldı** (onboarding.tsx baştan yazıldı, yalnız branş/rütbe = GorevAdim; profilKaydet/AuthGirdi/DogumTarihiSecici/adHatasi/telefonHatasi importları çıktı). AuthEkrani zaten yalnız e-posta+şifre (telefon-giris ayrı auth yöntemi, sorun değil). **(2) Guideline 3.1.2(c) (abonelik):** YENİ — abonelik artık gerçekten incelendi (agreement aktif); App Store metadata'sında **Kullanım Şartları (EULA) linki** eksik. → app açıklamasına (tr localization, API PATCH) abonelik paragrafı ardına **"Kullanım Şartları (EULA): mevzujsps.com/sartlar.html + Gizlilik: mevzujsps.com/"** eklendi (sartlar.html 200 doğrulandı); paywall'da in-app yasal linkler ZATEN vardı (satır 543-559); app-level Privacy URL zaten set. **İnceleme notları (appStoreReviewDetails PATCH)** güncellendi: düzeltmeler İngilizce açıklandı (reviewer "Notes'a yaz" demişti). **BUILD 23** başlatıldı (icerik:manifest strip + TEMP D: + eas build, arka plan bj6g1nkvs). Bitince: build 23 bağla + (iptal-eski + yeni submission reçetesiyle) resubmit. tsc 0. commit e4b6750.
>
> ### ▶ 9 Tem (4) — 🐛 iOS SATIN ALMA PREMIUM AÇMIYOR → sunucu fix (JWS token indekse sığmıyor)
> Başkan TestFlight build 22'de test etti: **fiyatlar görünüyor ✅, isim/telefon boş geçildi ✅ (4.0+5.1.1 canlı doğrulandı), satın alma BAŞARILI ✅ (2.1 canlı doğrulandı — agreement aktif olunca çalıştı) AMA kilitler açılmadı + premium sembolü gelmedi.** Teşhis (Supabase Management API SQL): `satin_alma_log` → satın alma "dogrulandi" (Apple onayladı, abonelik bitiş +1g sandbox) AMA `uyelik_haklari` **BOŞ**. Elle INSERT reprodüksiyon → **`ERROR 54000: index row size 5640 exceeds btree maximum 2704 for index uyelik_haklari_token_uniq`.** KÖK SEBEP: iOS StoreKit token'ı **JWS** (5KB+, x5c sertifika zinciriyle); `satin_alma_token` üzerindeki BENZERSIZ partial index'e sığmıyor → upsert çöküyor. Kod upsert hatasını KONTROL ETMEDİĞİ için yine 'dogrulandi' logluyordu (sessiz başarısızlık). Android'de Google token kısa → sorun yoktu (iOS-özel). **FIX (commit 404d221):** iOS'ta `satin_alma_token` = kısa+kalıcı **originalTransactionId** (JWS payload'dan; paylaşım kontrolü + kayıt; yenilemelerde aynı). iOS upsert hatası artık `throw` (sessiz 'dogrulandi' yok). **SUNUCU FIX → app resubmit GEREKMEZ** (`supabase functions deploy dogrula-satinalma`, build 22 aynı fonksiyonu çağırır → reviewer için de düzeldi). Test kullanıcısının (98be2c62) hakkı elle yazıldı (kısa token) → uygulama yeniden açılınca premium görünmeli. **DERS:** iOS IAP token'ını (JWS) DB'de indeksleme/ham saklama — dev boyutlu; kısa txId kullan. **KALAN:** başkan app restart→premium teyit + Apple incelemesi.
>
> ### ▶ 9 Tem (3) — ✅ Paid Apps AKTİF + APPLE'A YENİDEN GÖNDERİLDİ (build 22, WAITING_FOR_REVIEW)
> Başkan banka/vergi/sözleşmeyi (kendi tarayıcısında, adım adım Claude yönlendirdi) tamamladı: **Legal Entity Compliance (pasaport) → Apple onayladı ("legal entity details accepted", ~1 saat)** → Paid Apps Agreement imzalandı → **banka (Ziraat IBAN, hesap ADEM YILMAZ 5012) + W-8BEN vergi formu (No US resident/activities, TC kimlik=foreign TIN, treaty box 9)** girildi → **Business ekranı: Paid Apps + Bank + Tax + Free Apps HEPSİ ACTIVE.** Küçük İşletme Programı (%15 komisyon) başkana önerildi (sonra). **RET UYARILARI TEKRAR OKUNDU (başkan istedi, IMG_2472-2478):** 4 madde + reviewer metni doğrulandı, hepsi kapalı: (2.1b IAP yüklenemedi → Apple AÇIKÇA "Paid Apps Agreement in effect olmalı" demiş = bugün çözüldü + geçersiz SKU koddan çıktı; 3.1.1 promo→iOS gizli; 4.0 Apple-giriş sonrası isim/mail isteme→opsiyonel; 5.1.1v telefon/doğum zorunlu→opsiyonel). **RESUBMIT (API):** build 22'yi sürüme bağla (`PATCH appStoreVersions/{id}/relationships/build`) → eski submission'ı submitted:true tekrar zorlamak "Version not ready to be submitted yet" 409 verdi (46dk boyunca, YAPISAL). **ÇÖZÜM:** eski reddedilmiş reviewSubmission'ı **iptal et** (`PATCH canceled:true`→CANCELING→COMPLETE) → **YENİ reviewSubmission oluştur** (app+IOS) + reviewSubmissionItem(appStoreVersion) + `PATCH submitted:true` → **200 WAITING_FOR_REVIEW** ✅. **NOT:** IAP ürünleri hâlâ DEVELOPER_ACTION_NEEDED (API version-submit onları dahil etmiyor) AMA Apple "IAP prior approval gerekmez, sandbox'ta çalışması + Paid Apps in-effect yeter" dediği için sorun DEĞİL (agreement aktif + ürünler fetch'lenebiliyor+artık purchasable). **DERS:** reddedilmiş sürümü resubmit = eski submission'ı zorlamak değil, İPTAL+YENİ submission. Otomatik izleme (ScheduleWakeup 1800s) yeniden kuruldu. **KALAN:** başkan TestFlight'ta build 22 satın alma testi (agreement aktif→çalışmalı) + Apple incelemesi (~24-48s, releaseType MANUAL→onay sonrası başkan elle yayınlar).
>
> ### ▶ 9 Tem (2) — 🔑 KÖK SEBEP: Paid Apps Agreement AKTİF DEĞİLMİŞ → kimlik doğrulamaya gönderildi
> Build 22 TestFlight'a düştü, başkan test etti: **yıllık fiyatı GÖRÜNÜYOR ama satın al → "işlem başlatılamadı" / ömür boyu → "tamamlanamadı".** Yani fetch düzeldi (kod fix çalıştı) ama SATIN ALMA olmuyor. **Teşhis:** iki ürün de `DEVELOPER_ACTION_NEEDED` (ret sonrası askı) + asıl kök sebep → **App Store Connect → Business/Agreements'te "Paid Apps Agreement" durumu "New" (İMZALANMAMIŞ).** Ücretli satış için şart; imzalanmadan hiçbir satın alma (test/inceleme) çalışmaz. **Bu Apple'ın orijinal 2.1 reddinin de gerçek sebebi olabilir** (reviewer da IAP yükleyemedi). **API bu durumu VERMİYOR** (agreements/banka ASC API'de yok) → başkan ekranı elle açtı, doğrulandı. **AKIŞ:** Business → "Paid Apps" = New + banner "imzadan önce legal entity güncelle" → **Edit Legal Entity → Legal Entity Compliance Screening** formu: pasaport yüklendi (pasaport tam.jpg), Birth Country Türkiye, Birth City osmaniye, Public Company No → **Next** → **"The information provided is being verified. We will notify you at dev.ademyilmaz@gmail.com"** → Paid Apps satırı geçici KAYBOLDU + Free Apps "Verifying"e döndü (tüm legal entity inceleniyor). **BEKLEME:** Apple kimlik doğrulaması ~saatler–1-2 iş günü, dev.ademyilmaz@gmail.com'a mail gelecek. **ONAY SONRASI SIRA (başkanla, elle — API/otomasyon yok):** Paid Apps satırı döner → imzala → banka (IBAN) ekle → vergi formu → durum Active → satın almalar çalışır → SONRA build 22'yi (veya taze) Apple'a resubmit (başkan onayıyla). **RESUBMIT'İ Paid Apps AKTİF olmadan YAPMA** (yoksa aynı 2.1 ile yine ret). Not: başkanın satıcı tipi = ŞAHIS (kendi adına). memory/apple-hesap-bilgileri güncellenecek.
>
> ### ▶ 9 Tem — ✅ APPLE 4-MADDE RET DÜZELTİLDİ + iOS build 21 başlatıldı
> Apple sürüm 1.0'ı REJECTED etti (4 madde). **Hepsi düzeltildi (commit 2737155, tsc 0):** (1) **2.1 "IAP yüklenemedi"** — iOS `fetchProducts`'a Google-only SKU `musterek_omurboyu_yukseltme` da veriliyordu → geçersiz SKU StoreKit fetch'ini bozuyordu; iOS artık yalnız `[URUN_OMURBOYU]` (in-app) + `[URUN_YILLIK]` (subs) çeker. (2) **3.1.1 promo/dış-mekanizma** — Promosyon Kodu satırı iOS'ta gizlendi (ayarlar.tsx + paywall.tsx link `{!ios ? … : null}`) + promo-kod.tsx iOS'ta inert (`useEffect router.replace('/')` + erken `return null`). Android'de aynen kalır. (3+4) **5.1.1(v) gizlilik + 4.0 Apple giriş** — onboarding kişisel alanları (ad/soyad/telefon/doğum/cinsiyet) ZORUNLU→OPSİYONEL; `profilKaydet` yalnız dolu alanı yazar (yeniden-kurulumda veri kaybı yok). Onboarding kapısı `profilTamam` yerine **branş/rütbe**ye bağlandı (_layout.tsx `eksik = girisGerek || (kullanici && gorevEksik)`; onboarding.tsx GorevAdim/profilTamam kaldırıldı). **Başkan endişesi (sızıntı takibi) yanıtlandı:** isim/telefon zaten sahte girilebilir → gerçek iz **giriş(e-posta)+filigran(user.id)+sunucu log**; bunlara dokunulmadı, takip gücü aynı. **BUILD:** `eas build -p ios --profile production --non-interactive` (arka plan br7tehiac) → buildNumber 20→**21**, remote credentials hazır (cert exp 2027-07-06, Team BPJR5S85TK), dosyalar yükleniyor. Sonra `eas submit` → TestFlight → **başkan Sandbox IAP testi** (2.1'i canlı doğrula) → onay → App Store resubmit. **NOT:** IAP `musterek_omurboyu` state şu an DEVELOPER_ACTION_NEEDED (resubmit'te sürümle tekrar bağlanacak). Otomatik izleme (ScheduleWakeup) durduruldu — resubmit sonrası yeniden kurulur.
>
> ### ▶ 8 Tem (4) — ✅ ANDROID ÜRETİME GÖNDERİLDİ (vCode 40 / 1.0.28, Google incelemesinde)
> Başkan "üretim onayı geldi, üretime al" → Google Play üretim ERİŞİMİ verilmiş (kapalı test şartı tamam) ama üretim kanalı boştu. **Eski vCode 32 (1.0.22) KİLİTLİ ama eski** (Günün Maddesi fix + markalı adres yok — başkan "kilitli hatırlıyorum" dedi, git geçmişi doğruladı: 1.0.22'de KILIT_AKTIF=true) → **taze build şart.** `bash scripts/build-aab.sh` (strip+prebuild+gradle, ~4dk, .env'den Supabase+içerik alır) → **vCode 33 "zaten kullanılmış"** hatası → **vCode 40** ile yeniden derle → app-release.aab (47MB, KILIT_AKTIF=true + tüm fix). **PLAY YÜKLEME (CDP, dev.ademyilmaz):** Üretim → Yeni sürüm oluştur → AAB `input[type=file].setInputFiles({timeout:240000})` (47MB, uzun) → sürüm notları (tr-TR) → **ÜLKE HATASI** ("hiçbir ülke seçilmedi") → Ülkeler/bölgeler sekmesi → başlık checkbox "tümünü seç" (177) → Kaydet → sürüme dön → Kaydet (artık ülke engeli yok) → **Yayın Özeti → "3 değişikliği incelemeye gönder" → onay** → **"Değişiklikleriniz şu anda inceleniyor" ✓** (bekleyen 0). Yönetilen yayınlama KAPALI → onay sonrası OTOMATİK canlı. **İki platform da incelemede: Apple 1.0.28 + Android vCode 40.** **KALAN (başkan, ACİL): ödeme doğrulaması** (satıcı banka yöntemi, 31 Tem deadline — yapılmazsa hesap+uygulamalar kaldırılır; Claude yapamaz).
>
> ### ▶ 8 Tem (3) — ✅ APP STORE'A GÖNDERİLDİ (sürüm 1.0 + 2 ürün birlikte, WAITING_FOR_REVIEW)
> Başkan onayı ("yapıştır gelsin") → **gönderim TAMAMLANDI.** Önce metadata gözden geçirildi + rötuşlandı (başkanla): (1) **Vurucu metinler** (yapay-zeka kokmayan) — alt başlık "Ezberleme, gözünde canlandır", yeni tanıtım + açıklama (hook: "Aynı maddeyi onuncu kez okuyup yine mi unutuyorsun?"); free/paid netleştirildi ("SADECE TCK — çalışması+denemesiyle — ücretsiz, gerisi premium"; "deneme = Talim sekmesi kanun sınavları"). (2) **Support URL** ana sayfadan → yeni **docs/iletisim.html** (mevzujsps.com/iletisim.html, destek + iletişim@ + hızlı yardım; canlı 200). (3) **İnceleme notu düzeltildi** (eski "kilit kapalı test" çelişkisi → "TCK bedava, premium için Sandbox satın alma"). (4) **Fiyatlar Google ile eşitlendi** (başkan istedi): yıllık 999,99→**1.079,99**, ömür boyu →**1.559,99** (Apple ince fiyat noktaları artık var; KDV dahil; baz ülke TR). **GÖNDERİM (tarayıcı UI, API yapamıyor):** ASC sürüm sayfası → "In-App Purchases and Subscriptions" bölümü → **"Select In-App Purchases or Subscriptions"** → 2 ürün işaretlendi (Done) → **Add for Review → Create New Submission → Submit for Review**. **SONUÇ (API doğrulama): sürüm 1.0 + musterek_omurboyu + musterek_yillik = HEPSI WAITING_FOR_REVIEW.** releaseType MANUAL → Apple onaylasa bile başkan ELLE yayınlar (önce Sandbox ödeme testi). Beklenen: Apple incelemesi ~24-48s. **NOT:** İlk IAP web-UI'dan sürüme baglanip gonderildi (API kisiti asildi — ürünler sürümün "In-App Purchases" bölümüne eklenince binary'yle birlikte gidiyor).
>
> ### ▶ 8 Tem (2) — ÜRETİME ÇIKIŞ: 1.0.28 (kilit AÇIK) build + App Store gönderimi hazırlığı
> Başkan: "hepsini ayarla, üretime ver, beklemeyelim." Plan: kilidi aç + hızlı ödeme testi + bugün gönder (başkan seçti). **YAPILDI (hepsi API, tarayıcısız):** (1) `KILIT_AKTIF false→true` (urunler.ts — üretim: TCK+denemesi ücretsiz, gerisi premium). (2) Sürüm 1.0.27→**1.0.28**, iOS strip+build (**build 20**, FINISHED) + TestFlight'a submit (VALID). (3) **IAP metadata tamamlandı (ASC API):** iki ürün MISSING_METADATA'ydı → inceleme ekran görüntüsü (mağaza görselinden indirilip 3-adım reserve/PUT/commit ile yüklendi) + **satış bölgesi 175 ülke** (availability) eklendi → ikisi de **READY_TO_SUBMIT**. (4) Sürüm metadata teyidi (açıklama/keyword/destek=mevzujsps.com/kategori=Eğitim/yaş 4+/gizlilik/fiyat hepsi tam) + build 20 sürüme bağlandı + **releaseType=MANUAL** (onay sonrası ELLE yayın → ödeme testi için pencere). **DUVAR — SON TIK API'DE YAPILAMIYOR:** İlk kalıcı ürün gönderimi Apple kuralı gereği **sürümle AYNI anda web panelinden** olmalı (`STATE_ERROR.FIRST_NON_CONSUMABLE_MUST_BE_SUBMITTED_ON_VERSION`); `reviewSubmissionItems` IAP relationship'ini kabul etmiyor, `inAppPurchaseSubmissions` ilk ürün için hata veriyor, sürüm-tek gönderimde ürünler dahil olmuyor (60sn beklenip doğrulandı). Tarayıcı otomasyonu da mümkün değil (başkanın açık Brave'i debug portsuz + kapatmak YASAK + cookie kopyası çalışan Brave'de kilitli, admin/VSS gerekir). **KALAN TEK İŞ (başkan, ~4 tık):** ASC → Sürüm 1.0 → "Uygulama İçi Satın Alımlar" bölümü → 2 ürünü ekle → İncelemeye Gönder. Ürünler WAITING_FOR_REVIEW'a geçince ben API'den otomatik doğruluyorum. Sürüm PREPARE_FOR_SUBMISSION, build 20 bağlı, temiz. Gönderilince → Apple incelemesi (~24-48s) → onay → başkan ELLE yayınlar (releaseType MANUAL) → önce Sandbox ödeme testi. **App Store submit onayı VERİLDİ** (başkan "üretime ver" dedi — memory/app-store-submit-onay-kurali karşılandı).
>
> ### ▶ 8 Tem — TESTÇİ BULGUSU: "Günün Maddesi" ilk kurulumda BOŞ BEYAZ EKRAN → DÜZELTİLDİ + 1.0.27 TestFlight'a gönderildi
> iOS testçileri sıfır kurulumda **Karargah → "Günün Maddesi"** kutusuna basınca boş beyaz ekran görmüş. **KÖK SEBEP:** içerik artık sunucudan iniyor (strip'li build); ilk kurulumda hiçbir kanun inmemiş. Arama ve Patika'da bir **İNDİRME KAPISI** var (inmemiş kanuna basınca "indir ve aç" modalı), ama **Günün Maddesi bu kapıyı ATLAYIP** doğrudan `/akis`'e gidiyordu → görsel yok → boş kart (`gorselKaynak` native'de private bucket'ın public URL'ini döndürüp 400 alıyor). **DÜZELTME (`index.tsx`, commit f1e9fed):** Günün Maddesi onPress artık Arama'daki (`ara.tsx`) ile birebir aynı kapı — `LAW_KLASOR` + `kanunIndirilmisMi` kontrolü → inmemişse Alert "İndir ve aç" → yüzdeli indirme modalı (progress dinleyici + "arka planda indir" + hata/tekrar dene) → biter bitmez o maddenin kartına gider. Aday havuzu zaten `lawErisilebilirSaf` ile erişilebilir kanunlardan → paywall gerekmez. **ANDROID:** kod native'de birebir aynı → Android'de de aynı bug vardı (iOS'a özel değil); bu düzeltme ikisini birden kapatır. **KARARGAH diğer girişler güvenli:** "unutulan" satırları `/patika`'ya (kapılı) gider, zayıf mod `calisilabilirZayif` ile yalnız inmiş içerik. Günün Maddesi tek açıktı. tsc 0. **BUILD + YAYIN (1.0.27 build 19):** sürüm 1.0.26→**1.0.27**; iOS strip (icerik:manifest + .easignore, commit e6b7b8f) → `eas build --platform ios --profile production` (arşiv 796MB, ~25dk, FINISHED, ipa hazır) → `eas submit` ile App Store Connect'e → Apple işledi **VALID** (encryption:false otomatik) → geri yükle (icerik:tam + .easignore sil, commit 620a86b, push'landı). **ASC — TARAYICI YOK, App Store Connect API ile (yeni, temiz yöntem):** ES256 JWT (AuthKey_RGL99F5D25.p8, issuer 6ad2e590…) → build 19 **dış grup "Herkese Acik Test"e eklendi** (POST betaGroups/{id}/relationships/builds → 204) + **beta incelemeye gönderildi** (POST betaAppReviewSubmissions → 201, WAITING_FOR_REVIEW, submittedDate 08.07 01:20). Build 19 **iç testçilerde ZATEN canlı** (IN_BETA_TESTING); dış link **https://testflight.apple.com/join/KM7Y48Wm** Apple beta onayı gelince 19'u sunar (build 18'i sildi/süpürdü — 2. inceleme genelde hızlı). App Store yayını DEĞİL (o hâlâ başkan onayı bekler).
>
> ### ▶ 7 Tem (3) — TESTFLIGHT HERKESE AÇIK LİNK (dış test) beta incelemeye gönderildi
> Başkan: "TestFlight'ı linke sahip herkese aç, millet test etsin sorun tespit edelim." **YAPILDI (CDP):** yeni **dış test grubu "Herkese Acik Test"** oluşturuldu → build **1.0.26 (build 18)** gruba eklendi → Test Information formu dolduruldu (Beta App Description contenteditable-editör + feedbackEmail iletisim@mevzujsps.com + iletişim Adem/Yilmaz/+905441201211 + demo giriş apple.review@mevzujsps.com/MevzuReview.2026) → "What to Test" notu (contenteditable) → **Submit for Review** → build **"Waiting for Review"**. **HERKESE AÇIK LİNK: https://testflight.apple.com/join/KM7Y48Wm** (Public Link oluşturuldu). UYARI (beklenen): "grup onaylı build alana kadar testçiler katılamaz" → Apple **Beta App Review** (App Store yayın incelemesinden AYRI, daha hafif; birkaç saat–1 gün) geçince link tam açılır, linke sahip herkes (10.000'e kadar) indirir. **NOT:** Bu App Store yayını DEĞİL — App Store submit hâlâ başkan onayı bekliyor. Form alan tekniği: ASC contenteditable div editörleri (textarea değil) → click+Ctrl+A+type; radio gerçek fare tıklamasıyla (React state).
>
> ### ▶ 7 Tem (2) — MARKALI ALAN ADI mevzujsps.com CANLI + tüm "mayibey" public URL'lerden TEMİZLENDİ + iOS Google nonce fix
> Başkan Hostinger'dan **mevzujsps.com** aldı → GitHub Pages'e bağlandı. **DNS:** apex A kaydı @ → **185.199.108.153** (Hostinger panel, TTL 50 geçersizdi→3600 + değer klavyeyle; ekleme formu diğer 3 yedek IP'yi otomasyona almadı ama GitHub tek anycast IP ile çalışır, yeterli), www CNAME zaten var. **GitHub Pages:** `docs/CNAME`=mevzujsps.com commit'liydi ama panelde custom domain=null + eski legacy build "errored" görünüyordu → `gh api PUT .../pages -f cname=mevzujsps.com` (önce https_enforced'suz, sertifika yokken 404 verdi), workflow yeniden tetiklendi → **http+https UÇTAN UCA 200** (/, /sartlar.html, /hesap-sil.html, /onaylandi.html hepsi 200) → **https_enforced=true**. Gerçek yayın Actions workflow'u (pages.yml) yapıyor, API'deki "errored" eski legacy ölçüsüydü (geçersiz). **"mayibey" TEMİZLİĞİ (başkan kuralı: hiçbir public URL'de mayibey geçmeyecek):** (1) ASC **Support URL** version sayfasında mayibey.github.io→**https://mevzujsps.com/** (keyboard tekniği+Save). (2) ASC **Privacy Policy URL** App Privacy sayfasında Edit modal→**https://mevzujsps.com/** (mayibey/index.html'di). (3) `config.ts` GIZLILIK_URL/SARTLAR_URL boş→mevzujsps.com (uygulama içi "web'de aç" linki artık çalışır). (4) `auth.ts` ONAY_DONUS (e-posta onay dönüş) mayibey→mevzujsps.com/onaylandi.html. (5) **Supabase** redirect allow-list'e `https://mevzujsps.com/**` EKLENDİ (Management API PATCH 200; eski mayibey ŞİMDİLİK duruyor — şu an gönderilen build hâlâ eski adresi kullanıyor, yeni build çıkınca kaldırılır). **iOS Google nonce (önceki turdan):** Supabase external_google_skip_nonce_check=true → "passed nonce…" hatası çözüldü; iOS Google butonu geri açıldı (iosClientId+iosUrlScheme yapılandırıldı). tsc 0. **NOT:** docs/*.md + .ts + DEGERLENDIRME/ dosyaları GitHub Pages'te public serviste (mevzujsps.com/YAYIN_HAZIRLIK.md vb. erişilebilir) — iç planlama sızıntısı, ileride docs kök temizlenmeli. **APP STORE:** build 1.0.26 sürüme bağlı+hazır, **başkan onayı olmadan "Add for Review" YOK** (memory/app-store-submit-onay-kurali).
>
> ### ▶ 7 Tem — GECE OTONOM: App Store gönderim + güvenlik/adli iz + Android→iOS parite 🌙
> Başkan uyurken otonom: 3 ajan analizi → uygula. **(1) IAP fiyat boş = KOD DEĞİL** — yeni app'te IAP ürünleri sürümle App Review'e gönderilene kadar StoreKit'e (TestFlight) dönmüyor → çözüm panelde IAP'yi sürüme bağla + Submit for Review. **(2) FİLİGRAN adli iz:** `gorsel` edge fn filigran artık **user.id** basıyor (e-posta DEĞİL — Apple "E-postamı Gizle" relay kişiye ulaşmıyordu); yeni tablo **`icerik_erisim_log`** (docs/v2/20; user_id+yol+IP+UA+zaman, RLS service_role; gorsel & imzali-url insert, fail-open) → **deploy + uçtan uca test edildi** (log IP ile düştü). MAC/gerçek cihaz IP mobilde alınamaz (sadece NAT IP). **(3) PARİTE:** iOS Google giriş butonu **gizlendi** (iOS native client yok → hata verirdi; Apple+e-posta yeter); yasal-metin.ts erişim logu+12ay saklama (KVKK); ekran-koruma iOS screenshot engelleyemez (OS limiti, filigran telafi). **Hosted yasal sayfalar** `scripts/yasal-html-uret.mjs` ile yasal-metin.ts'ten yeniden üretildi (platform-nötr Google Play/App Store, kanonik renk). **BUILD 1.0.25** (tüm fix) — ilk denemede pod install "Unknown error" GEÇİCİ hata → **retry BAŞARILI** → TestFlight. **PANEL (Adem/admin, tek-Adem giriş sabitledi):** metadata + 3 ekran görüntüsü (1284×2778, HTML→Playwright render, gerçek kartlar) + App Review (demo hesap+iletişim+notlar) + Privacy Policy URL + **App Privacy YAYINLANDI** (6 veri türü, batch otomasyon) + **Age Rating 4+**. **KALAN TEK İŞ:** 1.0.25 işlenince sürüme bağla + IAP ekle + release=Manuel + Add for Review → IAP fiyatları aktifleşir. Detay: memory/apple-hesap-bilgileri.
>
> ### ▶ 6 Tem (6) — iOS İLK BUILD BAŞARILI + TESTFLIGHT'A YÜKLENİYOR 🎉
> Uzun bir hata turundan sonra **iOS build FINISHED** (v1.0.22 build 13, .ipa hazır) → `eas submit` ile TestFlight'a yükleniyor. **Çözülen build hataları:** (1) pod install "AppCheckCore modular headers" (google-signin/Firebase) → plugins/withModularHeaders. (2) **PUSH BLOKAJI (asıl zorlu):** expo-notifications iOS'a aps-environment ekliyor → build push-profili ister → App Store Connect API key profile'a push ekleyemez (Apple ID+2FA gerekir, telefon yok) → Xcode düşüyor. Config-plugin ile kaldırılamadı (Expo plugin sırası) → **patch-package** ile paketten aps-environment satırı silindi + postinstall (EAS'te otomatik). 2FA gerekmedi. (3) "Build request failed" → eas-cli güncellendi. **KİMLİK KURULUMU:** başkan kendi terminalinde bir kez interaktif (API key env'leriyle, 2FA'sız) sertifika+profil üretti → sonrası Claude non-interactive. **AÇIK:** iOS build 1.5GB (yükleme 15dk) — içerik-kırpma App Store öncesi şart. Detay + tüm CDP/build tuzakları: memory/apple-hesap-bilgileri. Şimdi: TestFlight processing → başkan iPhone'undan test.
>
> ### ▶ 6 Tem (5) — APPLE / iOS HAZIRLIĞI BAŞLADI (plan + iOS temeli + "Apple ile Giriş" + silme metni)
> Başkan: "Apple tarafına başlayalım — önce araştır/planla, sonra gerekli düzenlemeleri yap, apple mod on." + "hesabım zaten var" + Bundle ID **app.mevzujsps.ios**. **PLAN:** çok-ajanlı araştırma → `docs/APPLE_YAYIN_PLANI.md` (6 bölüm + ilk 5 adım + kaynaklar; Apple guideline no'lu). 3 zorunlu engel: StoreKit ödeme (3.1.1), Apple ile Giriş (4.8), gerçek hesap silme (5.1.1(v)). **YAPILDI (kod, hepsi push):** (1) `app.json` ios: bundleIdentifier app.mevzujsps.ios + buildNumber + usesAppleSignIn. (2) **Apple ile Giriş (4.8):** expo-apple-authentication ~8.0.8; auth.ts `appleIleGiris()` iOS-only dinamik import → signInWithIdToken({provider:'apple'}), ilk giriş ad-soyadı meta veriye; auth-context girisYapApple; auth-ekrani apple() + rıza; saglayici-butonlari iOS'ta Apple native butonu. (3) Hesap silme metni (5.1.1(v)): reaktivasyon reklamı kaldırıldı, "30 gün içinde KALICI silinir". tsc 0. **BAŞKAN PANEL İŞLERİ (kod bunlarsız çalışmaz):** (a) hesabın ÖDEMELİ Apple Developer Program mı teyit; (b) Apple Developer'da "Sign in with Apple" capability + Services ID/key; (c) Supabase Auth → Apple provider aç; (d) iOS Google girişi için iOS OAuth client + GoogleService-Info.plist; (e) gizlilik/şartlar URL'lerini yayınla (config.ts boş); (f) App Store Connect'te uygulama + IAP ürünleri (StoreKit testinden önce). **KALAN BÜYÜK KOD:** StoreKit paywall (paywall.tsx platform ayrımı + dogrula-satinalma'ya Apple makbuz doğrulaması) — ürünler App Store Connect'te oluşunca; iOS'ta indirim/yükseltme mantığı yeniden tasarlanacak (doğrulanmalı). **APPLE PANEL (6 Tem, CDP ile YAPILDI):** ödemeli üyelik teyitli (Team ID **BPJR5S85TK**); **App ID `app.mevzujsps.ios` KAYITLI + Sign in with Apple capability AÇIK** (developer.apple.com Identifiers). Künye → memory/apple-hesap-bilgileri. Sıradaki panel: App Store Connect uygulama + IAP ürünleri (fiyat onayı) + Supabase Apple provider (bundle'ı authorized client ID ekle). **Google "test ödeme" ÇÖZÜLDÜ:** başkan Lisans testini kendi kapattı (otomasyonla Play ayar sayfalarına girilemiyordu; bizim tarafta ödeme bypass'ı YOK — teyit edildi, sadece Google lisans testi hesapları parasız alıyordu; gerçek müşteriler zaten gerçek öder). **APPLE İLERLEME (6 Tem, CDP):** App Store Connect ToS onaylandı; **Supabase Apple provider AÇIK + Client ID app.mevzujsps.ios kaydedildi → Apple girişi UÇTAN UCA HAZIR** (kod+App ID+Supabase). **App Store Connect UYGULAMA + IAP ÜRÜNLERİ OLUŞTU (CDP+elle):** uygulama "Mevzu — JSPS Hazırlık" iOS 1.0; Paid Applications Agreement ACTIVE (banka/vergi/W-8BEN, DSA "AB'de dağıtma" → adres gizli); **iki ürün hazır:** `musterek_yillik` (yıllık, 999 TL) + `musterek_omurboyu` (ömür boyu, 1.499,99 TL) — grup+localization'lar CDP ile (native select JS-set + keyboard type + son-buton submit reçetesi), fiyatlar elle (kilitli picker). Apple girişi zaten uçtan uca hazırdı. **iOS ÖDEME KODU YAZILDI (6 Tem, kod, commit'li, tsc 0):** (1) paywall.tsx platform ayrımı — iOS `requestPurchase apple:{sku,appAccountToken=UUID}`, indirimsiz base; yıllık→ömür boyu tam fiyat (fark ürünü yok); UI metinleri platform-nötr. (2) satinalma.ts istek gövdesine platform. (3) dogrula-satinalma Apple yolu — JWS'ten transactionId → kendi ES256 JWT'mizle App Store Server API (üretim→sandbox) → imzalı transaction doğrula (bundleId+productId+iptal+appAccountToken sahiplik); Android akışı korundu (geriye uyum). **APPLE API KEY KURULDU + FONKSIYON CANLI:** başkan "In-App Purchase" key üretti (Key ID JPFAY6DKU3, Issuer 6ad2e590-...); ben Supabase secrets APPLE_IAP_ISSUER_ID/KEY_ID/PRIVATE_KEY set + `functions deploy dogrula-satinalma` → **iOS ödeme uçtan uca canlı** (kod + platform + sunucu Apple doğrulama). **EAS iOS BUILD BAŞLADI (6 Tem):** Kimlik-bilgisi kurulumu gerçek TTY istediğinden (Claude'da TTY yok) başkan kendi PowerShell'inde çalıştırdı — App Store Connect API key env'leriyle Apple GİRİŞİ SORMADAN (2FA'sız) sertifika+profil üretildi (Team Individual/BPJR5S85TK; push notification'ı ATLADIK — apple id/2FA istiyordu, yerel bildirim gerektirmiyor). Build id f85d6b3a... in progress (production, store, v1.0.22, build 5). Reçete + tuzaklar memory/apple-hesap-bilgileri'nde. **iOS İÇERİK KIRPMA YOK → build 1.5GB** (Android strip'i iOS'ta yok); App Store gönderiminden ÖNCE iOS için de kırpılmalı. **SIRADAKİ:** build bitince `eas submit --platform ios` → TestFlight → gerçek iPhone'da Apple giriş + Sandbox ödeme testi → iOS içerik-kırpma + ekran görüntüleri/metadata → App Store gönderim.
>
> ### ▶ 6 Tem (4) — DOĞRUDAN KART AKIŞI (Zor Detay gizlendi) + vCode 32 İNCELEMEYE GÖNDERİLDİ
> Başkan: kanuna basınca açılan "Görsel Konu Anlatımı / Zor Detay Kartları" iki-seçenek ekranını kaldır, Zor Detay v2'ye kalsın; tıklayınca ESKİ USÜL doğrudan kartlar başlasın; sonra build+yükle, üretime doğru gidelim ("sürekli bişi çıkıyor bi yayınlayalım artık"). **YAPILDI:** mevzuat.tsx `kanunaGit` artık /kanun-sec ara ekranı yerine DOĞRUDAN /patika kart akışına gider (tek nav noktasıydı; hem liste hem "Devam Et" bunu kullanır). kanun-sec.tsx dosyası v2 için DURUYOR (artık çağrılmıyor, _layout kaydı zararsız). /patika lawId'yi tek başına okur (doğrulandı). tsc 0. **BUILD vCode 32/1.0.22 (BUILD SUCCESSFUL 4m47s) → D:\buildler\mevzu-vCode32.aab (47MB) → Kapalı test-Alpha'ya CDP ile YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅** (upload 53s; notlar TR; İleri→review[rollout %100]→Kaydet→Genel bakış→"1 değişikliği incelemeye gönder"→onay "Değişikliği incelemeye gönder"→gönderildi, kalan buton 0). İÇERİK: doğrudan-kart + 3 arama/gösterim sızıntı düzeltmesi + gece denetim düzeltmeleri (premium kilit mekanizması, sunucu fail-closed, zorunlu güncelleme). vCode 31'i (hâlâ incelemede olabilir) sü'rer. **ÜRETİME GEÇİŞ — TEK GERÇEK ENGEL: ÖDEME PROFİLİ.** Play Console'da hâlâ CANLI uyarı: "Ödeme hesabınızda ilgilenmeniz gereken acil bir sorun var — Sorunu düzelt" (banka/ödeme profili doğrulaması, BAŞKANDA, "31 Tem" ölüm tarihi). Bu çözülünce: vCode 32 onaylandıktan sonra üretim sürümüne yükselt (Google tam inceleme) → yayın. 12-tester/14-gün kapalı test şartı zaten karşılandı.
>
> ### ▶ 6 Tem (3) — ÇOK-AJANLI DOĞRULAMA: kilit EKSİK çıktı, 3 sızıntı düzeltildi (build BEKLİYOR)
> Başkan "ajanların çalıştı mı?" diye sordu → dürüst: bulguları önceki tur ajanlar bulmuştu, bu turda ben düzeltip build aldım; düzeltmelerin TUTTUĞUNU ajanlarla bağımsız doğrulamamıştım. Bunun üzerine gerçek bir doğrulama turu koşturuldu (Workflow: 3 bulucu [envanter/istemci-sunucu/config-fail-open] ∥ + her bulguya aksi-doğrulayıcı [REFUTE] + baş-şüpheci; **15 ajan, 0 hata, 1.3M token**). **SONUÇ — mekanizma kilidim KART AÇMAYI her yerde kapatmış** (mevzuat/kanun-seç/sınav/zor-detay/indirme yolları doğrulayıcılarca ÇÜRÜTÜLDÜ = sağlam) **AMA GÖSTERİMİ kapatmamış → 3 GERÇEK SIZINTI:** (1) 🔴 **ara.tsx** arama sonuç snippet'i kilitli kanunda premium madde metni + "Aklınıza çivileyin" anlatımını KOŞULSUZ gösteriyordu (dokunma kapısı snippet'i durdurmaz — başkanın "aile" şikayet sınıfı). Düzeltme: kilitli sonuçta snippet gizli, yalnız kanun+başlık + "üyelik gerekli" + paywall. (2) 🟡 **index.tsx** "Günün Maddesi" aday havuzu premium süzülmüyordu → yalnız erişilebilir kanunlardan seçilir. (3) 🟡 **sicil.tsx** "Zayıf Mevziler" premium süzülmüyordu → kilitliler listeden çıkar, "üyelik gerek" sayacına düşer. tsc 0. Commit `3587f84` (push edildi). **MİMARİ SINIR (düzeltilmedi, gece çözülmez):** madde metinleri + sınav soruları + anlatım JS bundle'a GÖMÜLÜ, kapı yalnız istemcide → APK açan ödeme yapmadan alır (görseller sunucuda korumalı; metin/sorular değil). = "içeriği sunucuya taşı" yol haritası işi (Yayın İş Planı V2). LATENT: ICERIK_TABANI `/object/public/` yolu — şu an bucket private (404) canlı değil. **AÇIK KARAR:** bu 3 sızıntı düzeltmesi için **vCode 32 build şimdi mi (vCode 31 incelemede, düzeltme onda YOK) yoksa 31 geçince topluca mı** — başkana soruldu, cevap beklerken internet gitti. Kod hazır, build BEKLİYOR. Detay: docs/DENETIM_SISTEMI.md (dersler güncellendi).
>
> ### ▶ 6 Tem (2) — GECE OTONOM DENETİM DÜZELTMELERİ + vCode 31 BUILD (premium-gate + sunucu fail-closed + zorunlu güncelleme)
> Başkan gece talimatı: "ajanlar birbirini denetlesin, aksi bir ajan hepsini sınasın; sabah tüm detaylar kontrol edilmiş olsun; sorunları düzelt, topluca build al, sürümü güncelleyip yayınla." + KRİTİK BUG bildirimi: arama "aile" → premium kart bedava açılıyordu; "indir→aç" da kilidi atlıyordu. **KÖK SEBEP:** /akis kilidi çağırana devredilmişti → arama/sınav/zor-detay/sesli-nöbet/kanun-seç kapısız. **ÇÖZÜM (MEKANİZMA SEVİYESİ, commit b980db7 — denetim P1):** yeni `lib/icerik-kilidi.ts` (paylaşılan `useKanunKilidi` hook + `lawErisilebilirSaf`) + `urunler.ts kanunErisilebilirSaf` saf fn; TÜM içerik giriş noktalarına kapı takıldı (akis + queue premium filtresi, sinav, zor-detay, sesli-nobet, kanun-sec, patika fail-open fix). **SUNUCU (denetim P0):** imzali-url + gorsel edge fn KILIT varsayılan AKTİF (secret yoksa da premium kapalı; eskiden `=== '1'` fail-open, secret unutulursa tüm premium açığa çıkardı) → redeploy; dogrula-satinalma obfuscatedExternalAccountId ≠ user.id → 403 (başka hesaba ait satın alma bağlanamaz); KILIT_AKTIF=1 secret set. **F13:** profiles.created_at BEFORE UPDATE trigger ile donduruldu (48s indirim penceresi güncellemeyle sıfırlanamaz — docs/v2/19). **F4:** paywall indirim banner'ı yalnız Play'de hazır plan(lar) için "geçerli" der (sadece biri indirimliyken fazla vaat etmez). **F8:** senkron yerelBosMu web'de hep dolu görünüp bulut geri-yükleme tetiklemiyordu → kutu>0 (gerçek SRS) sayılır, native/web parite. tsc 0 hata. **BUILD vCode 31/1.0.21 (BUILD SUCCESSFUL 4m19s) → D:\buildler\mevzu-vCode31.aab (47MB) → Kapalı test-Alpha'ya CDP ile YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅** (setInputFiles 102s; sürüm notları TR; İleri→review [rollout %100 hazır, yalnız 1 zararsız manifest-cihaz uyarısı]→Kaydet→Genel bakış→"1 değişikliği incelemeye gönder"→onay diyaloğu "Değişikliği incelemeye gönder"→gönderildi, kalan gönder butonu 0). Google onayı sonrası testçilere düşer. **ZORUNLU GÜNCELLEME AYARI (bilinçli KAPALI kaldı):** uygulama_ayar.zorunlu_min_surum=**0** bırakıldı. Neden: 31 yapılırsa hâlâ vCode 30'daki testçiler, 31 indirilebilir olmadan (inceleme sürerken) kilitlenirdi. Mekanizma vCode 31 İÇİNDE; 31 canlı olunca (ya da bir sonraki sürüm 32'de) min=+1 yapılıp devreye alınacak. **ÖMÜR BOYU İNDİRİM TEKLİFLERİ (indirim20/indirim30 musterek_omurboyu): hâlâ "Planlandı"** (satın alma seçeneği Etkin ama teklifler planlı; otomatik-aktifleşme bugüne dek olmadı). Teklif detay sayfasında etkinleştir/düzenle aksiyonu YOK; editör canlı faturalandırmada kırılgan (önceki denemeler de başarısız) → gece riske atılmadı. **GÜVENLİ:** F4 sayesinde uygulama yalnız Play'de GERÇEKTEN hazır plan için indirim gösterir → ömür boyu Planlandı kalsa da "ömür boyunda indirim" diye YALAN vaat etmez (yıllık indirimi doğru görünür). BAŞKAN ELLE (2 dk) ya da planlı tarih gelince açar.
>
> ### ▶ 6 Tem — İNDİRİM HATIRLATMA MODALI + geri sayım gün/sn formatı + geri sayım NaN fix + vCode 28 İNCELEMEDE
> **GERİ SAYIM NaN BUG (docs/v2/18, canlı):** indirim_durumu.bitis "+00" formatındaydı → JS new Date NaN → "NaN dk NaN sn". UTC+"Z" formatına çevrildi (sunucu → mevcut build'lerde de anında). ilk giriş süresi 24→**48 saat**. **GERİ SAYIM FORMATI:** lib/geri-sayim.ts kalanParcala + kalanMetin → "1 gün 13 sa 19 dk 20 sn" (gün+saniye dahil); paywall şeridi + modal kullanır. **İNDİRİM HATIRLATMA MODALI:** components/premium/indirim-hatirlatma.tsx — segmentli canlı geri sayım kutulu (GÜN/SAAT/DK/SN) fırsat kartı, Karargah'a yerleşik. Tetik: patika'ya (TCK) girildikten sonra (lib/indirim-hatirlatma.ts flag) + sonraki açılışlarda, oturumda 1, EN FAZLA 3 kez, yalnız ilk-giriş indirimi aktif + premium değilken. "İndirimli Al"→paywall. **BUILD vCode 28/1.0.18 → D:\buildler\mevzu-vCode28.aab → kapalı teste YÜKLENDİ + İNCELEMEDE.** BAŞKANA FİYAT: KDV=%20; i20 KDV dahil 1.247,99 / hariç 1.039,99, i30 dahil 1.091,99 / hariç 909,99 (Play alanı hangisini soruyorsa; müşteri sonuçta dahil fiyatı ödemeli). Başkan hâlâ i20/i30 fiyatını düzeltecek + ödeme profili.
>
> ### ▶ 5 Tem (10) — KAYIT SIRASI FIX + "JSPS'ye" + vCode 27 İNCELEMEDE + ömür boyu ürün temizlik listesi
> **KAYIT SIRASI (bug):** Google ile yeni kayıtta ÖNCE tutorial çıkıp SONRA profil geliyordu. `_layout.tsx`: uygulama turu "EN ÜST KAPI" idi (satır 189) → değiştirildi, tur artık profil/görev BİTİNCE (son adım) gösterilir. Yeni sıra: giriş→profil→görev→TUR→ana ekran (onboarding guard'dan `tanitimTamam!==true` beklemesi kaldırıldı; tur kapısına `!eksik && !profilBekle` eklendi). **"JSPS'e hazırlan"→"JSPS'ye hazırlan"** (uygulama-turu.tsx; tek yerdeydi). tsc 0. **BUILD vCode 27/1.0.17 → D:\buildler\mevzu-vCode27.aab → Kapalı test-Alpha'ya YÜKLENDİ + İNCELEMEDE** ("27 (1.0.17) sürümü incelemede"). CDP NOTU: yükleme öncesi video-akış sekmeleri kapatıldı (Playwright'ı kilitliyordu). **ÖMÜR BOYU PLAY ÜRÜN TEMİZLİĞİ (başkana tarif edildi, başkan yapacak):** KAL→ musterek_omurboyu + _yukseltme + _i20(fiyat 1.499,99→1.247,99 DÜZELT + isim netleştir); EKLE→ musterek_omurboyu_i30 (1.091,99); PASİF YAP→ paket_omurboyu, brans_omurboyu, brans_omurboyu_yukseltme (+ aboneliklerde brans_yillik, paket_yillik) — eski model, satılmıyor, uygulama çağırmıyor (zararsız ama temizlik).
>
> ### ▶ 5 Tem (9) — İLK GÜN İNDİRİMİ CANLI GERİ SAYIM + vCode 26 İNCELEMEDE (i20 fiyat düzeltme + i30 ELLE)
> Başkan: geri sayım ekle + ömür boyu indirim ürünlerini ayarla + build al yükle. **YAPILDI (kod):** paywall indirim şeridine İLK GİRİŞ indirimi için CANLI geri sayım ("%20 indirimin uygulanıyor — İlk gün fırsatı: 23 sa 45 dk kaldı!"); bitiş sunucudan (indirim_durumu.bitis), saniyede tikler, yalnız kaynak='ilk_giris' (kod indiriminde sayaç yok). tsc 0 hata. **BUILD vCode 26/1.0.16 → D:\buildler\mevzu-vCode26.aab (47MB) → Kapalı test-Alpha'ya YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ** ("26 (1.0.16) sürümü incelemede"). Base fiyatlar başkandan TEYİTLİ: Yıllık 1.079,99 / Ömür Boyu 1.559,99. **ÖMÜR BOYU İNDİRİMLİ ÜRÜNLER — ELLE KALDI (otomasyon çözemedi):** i20 başkan elle açmış ama fiyatı YANLIŞ girmiş (1.499,99 ≈ %4, olması gereken %20=1.247,99); i30 hiç yok. Play'in YENİ tek-seferlik ürün fiyat ekranı (Set prices→toplu / hücre editörü) otomasyona işlemiyor: değer giriliyor ama satıra yansımıyor + "Kaydet" pasif kalıyor, toplu editör yanlış "vergi kategorisi" penceresi açıyor; canlı faturalandırmada riskli → dokunulmadı. **BAŞKAN ELLE:** (1) i20 fiyatını 1.499,99→**1.247,99** düzelt, (2) **musterek_omurboyu_i30** ürününü **1.091,99** ile aç. App zaten hazır (ürün yoksa base fiyata düşer, güvenli). CDP NOTU: video akış sekmeleri (dramadizilerim /izle) Playwright bağlantısını kilitliyor → otomasyon öncesi kapat.
>
> ### ▶ 5 Tem (8) — vCode 25 KAPALI TESTE YÜKLENDİ + İNCELEMEDE (ömür boyu indirimli ürünler ELLE kalacak)
> vCode 25/1.0.15 AAB (D:\buildler\mevzu-vCode25.aab) Brave CDP ile Kapalı test-Alpha (track 4699518344207255017) → yeni sürüm → AAB yüklendi (setInputFiles handle, ~90s) → TR sürüm notları → Kaydet → yayınlama özeti → **1 değişiklik incelemeye GÖNDERİLDİ**. Kanal: "25 (1.0.15) sürümü incelemede". Google onayı sonrası testçilere düşer. İçerik: duyurular + bedava promo kod + %indirim (yıllık teklifler CANLI) + offerToken fix. **ÖMÜR BOYU İNDİRİMLİ ÜRÜNLERİ (musterek_omurboyu_i20/_i30) OLUŞMADI:** Play tek-seferlik ürün fiyat editörü (Set prices menüsü + toplu fiyat) otomasyona kararsız direndi (menü kapanıyor, yanlış "vergi kategorisi" diyaloğu açılıyor) → canlı faturalandırmada yanlış fiyat riski almadım, yarım ürün KAYDEDİLMEDİ (liste temiz, i20/i30 yok). Fiyat başkanla TEYİTLİ: i20=1.247,99 / i30=1.091,99 TL. ELLE yapılacak (2 dk/ürün) ya da ödeme profili düzelince birlikte (lifetime indirim zaten o bitmeden test edilemez; app ürün yokken base fiyata düşer=güvenli). ELLE ADIM: Para kazanın→Tek seferlik ürünler→Oluştur→Kimlik musterek_omurboyu_i20, Ad "Tam Erişim – Ömür Boyu"→İleri→satın alma seç.kimliği 'satinal'→Set prices→Türkiye 1.247,99→Etkinleştir (i30 aynı, 1.091,99).
>
> ### ▶ 5 Tem (7) — GOOGLE PLAY YILLIK İNDİRİM TEKLİFLERİ CANLI + vCode 25 BUILD
> Başkan "ödeme profili sorununa takılma, dene" dedi — DENENDİ ve OLDU: ödeme profili uyarısı teklif oluşturmayı/aktifleştirmeyi **ENGELLEMİYOR**. Brave CDP ile yıllık base plana (musterek_yillik/yillik) 2 teklif kuruldu + **ETKİNLEŞTİRİLDİ (CANLI):** **indirim20** (%20) ve **indirim30** (%30) — tip "Tek ödeme / 1 yıl" (ilk yıl indirimli, sonra normal yenilenir), uygunluk "Geliştirici belirler" (app kime vereceğini seçer, offerId ile). App (vCode 25) bunları OTOMATİK kullanır: kod/ilk-giriş indirimi olan kullanıcıya yıllıkta offerId eşleşen teklif, normalde temel teklif. **ÖMÜR BOYU indirimli ürünleri (musterek_omurboyu_i20/_i30) HENÜZ YOK:** oluşturma formuna girildi ama bölgesel fiyat editörü kırılgan + base ömür boyu fiyatı (kayıtta 1.559,99 TL) canlı hesapta tahminle girilmemeli → yarım ürün KAYDEDİLMEDİ (hesap temiz). Bunlar başkanla fiyat teyidi sonrası bitecek (i20=%80≈1.247,99 / i30=%70≈1.091,99). App zaten hazır; ürün yokken ömür boyu indirimi sessizce base fiyata düşer (güvenli). **BUILD: vCode 25/1.0.15 → D:\buildler\mevzu-vCode25.aab (47MB, BUILD SUCCESSFUL 4m10s)** — içerik: duyurular + bedava promo kod + %indirim (yıllık canlı) + offerToken fix. Başkan yükleyip inceletecek. NOT: ödeme profili banka doğrulaması hâlâ başkanın işi (satış/test o bitince).
>
> ### ▶ 5 Tem (6) — İNDİRİM 2 KATMAN (ilk giriş %20 + kod %30) + YILLIK & ÖMÜR BOYU + ÜST ÜSTE BİNMEZ
> Başkan genişletti: (a) uygulamayı yükleyen herkese **ilk girişten 24 saat otomatik %20** (kod gerekmez), (b) devre özel **SUEM2020OZEL30 → %30**, (c) kodu giren %30 alır — **20+30=50 OLMAZ** (en yüksek TEK indirim), (d) her indirim **hem yıllık hem ömür boyunda** geçerli. NET SINIR (Google): tek seferlik üründe kodla % teklif YOK → ömür boyu indirimi **AYRI ÜRÜNLE** (musterek_omurboyu_i20/_i30, %80/%70 fiyat) uygulanır; yıllık ise teklifle (indirim20/indirim30). **Sunucu (docs/v2/17, CANLI):** indirim_kodlari/indirim_hak'a omurboyu_urun kolonu; **indirim_durumu() RPC** tek/en yüksek indirimi döner {yuzde,kaynak,yillik_offer,omurboyu_urun,bitis} (kod > ilk giriş > yok — üst üste binmez, sunucu saatiyle); ilk giriş ayarları uygulama_ayar'da (ilk_giris_indirim_aktif/saat/yuzde/yillik_offer/omurboyu_urun — başkan build'siz değiştirir); SUEM2020OZEL30 AKTİF (indirim30 + musterek_omurboyu_i30); HOSGELDIN20 silindi. **UÇTAN UCA TEST GEÇTİ** (yeni kullanıcı→ilk giriş %20; kod→%30 [50 değil!]; 24s sonra→yok). **İstemci:** urunler.ts INDIRIMLI_OMURBOYU_URUNLERI (PREMIUM_URUNLERI'ne + fetch'e); lib/indirim.ts indirimDurumuOku(); paywall açılışta durumu okur → YILLIK'ta indirimli teklif (offerToken), ÖMÜR BOYU'da indirimli SKU satın alınır, fiyat/etiket/şerit indirimli gösterilir AMA yalnız Play tarafı gerçekten hazırsa (indirimUygulanabilir — "vaat edip tam fiyat çekme" yok); dogrula-satinalma whitelist'e i20/i30 eklendi + **redeploy edildi** (v-yeni, CANLI). tsc 0 hata. **DİKİŞ (ödeme profili düzelince Play'de):** yıllık base plana 2 teklif (offerId **indirim20**/**indirim30**, uygunluk=developer determined, %20/%30) + 2 ömür boyu ürünü (**musterek_omurboyu_i20**/**i30**, %80/%70 fiyat). GOOGLE TARAFI DENENDİ: teklif ekleme ekranı AÇILIYOR ama üstte "Ödeme profilinizle ilgili bir sorun var" → aktifleştirme/test o düzelene kadar takılı (başkanın ödeme profili işi). Başkan indirim kodu ekler: dashboard → indirim_kodlari (kod BÜYÜK, offer_id, yuzde, omurboyu_urun).
>
> ### ▶ 5 Tem (5) — YILLIK %İNDİRİM KODU SİSTEMİ (Google teklifiyle; app+sunucu hazır, Play tarafı ödeme sonrası)
> Başkan "gerçek %20 indirim" istedi (bedava değil; kişi indirimli öder, para Google'dan). NET SINIR: kodla % indirim Google'da YALNIZ abonelikte olur → **sadece YILLIK**; Ömür Boyu (tek seferlik) için Google'da kodla % indirim YOK. Mekanizma: Play'de yıllık base plana "%20 promosyon teklifi" (offerId='indirim20', uygunluk=developer determined) açılır; kod giren kullanıcıya app satın almada TEMEL teklif yerine indirimli teklifi (offerToken) verir → Google az tahsil eder. **Karar: "şimdiden başla"** → ödemeden bağımsız kurulabilecek her şey yapıldı, Play teklifi + test ödeme profili aktifleşince. **Sunucu (docs/v2/16, CANLI):** indirim_kodlari (kod/offer_id/yuzde/limit/gecerlilik/aktif) + indirim_kullanim (kişi başı tekil) + indirim_hak (kullanıcının güncel indirim hakkı, RLS ile kendi okur) + indirim_kodu_kullan RPC (atomik, idempotent; jsonb {ok,hata?,yuzde,offer_id}). Tablolar istemciye kapalı (indirim_hak hariç: yalnız kendi satırı). **UÇTAN UCA TEST GEÇTİ** (geçici kullanıcı: geçerli→ok+%20, tekrar→idempotent sayaç şişmez, farklı kod→hak güncellenir, RLS okuma, limit; sonra temizlendi). **İstemci:** urunler.ts INDIRIM_OFFER_ID='indirim20'; lib/indirim.ts (kullan + hakkiOku); /promo-kod ekranı TEK KUTU iki tür kod (önce bedava promo dener, olmazsa indirim → başarı ekranı ayrışır: "erişim açıldı" vs "%20 indirim tanımlandı → Yıllık Planı Gör"); paywall: açılışta indirim hakkı okunur, YILLIK satın almada indirimli teklif seçilir (**BONUS FIX:** offerToken eskiden hep [0] seçiyordu → teklif eklenince herkese indirim kazası olurdu; artık indirim yoksa TEMEL teklif [offerId boş] seçilir), yıllık planda "%20 indirimli" şeridi + alt yazı. tsc 0 hata. **DİKİŞ (ödeme sonrası):** Play Console → yıllık base plan → promosyon teklifi offerId=**indirim20** (yoksa app temel fiyata düşer, güvenli). Örnek kod HOSGELDIN20 PASİF (Play teklifi açılıp yeni build çıkınca aktifle). **Başkan indirim kodu eklemek için:** dashboard → indirim_kodlari satırı (kod BÜYÜK, offer_id=indirim20, yuzde, limit).
>
> ### ▶ 5 Tem (4) — YAYIN TEMİZLİĞİ + PROMOSYON KODU SİSTEMİ
> **(A) SUNUCU TEMİZLİĞİ (yayına tertemiz çıkış):** Başkan kararıyla tüm test verisi silindi → 22 hesap (kendi mayibey dâhil) + 20 ilerleme + 14 test satın alması + 102 satın alma logu + cihaz kayıtları + 3 geri bildirim SİLİNDİ (auth.users delete → cascade; satin_alma_log truncate). KALDI (bilerek): uygulama_ayar (4 sistem ayarı: cihaz_max=2/cihaz_gun=7/telefon_giris=0/premium_offline_gun=36500), duyurular (hoş geldin), tüm tablo/RLS/fonksiyon iskeleti. Yeni gerçek kullanıcılar sıfırdan başlar. NOT: test cihazlarındaki yerel ilerleme o telefonlarda; sunucuyu ilgilendirmez. İDEAL: bu temizliği üretime basmaya çok yakın 1 kez daha tekrarla (testçiler arada kayıt oluşturabilir).
> **(B) PROMOSYON KODU SİSTEMİ (kendi sistemimiz — başkan tercihi):** Play Billing DIŞINDA bedava erişim (kod satılmaz → politika uygun). **Sunucu (docs/v2/15, CANLI):** `promosyon_kodlari` (kod/verilen_urun[promo_omurboyu|promo_yillik]/verilen_tip/sure_gun/kullanim_limiti/gecerlilik_bitis/aktif) + `promo_kullanim` (kod×user tekil, mükerrer engel) + `promo_kullan(p_kod)` RPC (SECURITY DEFINER, atomik: doğrula→hak yaz→sayaç artır; jsonb {ok,hata} döner). Tablolar istemciye KAPALI (RLS policy yok → kodlar sızmaz); yalnız RPC + dashboard. **İstemci:** urunler.ts'e promo_yillik/promo_omurboyu eklendi (PREMIUM_URUNLERI'ne girdi → premium sayılır); lib/promo.ts (RPC + TR hata eşleme); /promo-kod ekranı (kod gir→yenile()→premium açılır, giriş yoksa uyarır, başarı ekranı); giriş noktaları: Ayarlar "Promosyon Kodu" satırı + Paywall "Promosyon kodun mu var?". **UÇTAN UCA TEST GEÇTİ** (gerçek geçici kullanıcı: geçerli→ok+ömür boyu hak, tekrar→zaten_kullandin, yok→gecersiz, premium_mi=true, sonra temizlendi). Örnek kod ORNEK2026 PASİF duruyor (başkan aktifleştirip çoğaltır). **BAŞKAN kod eklemek için:** dashboard → promosyon_kodlari tablosuna satır (kod BÜYÜK harf, verilen_urun+tip seç, limit/süre iste). tsc 0 hata. Sonraki build'e girecek.
>
> ### ▶ 5 Tem (3) — DUYURULAR ÖZELLİĞİ (Karargah çan → duyuru ikonu, sunucudan yönetilir)
> Başkan: "duyurular bölümü neden yok?" + "çan ikonunun yerine duyuru ikonu koy, duyuru yoksa 'duyuru yok' yazsın, geçmiş duyurular da görünsün." **YAPILDI:** (1) **Sunucu (docs/v2/14):** `duyurular` tablosu (baslik/metin/hedef['herkes'|'premium']/aktif/created_at) + RLS yalnız-oku (aktif=true), yazma yalnız dashboard/service_role. Başkan artık build almadan Supabase'den satır ekleyince herkeste anında görünür. İlk "hoş geldin" duyurusu eklendi (canlı, teyitli). (2) **İstemci `lib/duyuru.ts`:** aktif duyuruları yeni→eski çeker, premium-hedefli olanları yalnız premium kullanıcıya gösterir; okunmamış takibi AsyncStorage son-görülme zamanıyla (çevrimdışı→boş, çökme yok). (3) **Ekran `/duyurular`:** geçmiş dâhil tüm duyurular, boşsa "Henüz duyuru yok", premium rozeti; açılışta okundu işaretlenir. (4) **Karargah başlığı:** eski çan (bell-outline → /egitim-plani) KALDIRILDI, yerine `DuyuruIkonu` (bullhorn-outline, okunmamış varsa kırmızı nokta → /duyurular). egitim-plani hâlâ Ayarlar'dan erişilebilir (orphan değil). _layout'a Stack.Screen eklendi. tsc 0 hata. **Sonraki build'e (vCode 25) girecek; push/FCM bildirimi sonraya.**
>
> ### ▶ 5 Tem (2) — vCode 24/1.0.14 KAPALI TESTE + İNCELEMEDE (giriş+kaydol onay kutucuğu her ikisinde)
> vCode 23 sonrası başkan: onay kutucuğu Giriş sekmesinde de zorunlu olsun (Google girişten de hesap açılabiliyor → rıza kaçmasın). auth-ekrani: kutucuk her iki modda görünür + google()/gonder() her modda sartlar zorunlu (pasif metin kaldırıldı). versionCode 24/1.0.14 build → D:uildlermevzu-vCode24.aab → kapalı test release 25 → başkan incelemeye GÖNDERDİ ("İncelenmekte"). vCode 24 = 23 üstü (her şey + onay). SATILAN ÜRÜNLER/FİYAT (teyit): Tam Erişim Yıllık 1.079,99 · Ömür Boyu 1.559,99 · Ömür Boyuna Yükseltme 479,99 (Türkiye KDV dahil); eski brans/paket Play'de uykuda, app satmıyor. EMÜLATÖR NOTU: D:/android-avd var ama PC reset'te AVD kaydı bozuldu (emulator -list-avds boş); native önizleme için yeniden kayıt/boot + APK gerek.
>
> ### ▶ 5 Tem — YAYIN ÖNCESİ 4-KOLLU DENETİM + vCode 23 FINAL BUILD (üretim yolu netleşti)
> Başkan "üretime geçmeden a'dan z'ye her şeyi kontrol et" dedi. 4 kol (kod / sunucu / Play Console / yasal) — /code-review + /security-review + paralel ajanlar. SONUÇ: **YAYIN ENGELİ YOK, hepsi yeşil.** Düzeltilen küçük bulgular: (1) /code-review 4 gerçek hata (tek-oturum yarış [kritik], çıkışta bayat premium, geniş ekran sayfalama, seed çökme) — HEPSİ DÜZELTİLDİ. (2) marka adı 3 farklı yazımdaydı → yasal metinlerde "Mevzu · JSPS"e standardize. (3) package.json 1.0.9 bayat → 1.0.13 senkron. **DENETİM TEYİTLERİ:** kod tsc 0 hata + secret sızıntısı yok + SDK pinleri tam; sunucu KILIT_AKTIF açık + Edge fn'ler aktif + 5 tablo/kolon/4 fn/trigger/index/RLS tam + 3 kritik tablo yalnız-oku + mail TR; yasal metinler eski-model kalıntısız + cihaz kimliği beyanlı + çelişkisiz + iletişim tutarlı; Console Data Safety+gizlilik URL tam. **vCode 23/1.0.13 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅** (D:\buildler\mevzu-vCode23.aab; release 24, "İncelenmekte"). **ÜRETİM YOLU (başkana):** (a) 🔴 EN KRİTİK: ödeme profili doğrulaması (kırmızı banner "31 Tem hesap kaldırılacak" — Google parası/banka, BAŞKANDA); (b) ✅ 12-tester/14-gün kapalı test şartı KARŞILANDI ("Üretime başvur" düğmesi açık → Google incelemesi); (c) gerçek JSPS sınav tarihi hâlâ 2 Eyl placeholder. Sıra: ödeme profili çözülür → üretime başvur (Google inceler) → onay → üretim sürümü. Kartlar v2'de (build beklemez).
>
> ### ▶ 5 Tem — GÜVENLİK İNCELEMESİ (/security-review) + cihaz kilidi sunucuya zorlandı
> /security-review çalıştırıldı (working tree temiz → elle güvenlik-kritik dosyalara yönlendirildi: Edge fn'ler + RPC'ler + RLS + SQL). SONUÇ: ödeme/hak zinciri SAĞLAM (ödemeden premium yolu YOK). Tek gerçek bulgu (MEDIUM, güven 9): cihaz_kilit + tek-oturum yalnız istemcideydi → değiştirilmiş istemci atlar. **DÜZELTİLDİ (docs/v2/13, canlıda):** premium_mi artık `cihaz_kilit is null` da arar (imzali-url/gorsel kilitli hesabı reddeder); profiles_kilit_koru trigger (authenticated cihaz_kilit'i değiştiremez); premium_mi execute yalnız service_role. Küçük notlar (checklist): (a) YAYINDA `KILIT_AKTIF=1` zaten yapıldı ✓, (b) aktif_oturum istemci-yazılır kaldı (tek-oturum UX katmanı; sağlam katman = cihaz sayısı kilidi definer+RLS+premium_mi).
>
> ### ▶ 5 Tem — ÇEVRİMDIŞI 5-GÜN KİLİDİ KALDIRILDI (başkan kararı, ahlaki+pratik)
> Başkan sorguladı: arazide/sinyalsiz görevdeki jandarma personeli çevrimdışı çalışamayınca cezalanıyor. Etik değerlendirme: çalışma aleti vermek, kullanıcının görev ihmalinden bizi sorumlu yapmaz (o disiplin amirin işi); dürüst çoğunluğu azınlık için cezalandırmak yanlış. KARAR: çevrimdışı süre kilidi (heartbeat/offlineKilit/premiumOfflineGun) TAMAMEN kaldırıldı — çevrimdışı çalışma korunur (sunucu okunamazsa son premium değeri korunur). Paylaşım koruması iki katmanla yeter: **tek-oturum** (online olunca sahibi olmayan cihaz çıkışa zorlanır) + **cihaz kilidi** (7 günde 2 cihaz, docs/v2/12). Mevcut testerlar (vCode 21) için sunucudan premium_offline_gun=36500 (o build'de de tetiklenmez). Not: başkanın "internet zorunlu yap" fikri REDDEDİLDİ — aynı sebeple (arazi personeli) daha kötü olurdu; ürünün canı = indir+çevrimdışı çalış.
>
> ### ▶ 4 Tem GECE (4) — HAFIZA KARTI MODU (kanun seçim ekranı + zor-detay) + CİHAZ KİLİDİ (paylaşım koruması)
> **HAFIZA KARTI ÖZELLİĞİ (başkan yeni içerik üretiyor):** Kanuna tıklayınca `/kanun-sec` seçim ekranı → (1) "Görsel Konu Anlatımı" = mevcut /patika akışı (SRS'li), (2) "Zor Detay Kartları" = yeni `/zor-detay` SALT GÖRÜNTÜLEME (öğrendim/zor/SRS/quiz/zayıf-mevzi YOK; yatay pager + GorselZoom). Görseller `{klasor}_zor_{n}.webp`, aynı assets/kartlar klasöründe → indirme/şifreleme/imzalı-URL boru hattından OTOMATİK geçer; seed.ts `_zor_` anahtarlarını çalışma kartından hariç tutar (DB satırı/SRS yok, şema göçü yok); codegen `KART_ZOR_ANAHTARLARI` dışa verir. İçerik yok → "hazırlanıyor" boş ekranı. **BAŞKAN YAPACAK:** görselleri `assets/kartlar/{kanun}/{kanun}_zor_N.webp` koy → `npm run gorsel:uret` → içerik sunucuya yükle.
> **CİHAZ KÖTÜYE KULLANIM KİLİDİ (tester bulgusu — hesap paylaşımı):** hesap 7 günde en fazla 2 farklı cihazda (uzak ayar cihaz_max=2/cihaz_gun=7). Aşılırsa OTOMATİK kilit → premium reddedilir, paywall'da "iletisim@ ile aç". Normal telefon değişimi (eski+yeni=2) takılmaz; karakol paylaşımı (sürekli cihaz değişimi) kilitler. Manuel açma=destek (docs/v2/12: cihaz_gecmisi tablo + profiles.cihaz_kilit + cihaz_dogrula RPC, CANLI). uyelik-context: premium = hak && !offlineKilit && !cihazKilit. Katmanlı paylaşım koruması TAM: tek-oturum (online evict) + çevrimdışı heartbeat (5 gün) + cihaz sayısı kilidi (7g/2). Gizlilik metni + index.html + Data Safety'ye cihaz kimliği eklendi. NOT: tek-oturum lib/oturum-kilidi ile cihaz_gecmisi AYRI (biri son-login kimliği, diğeri kalıcı kurulum kimliği getCihazKimlik). **vCode 22 hâlâ YÜKLENMEDİ — bu iki özellik + öncekiler tek build (vCode 23) olacak (kartlar gelince).**
>
> ### ▶ 4 Tem GECE (3) — YAYIN ÖNCESİ DELİK KAPATMA + çevrimdışı paylaşım koruması + vCode 22 (build)
> Başkan "yayına almadan yapabileceğin her şeyi yap, ertelenenler kalsın, sonra bana yapacaklarımı söyle" dedi. **YAPILANLAR:** (1) **KİLİT AKTİF EDİLDİ (sunucu):** `secrets set KILIT_AKTIF=1` + imzali-url v6 + gorsel v13 redeploy → premium kapısı SUNUCUDA aktif (premium_mi RPC); ücretsiz kayıtlı biri artık imzalı içerik URL'i alamaz. (2) **Token benzersizlik indexi (09 SQL):** önce 3 çift makbuz temizlendi (test hesapları respectylmz1 vs respsctylmz223 — aynı token 2 hesapta; ilk doğrulayan tutuldu), sonra `uyelik_haklari_token_uniq` kuruldu → bir makbuz artık tek hesapta (2. savunma). (3) **ÇEVRİMDIŞI PAYLAŞIM AÇIĞI (tester bulgusu):** hesabı 2. cihazda açıp internet kapatınca ikisi de premium çalışıyordu → **heartbeat:** premium yalnız SUNUCUDAN taze okundukça geçerli; cihaz `premium_offline_gun` (uzak ayar, vars. 5) gün sunucuyu doğrulayamazsa premium KİLİTLENİR (paywall'da "internete bağlan" uyarısı). uyelik-context yeniden yazıldı (haklariOku offline≠boş ayrımı, AppState 'active'+auth'ta yenile). Tek-oturum + heartbeat birlikte: paylaşım penceresi 5 günle sınırlı. (4) **Play mağaza:** gizlilik URL girildi (mayibey.github.io), Data Safety formu CSV ile dolduruldu+kaydedildi (Ad/E-posta/Telefon/Kimlik/Diğer-kişisel/Satın alma/Uygulama-etkinliği = toplanıyor, hesap yönetimi+işlev+kişiselleştirme; konum/kişiler/reklam=hayır), mağaza metni temiz (branş kalıntısı yok). (5) **onaylandi.html** (markalı e-posta onay sayfası, GitHub Pages canlı) + kayıt/tekrar-gönder maili oraya döner (emailRedirectTo + Supabase allowlist). (6) **Giriş ekranı:** MEVZU logosu büyüdü (96→140) + altı teğmenle hizalı, "Giriş Yap" başlığı ortalandı. **vCode 22/1.0.12 build alınıyor.** **ERTELENEN (kararım, riskli):** servis hesabı ADMIN→granular daraltma — ödeme sistemi oturmadan (Google parası + profil doğrulaması beklenirken) yapılırsa test satın alması bozulursa sebep ayırt edilemez → ödeme stabil olunca tek seferde daralt+test. **BAŞKANA KALAN (kod değil):** (a) ödeme profili doğrulama payments.google.com (31 Tem hesap kapanma uyarısı — EN ACİL), (b) gerçek JSPS sınav tarihi (hâlâ 2 Eyl placeholder), (c) vCode 22 test turu.
>
> ### ▶ 4 Tem GECE (2) — vCode 21 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅
> Build BUILD SUCCESSFUL 4m23s, 47MB → `D:\buildler\mevzu-vCode21.aab` (bundle teyit: PREMIUM_URUNLERI/jsps.oturum.kimlik/sifremi-unuttum + "Tam Erişim"/"Ömür boyuna yükselt" utf16; versionCode 21/1.0.11). Brave CDP: Alpha → release 22 → AAB → TR sürüm notları → Kaydet → yayın özeti → incelemeye GÖNDERİLDİ ("İncelenmekte"). İÇERİK: tek kapsam üyelik + hesap seçici fix + şifre KOD akışı + guard fix + TEK OTURUM + e-posta doğrulama kapısı + Giriş/Kaydol sekmeleri + sol logo/sağ teğmen + çift-tık kilidi + Google hata teşhisi + tutorial hesaba bağlı + fiyat metinleri. Onaylanınca başkan test: (1) tek paket paywall + satın alma/yükseltme (2) Google giriş hesap seçici her seferinde çıkıyor mu (3) şifremi unuttum kod akışı (4) iki cihaz tek oturum düşürmesi (5) e-posta doğrulama kapısı (yeni kayıtla).
>
> ### ▶ 4 Tem GECE — ÜYELİK MODELİ KOMPLE SADELEŞTİ: TEK KAPSAM (başkan kararı — branş içeriği ÜRETİLMEYECEK)
> Başkan: "branşların kapsamı aşırı fazla, kart üretmeyeceğim; 2 tip üyelik olsun, her şeyi kapsasın." → müşterek/branş/paket AYRIMI KALKTI. **Model: uygulamanın TAMAMI = tek paket → Yıllık (abonelik) + Ömür Boyu (tek seferlik) + Ömür Boyuna Yükseltme (fark).** (tsc 0, web export OK, commit.) **Kod:** `urunler.ts` URUN_YILLIK/OMURBOYU/YUKSELTME + PREMIUM_URUNLERI (satılan + ESKI_PREMIUM: brans/paket geçmiş satın almalar hâlâ premium sayılır); `uyelik-context` tek `premium` bayrağı (kategori bazlı musterek/brans GİTTİ), kanunErisilebilir premium'a bakar (blok param opsiyonel, yok sayılır); `paywall` tek "Tam Erişim" kartı (kapsam listesi + yıllık/ömür boyu ya da yükseltme ya da tam-erişim durumu); mevzuat/tatbikat'ta **Müşterek/Branş segmenti + "yakında" blokları KALDIRILDI** (l.blok==='müşterek' filtresi kaldı — zaten sadece içerikli kanunları gösterir). NOT: `brans-context`/`brans-store`/profiles.brans = kullanıcının ASKERİ BRANŞI (jandarma/sb görev seçimi), premium'la alakasız — DOKUNULMADI. **Sunucu:** dogrula-satinalma v4 (ürün beyaz listesi + yükseltme şartı = herhangi aktif yıllık). **Play (Brave CDP):** satılan 3 ürün adı "Tam Erişim – Yıllık/Ömür Boyu/Ömür Boyuna Yükseltme" yapıldı; abonelik "Avantajlar" alanı 40 karakter sınırlı (başkan uyardı) → "Tüm uygulamaya 1 yıl sınırsız erişim" (36). Eski brans/paket ürünleri Play'de DOKUNULMADI (uykuda; uygulama artık çağırmıyor, IAP uygulama dışı satılamaz). **Fiyatlar (bir önceki kayıttaki güncel değerler geçerli):** Yıllık 1.079,99 · Ömür Boyu 1.559,99 · Yükseltme 479,99. **vCode 21 build bekliyor** (bu model + önceki tüm fixler).
>
> ### ▶ 4 Tem AKŞAM (2) — YENİ FİYAT KURGUSU UYGULANDI (piyasa çalışması + başkan onayı)
> Piyasa: JSPS kitap ~1.070 TL, kitap setleri 2.600-6.500, video kurslar 5.500-14.000. Başkan itirazı: branş 1000+ kart (müşterek 502) → maliyet 2 kat; ayrıca "%50 indirim" iddiası tam fiyat ilan edilmediği için boştu. **KURGU (onaylı):** branş TAM fiyatı = müşterekin 2 KATI (yıllık 2.159,99 / ömür 3.119,99 — İLAN edilir, yayında bu fiyata çıkar); ön satış %50 = MÜŞTEREK FİYATINA. **Play'de GÜNCELLENDİ (canlı, Türkiye raf):** brans_yillik 1.079,99 · brans_omurboyu 1.559,99 · paket_yillik 1.799,99 · paket_omurboyu 2.639,99 (2.649 küsuratlı olurdu) · brans_yukseltme 479,99 · müşterek tarafı değişmedi. Yayın günü: branş tam fiyata, paket ~%15 indirimli (2.749,99/3.979,99), brans_yukseltme 959,99 yapılacak. Metinler güncellendi (paywall/mevzuat/tatbikat — "2 kat içerik hedefi, ön satışta müşterek fiyatına"; vCode 21'e girer). NOT (otomasyon dersi): abonelik fiyat kaydında onay dialogu butonu küçük harf "Değişiklikleri kaydet" — /kaydet/i ile eşleşir; doğrulama okuması önbellekten eski gelebilir, 15 sn bekle.
>
> ### ▶ 4 Tem AKŞAM — TEK OTURUM (kendi mekanizmamız) + giriş ekranı yeni üst düzen (vCode 21'e hazır)
> Başkan tek-oturum için "sen yap" dedi (Pro plana gerek kalmadan; gelir henüz yok). **Yapıldı (tsc 0, commit):** `profiles.aktif_oturum` (docs/v2/11, canlıda) + `lib/oturum-kilidi.ts` — girişte cihaz benzersiz kimlikle hesabı sahiplenir; diğer cihaz açılış/öne-gelmede uyuşmazlığı görüp DÜŞER ("hesabına başka cihazdan giriş yapıldı" kartı giriş ekranında). Offline fail-open. Geçiş dönemi: sunucuda sahip yoksa ilk açan cihaz sahiplenir. **Giriş ekranı üst düzen (başkan tasarımı):** ortadaki arma KALDIRILDI → SOL: MEVZU logosu (mevzu-icon-512, yuvarlatılmış) + SAĞ: teğmen. oturumDustu auth-context'e eklendi. **vCode 21 build bekliyor** (hesap seçici + şifre kod akışı + guard + tek oturum + logo düzeni — başkanın kalan test bulgularıyla toplu basılacak).
>
> ### ▶ 4 Tem AKŞAMÜSTÜ — GOOGLE GİRİŞ ÇALIŞIYOR ✅ + başkanın 2. tur bulguları (vCode 21'lik fixler HAZIR, build bekliyor)
> Başkan doğruladı: Google ile giriş ÇALIŞIYOR. Yeni bulgular + yapılanlar (commit `1097f2e?` fix'ler + bu kayıt): (1) **Google hesap seçici 2. girişte çıkmıyor** → signIn öncesi GoogleSignin.signOut() (kod, build ister). (2) **Şifremi-unuttum sayfası açılmıyordu** (guard onboarding'e fırlatıyor) → guard girişsiz-rota listesi (sifre-yenile/sifremi-unuttum/telefon-giris). (3) **Şifre sıfırlama KOD AKIŞINA çevrildi** (başkan kararı): e-posta → maildeki 6 haneli kod (verifyOtp recovery) → yeni şifre; recovery mail şablonu konu+gövde {{ .Token }} (CANLI); maildeki yedek link de çalışır. Başkanın "link yanlış hesabı sıfırlatır mı" endişesi: HAYIR — kod/link hangi adrese gönderildiyse yalnız o hesabı sıfırlar. (4) **Mail sıklık sınırı: aynı adrese saatte 5** (smtp_max_frequency=720, CANLI) + UI'da net "çok sık istedin" mesajı. (5) **TEK OTURUM İSTEĞİ (her hesap tek cihazda):** Supabase'in hazır düğmesi (sessions_single_per_user) **PRO PLAN İSTİYOR (~25$/ay)** — 402 döndü. KARAR BAŞKANDA: Pro'ya geç (düğmeye basınca biter; mail/yedekleme limitleri de artar, yayın için zaten mantıklı) YA DA kendi mekanizmamız (profiles.aktif_oturum + girişte üret/karşılaştır, eskisini düşür — 1-2 saatlik iş, ücretsiz, bir sonraki build'e). (6) Tutorial "tekrar izle" ZATEN VAR: Evsaf → Ayarlar → "Tanıtım turunu tekrar izle" (vCode 20'de canlı) — başkana söylendi. **vCode 21 build bekliyor:** hesap seçici + şifre kod akışı + guard fixleri telefonda görünmesi için (başkanın diğer test bulguları beklenebilir, toplu basılır).
>
> ### ▶ 4 Tem ÖĞLEDEN SONRA — GOOGLE GİRİŞ KOD 10 KÖK NEDENİ BULUNDU + DÜZELTİLDİ ✅ (Android client YOKMUŞ + consent Testing'miş)
> vCode 20 onaylandı, başkan test etti: Google girişte "kod 10 — imza/istemci uyuşmazlığı" (yeni teşhis sistemi gerçek sebebi gösterdi). **KÖK NEDEN 1:** Cloud mevzu-jsps projesinde Android OAuth client HİÇ YOKMUŞ — 3 Tem gece "oluşturuldu" kaydı YANLIŞTI (muhtemelen authuser kayması, kayıt hiç olmamış; listede yalnız web client vardı). **FİX (Brave CDP):** 2 Android client oluşturuldu: `MEVZU-JSPS-Android` (app.mevzujsps.android + App Signing SHA-1 9C:31..., ID 1065249322807-vdc1copjnq63cfi1tbs21kru8djif99f) + `MEVZU-JSPS-Android-Upload` (upload SHA-1 E2:E5..., yerel kurulum testleri için, ID ...-lctd8elqa9gi3j3crtpqgo1kqf78n625). Kod değişikliği GEREKMEZ (webClientId aynı kalır). **KÖK NEDEN 2 (bonus yakalandı):** OAuth consent screen "Testing" modundaymış, izinli tek test kullanıcısı dev.ademyilmaz — Android düzelse bile mayibey/testerlar bloklanacaktı → **"In production"a alındı** (yalnız temel scope'lar; Google doğrulaması gerekmedi). Play App Signing SHA-1 keymanagement sayfasından teyitli. **TEST:** başkan birkaç dk-saat sonra (propagation) Google girişi tekrar denesin; kod 10 sürerse tekrar bakılır.
>
> ### ▶ 4 Tem ÖĞLE — vCode 20 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅ (vCode 19 ONAYLANDI, testerlarda)
> Build `bash scripts/build-aab.sh` → BUILD SUCCESSFUL 6m5s, 47MB → `D:\buildler\mevzu-vCode20.aab`. Bundle içi teyit (AAB zip → base/assets/index.android.bundle; NOT: Hermes Türkçe metinleri UTF-16 saklar, ASCII anahtarlar utf8 aranır): sifremi-unuttum/telefon-giris/tanitim_gordu/sozlesme_onay/musterek_omurboyu_yukseltme/paket_yillik/obfuscatedAccountId/uygulama_ayar VAR; versionCode 20/1.0.10 ✓. Brave CDP: Kapalı test Alpha → release 21 → AAB (setInputFiles accept=.aab, 120s timeout gerekti) → sürüm notları → Kaydet → yayın özeti → "1 değişikliği incelemeye gönder" ONAYLANDI → **"İncelenmekte olan değişiklikler" (20/1.0.10)**. Tek uyarı: R8 mapping yok (zararsız, hep vardı). **vCode 19 (native Google giriş) ONAYLANMIŞ — testerlarda canlı**; başkan Google girişini 19'la şimdi test edebilir (hâlâ hata verirse 20'de gerçek sebep ekranda görünecek).
>
> ### ▶ 4 Tem GÜN — BAŞKANIN 14 MADDELİK LİSTESİ: TAMAMI KODLANDI (vCode 20 build BEKLİYOR)
> Başkan 14 maddelik büyük liste verdi; plan konuşuldu, onaylandı, hepsi yapıldı (tsc 0; 3 commit: `c207790` sunucu, `2eae5ca` ödeme, `61725df` hesap akışları; sürüm 20/1.0.10'a çekildi).
> **SUNUCUDA CANLI (build gerekmez, ŞİMDİ aktif):** (1) profiles.tanitim_gordu+sozlesme_onay kolonları + `uygulama_ayar` bayrak tablosu (telefon_giris='0') — docs/v2/10 çalıştırıldı. (2) dogrula-satinalma v3: ürün beyaz listesi (paket_yillik/paket_omurboyu + musterek/brans_omurboyu_yukseltme), yükseltmede aktif-yıllık şartı (geri yüklemede aranmaz), "başka hesaba bağlı" 409 net mesaj+kod. (3) Mail şablonları TÜRKÇE (doğrulama/sıfırlama vb) + **autoconfirm KAPATILDI** + doğrulanmamış girişe izin AÇIK (kapı uygulamada; mevcut kullanıcılar autoconfirm döneminden zaten onaylı). (4) `sms-gonder` hook deploy (MasGSM/SenaGSM köprüsü; SMS_KULLANICI/SMS_SIFRE/SMS_BASLIK/SEND_SMS_HOOK_SECRET secrets girilince aktif; verify_jwt=false, güvenlik webhook imzasında).
> **İSTEMCİDE (vCode 20 build'ine girecek):** paywall paket kartı + "Ömür boyuna yükselt" (yıllık sahibine, fark fiyatı) + obfuscatedAccountId=user_id + net red mesajları; Giriş/Kaydol SEKMELERİ; sözleşme onayı yalnız kayıtta (onay anı profiles.sozlesme_onay'a girişte bir kez); ayrı şifremi-unuttum ekranı; e-posta doğrulama kapısı (Mevzuat/Talim; offline fail-open); telefon-giris ekranı (uzak bayrak `telefon_giris` arkasında — MasGSM hazır olunca BUILD'SİZ açılır); tutorial HESABA bağlı (sunucu esas; mevcut testerlara BİR KEZ tekrar çıkar — hesapları işaretlensin) + Ayarlar'da "tekrar izle"; Google giriş GERÇEK hata sebebi ekranda (kod 10 = imza/istemci uyuşmazlığı vs); çıkış/hesap-sil çift-tık kilidi; branş ÖN SATIŞ metinleri (mevzuat/tatbikat/paywall). Yeni rotalar .expo/types/router.d.ts'e elle eklendi (expo start yeniden üretir, uyumlu).
> **BEKLEYEN İŞLER:** (1) ✅ **4 YENİ ÜRÜN AÇILDI (4 Tem, Brave CDP):** paket_yillik 1.979,99 (temel plan 'yillik' yıllık oto-yenileme, Etkin) · paket_omurboyu 2.699,99 · musterek_omurboyu_yukseltme 479,99 · brans_omurboyu_yukseltme 359,99 — hepsi Etkin, 177 ülke. NOT: Play fiyat girişi VERGİSİZ istiyor; Türkiye rafı %20 KDV'li gösterilir (girilen: 1649,99/2249,99/399,99/299,99). Mevcutlar: musterek_yillik 1.079,99 · musterek_omurboyu 1.559,99 · brans_yillik 1.439,99 · brans_omurboyu 1.799,99. Başkan fiyatları sonra güncelleyebilir (kural: yükseltme=fark, paket≈%20 indirim). (2) vCode 20 build (bash scripts/build-aab.sh) + kapalı test — başkan onayı bekliyor. (3) TELEFON DOĞRULAMA ERTELENDİ (başkan 4 Tem: "vazgeçtim, vaktim yok") — Türk sağlayıcılarda OTP/marka başlık ŞİRKET istiyor (Netgsm OTP kurumsal-only), şahısla süründürüyor; şahıs şirketi kurulunca İleti Merkezi/Netgsm ile açılacak. Altyapı hazır ve kapalı: sms-gonder deploy'lu, telefon-giris ekranı bayrak arkasında (telefon_giris='0'); açmak build GEREKTİRMEZ (secrets + auth config + bayrak '1'). (4) Google giriş vCode 19 testi (başkan dünkü native değişiklik SONRASI "işlem yapılamadı" aldı → yeni build'de hata sebebini söyleyecek; kod 10 çıkarsa Android client SHA-1/paket kontrolü). (5) Madde 14 (e-posta VEYA telefon ile giriş) telefon aktive olunca hazır. NOT: 4 Tem SABAH kaydındaki Admin-geri-çekme testi hâlâ açık.
>
> ### ▶ 4 Tem SABAH — ÖDEME ÇÖZÜLDÜ ✅ (satın alma çalışıyor) + AÇIK İŞLER
> Başkan: ödeme sorunu çözüldü, artık satın alabiliyor (401 gitti). NEDEN çözüldü KESİN değil: (a) servis hesabına dün gece geçici ADMIN (tüm izinler) verildi (ChatGPT Test C) + (b) ödeme profili banka deposit süreci — hangisi çözdü belirsiz. **GÜVENLİK-YAPILACAK:** servis hesabında hâlâ geçici ADMIN var → granular (Finansal görüntüleme + Sipariş yönetme) izne GERİ ÇEKİLMELİ; ama önce Admin geri çekince ödeme bozulur mu test et (bozulursa granular yetmiyor/propagation, Admin kalmalı; bozulmazsa ödeme profili çözmüştür). Test: satin_alma_log yeni kayıt durum=dogrulandi mı + uyelik_haklari hak yazıldı mı. **AÇIK İŞLER (bu oturumdan):** (1) Google giriş NATIVE yapıldı, vCode 19/1.0.9 incelemede — onaylanınca test (takılma bitti mi, Android client propagation birkaç saat). (2) Başkan YENİ sorun(lar) tespit etti (henüz anlatmadı — sohbet başında sor). (3) Tutorial spotlight (vCode 16) + ilerleme sıfırlama (vCode 17) + acknowledge fix (vCode 18) hepsi yüklü. **NATIVE GOOGLE DEĞERLERİ:** webClientId=1065249322807-rooson7kmj49lgt3hvfl25s41hnmappv (Supabase external_google_client_id ile aynı); App Signing SHA-1 9C:31:BB:88:69:BC:93:AC:1A:89:E4:CC:CD:AB:BB:B3:F9:2E:F1:4B; Upload SHA-1 E2:E5:1F:7E:39:38:0E:2D:FB:E9:1A:CD:8B:C9:AE:0F:D2:DC:EE:D2. Play developer u/6 dev.ademyilmaz, Cloud proje mevzu-jsps (authuser=6). **KAPALI TEST BUILDLER:** vCode 15-19 D:/buildler/. Son yuklenen incelemede: vCode 19.
>
> ### ▶ 3 Tem GECE (2) — NATIVE GOOGLE SIGN-IN + vCode 19 YÜKLENDİ
> Google giriş takılması (mail seçince supabase.co redirect döngüsünde kalma) için Supabase OAuth/WebBrowser YERİNE @react-native-google-signin (native hesap seçici → idToken → supabase.auth.signInWithIdToken; redirect/callback YOK). Cloud: Web client (webClientId=1065249322807-rooson7kmj49lgt3hvfl25s41hnmappv, Supabase external_google_client_id ile AYNI) + YENİ Android client (package app.mevzujsps.android + App Signing SHA-1 9C:31:...). Web tarafı eski Supabase OAuth (GoogleSignin native-only, dinamik import). Build vCode 19/1.0.9 BUILD SUCCESSFUL (native derleme, google-signin bundle teyit). Play Alpha release 20 → incelemeye gönderildi. Onaylanınca test: Google giriş takılmadan + supabase.co yerine marka. NOT: Android client Google propagation birkaç saat olabilir. SHA-1ler: App Signing 9C:31:BB:88:69:BC:93:AC:1A:89:E4:CC:CD:AB:BB:B3:F9:2E:F1:4B, Upload E2:E5:1F:7E:39:38:0E:2D:FB:E9:1A:CD:8B:C9:AE:0F:D2:DC:EE:D2.
>
> ### ▶ 3 Tem GECE — vCode 18 KAPALI TESTE YÜKLENDİ (google giriş + acknowledge fix)
> vCode 17 sonrası 2 kod fix: (1) Google giriş PKCE yarışı daha sağlam (auth.ts, 5 deneme+artan bekleme, ilk-basış takılması) (2) acknowledge/iade önleme (paywall, doğrulama gecikse bile satın alma acknowledge → iade yok). Build vCode 18/1.0.8, bundle teyit (acknowledge/isConsumable). Play Alpha → release 19 → incelemeye gönderildi ("İncelenmekte"). ÖDEME 401 hâlâ: en son deneme 14:28 UTC 401; kök neden ödeme profili (banka deposit 2-3 gün bekleniyor). Tester mail farklı-hesap = Play Store hesabı (respectylmz1) ≠ uygulama girişi (mayibey), Play Billing doğası, bizim mail göndermiyoruz. Sipariş yönetimi: tüm satın almalar (35.99 gerçek + 0 test) GERİ ÖDEME YAPILDI, kimse mağdur değil.
>
> ### ▶ 3 Tem ÖĞLE — vCode 16 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅ (3 iş tek build)
> Başkan "hepsini yapıp tek build bas, sonra sırayla test ederim" dedi. **Yapılanlar (keşif→fix, tsc 0):** (1) **Zayıf mevzi kaynak etiketi** — başkanın "Talim/Tatbikat ayrı" sezgisine karşı ÖNERİM: tek havuz kalsın + kaynak ETİKETİ. Uygulandı: `PerformansKaynak`'a `'genel'` eklendi (Tatbikat/genel deneme; DB kaynak TEXT sütunu → **şema göçü YOK**), `sinav.tsx` genel modda `kaydetPerformans('genel',...)`, `performans.ts kotuMu` genel+yanlış kötü sayar + `ZayifKart.kaynaklar {talim, tatbikat}`, `sicil.tsx` her zayıf satırda "Talim"/"Tatbikat" rozeti. (2) **İndirme MB** — `indirme.ts IndirmeIlerleme/AktifIndirme` + `inenBayt` (her dosya bitince cihazda kapladığı boyut toplanır), hook + buton "%X · Y MB". (3) **Tutorial spotlight** — `uygulama-turu.tsx` koyu lacivert sahne + LinearGradient + merkezde parlayan altın ışık halkası (iç içe glow daireler) içinde bölüm simgesi + "N/4 · EKRAN" rozeti; altın "İleri/Başla" butonu. **Build:** `bash scripts/build-aab.sh` → BUILD SUCCESSFUL 4m33s, 47MB → `D:\buildler\mevzu-vCode16.aab` (bundle içi teyit: "Tatbikat"+"inenBayt" var). **versionCode 16, version 1.0.6.** **Brave CDP:** Alpha → yeni sürüm (release 17) → AAB yükle → İleri → Kaydet → Genel bakış → incelemeye gönder → onay. **Durum: "İncelenmekte olan değişiklikler"** (16/1.0.6). **NOT:** vCode 15 (release 16) da incelemedeydi; 16 > 15 supersede eder, testerlar 16'yı alır. **BAŞKAN TEST (onaylanınca):** ses ipucu bekleme, zayıf Talim/Tatbikat etiketi, indirme MB, spotlight tur, 3 genel deneme. **ÖDEME TESTİ ŞİMDİ (build beklemez):** premium ekranından "geri yükle" → kilit açılıp taç çıkmalı (servis hesabı izni verildi).
>
> ### ▶ 3 Tem AKŞAMÜSTÜ — vCode 17 KAPALI TESTE YÜKLENDİ (ilerleme sıfırlama + zayıf mevzi fixleri)
> Başkan "tüm kullanıcıların ilerlemesini sıfırla, temiz test" istedi + eski cihaz verisini de otomatik silen bir mekanizma. **YAPILANLAR (bu oturum, tsc 0):** (1) **Tek seferlik ilerleme sıfırlama** (`senkron.ts`): `senkronYukle` içinde BULUT ÇEKİMİNDEN ÖNCE `ilerlemeSifirlamaMigrasyonu` → cihaz SQLite (srs/study_days/kart_performans/sicil/geri_bes/sinav) + yarım sınav + favori/arama + kullanıcının `kullanici_ilerleme` bulut yedeği silinir → boş bulut çekilir (geri yükleme yok). AsyncStorage bayrağı `jsps.ilerleme.sifirlamaTohumu` = `SIFIRLAMA_TOHUMU (1)` → bir kez çalışır; TOHUM artırılınca herkeste tekrar. Referans veri (laws/cards) korunur. Tester sadece güncellemeyi açar → oturumu zaten açık → otomatik sıfırlanır (sil-kur YOK). **Bulut elle SİLİNMEDİ** (başkan kararı: migration halletsin; 14 yedek her tester girince kendiliğinden silinecek). (2) **Zayıf mevzi tutarlılığı** (`gorsel-kaynak.ts` calisilabilirZayifMevzi + `sicil.tsx` + `sicil-servis.ts`): Karargah/Evsaf/emir/çalış 4 yer AYNI (çalışılabilir=indirilmiş) sayar; emir/ceza sadece çalışılabilir üzerinden → çalışılamayan mevzi için sonsuz ceza YOK. (3) **Kilit-farkında mesaj** (`kartKlasoru` + `kanunErisilebilir`): çalışılamayan zayıf kilitliyse "üyeliğinle açılır", erişilebilir-inmemişse "Mevzuat'tan indir" (kilitli kanun İNDİRİLEMEZ, "indir" yanıltıcıydı). **Build:** `mevzu-vCode17.aab` 47MB (bundle teyit: sifirlamaTohumu/kartKlasoru/calisilabilirZayifMevzi). **versionCode 17, version 1.0.7.** **Play:** Alpha → release 18 → İleri→Kaydet→incelemeye gönder → **"İncelenmekte olan değişiklikler" 17 (1.0.7)**. Onaylanınca testerlar güncellemeyi alıp açınca ilerleme otomatik sıfırlanır. **NOT:** Bu tester'ın 50+ zayıf sorunu = ödeme (kilitli içerik) yan etkisiydi; ödeme çözülünce üyelik→kanun açılır→zayıf çalışılabilir. **AKŞAM: ödeme testi** (401 propagation).
>
> ### ▶ 3 Tem AKŞAM — ÖDEME 401: önceki "propagation kanıtı" YANLIŞTI + ödeme profili şüphesi + izin tazelendi
> Başkan "propagation'a %100 emin misin" diye sordu → DÜRÜST TEKRAR TEST (geçici sa-test4 edge fn, sonra silindi): fresh token ile TÜM Purchases endpoint'leri **401** (products.get, subscriptionsv2, voidedpurchases düzgün-param). **ÖNCEKİ "voidedpurchases çalışıyor" ÇIKARIMI HATALIYDI** — o 400, param 30 günden eski olduğu için AUTH'tan ÖNCE reddediliyordu; param düzeltilince O DA 401. Yani servis hesabının Play API'ye erişimi HİÇ yok, propagation "kanıtı" geçersiz. **Google resmî rehberi (getting_started) okundu:** Cloud proje BAĞLAMA (link) ARTIK GEREKMİYOR; sadece Users&permissions → "Finansal görüntüleme" + "Sipariş yönetme" izni yeterli — ki İKİSİ DE verili+işaretli (ekran teyit). Yani kurulum Google rehberiyle BİREBİR doğru. **YENİ GÜÇLÜ ŞÜPHE:** hesap ana sayfasında kırmızı "Google satıcı ödeme yöntemini doğrulayamadı" + 31 Tem hesap kaldırma uyarısı → ödeme profili doğrulanmamış hesaplarda Google finansal/Purchases API'yi KISITLAYABİLİR → 401'in gerçek nedeni propagation DEĞİL, ödeme profili olabilir (WebSearch net doğrulamadı ama güçlü aday). **AKSIYON:** izin TAZELENDİ (finansal 2 izin uncheck→kaydet→re-check→kaydet, Brave; Google'a taze sinyal). **KALAN 2 kök neden (ikisi de kod değil):** (1) izin yayılımı 24+ saat, (2) ÖDEME PROFİLİ doğrulama (başkanın banka/kimlik işi — ZATEN acil, muhtemelen 401 nedeni). **PLAN:** başkan ödeme profilini doğrulasın (payments.google.com) → hem hesabı kurtarır hem muhtemelen 401'i çözer; yarın tekrar test. **NOT:** kod tarafı tamam (acknowledge fix: doğrulama gecikse bile satın alma acknowledge edilir → iade önlenir, `372f510`).
>
> ### ▶ 3 Tem ÖĞLEDEN SONRA (2) — ÖDEME 401 (sa-test: voidedpurchases 400'ü YANILTICIYDI, düzeltme yukarıda)
> Tester hâlâ 401. Derin, KESİN teşhis (geçici `sa-test`/`sa-test2`/`sa-test3` edge fn deploy edilip GOOGLE_SA ile gerçek androidpublisher çağrıları yapıldı, sonra HEPSİ SİLİNDİ): **fresh token ile aynı servis hesabıyla:** (a) `purchases/voidedpurchases?startTime=...&endTime=...` (FİNANSAL yetki) → **400** "Start time must be within 30 days of data" = **YETKİ GEÇTİ**, endpoint çalışıyor (sadece param eskiydi). (b) `purchases/products/musterek_omurboyu/tokens/FAKE` → **401 permissionDenied** (yetki geçmedi). (c) `inappproducts` → 403 (farklı yetki grubu, alakasız). **SONUÇ:** İzinler Play'de tam+işaretli (Finansal görüntüleme + Sipariş yönetme, ekran teyit), servis hesabı bağlı, Cloud API enabled, financial yetki bir komutta ÇALIŞIYOR (voidedpurchases) ama Purchases.products.get henüz 401. İkisi AYNI izne bağlı ("Finansal görüntüleme" açıklaması: "Satın Alma İşlemleri API'sine erişebilir") → biri çalışıp diğeri çalışmıyorsa = **Google yetkiyi endpoint-bazında KADEMELİ yayıyor, henüz tamamlanmadı ama İLERLİYOR** (sabah verildi, öğleden sonra voidedpurchases geldi). Kurulumda HATA YOK, tamamen Google propagation gecikmesi (resmî 24 saate kadar). **KARAR (başkan): BEKLE** — akşam yeni test satın alması → loglardan doğrulanacak. Yayılım kanıtlı ilerliyor; products.get de gelince doğrulama geçer → hak yazılır → kilit açılır + taç. **NOT:** "test iptal maili" = doğrulama 401 → acknowledge yapılamıyor → 3 günde otomatik iade (ayrı adım değil, 401'in sonucu).
> Sabah servis hesabına Play finansal izni verilmesine rağmen tester HÂLÂ "ödeme doğrulanamadı" alıyor. `satin_alma_log` yeni denemeler (08:23 UTC) hâlâ **401 permissionDenied**. **DERİN TEŞHİS (hepsi DOĞRU çıktı):** (1) Geçici `sa-check` edge fn deploy edip GOOGLE_SA kimliğini okudum (private_key HARİÇ) → `client_email=play-dogrulama@mevzu-jsps.iam.gserviceaccount.com`, `project_id=mevzu-jsps` → Play'de izin verdiğim hesapla **BİREBİR AYNI** (mismatch YOK); fn hemen SİLİNDİ. (2) Token alınıyor (kimlik geçerli). (3) Google Cloud Console (mevzu-jsps, **authuser=6** — Brave'de sayfa her yenilenişte authuser düşüp yanlış hesaba/MFA'ya kayıyordu; authuser=6 KORUYARAK doğru açıldı) → "Google Play Android Developer API" **API Enabled**. (4) Finansal izin bu oturumda (3 Tem, birkaç saat önce) İLK KEZ verildi — sabah servis hesabı ekliydi ama "Uygulama izinleri boş"tu. **SONUÇ:** Elle kontrol edilebilen her şey doğru → kalan tek makul neden **Google izin YAYILIMI** (resmî "24 saate kadar"). **Tester'ın gözlemi (kök nedeni doğruluyor):** ödeme başarılı+fatura → uygulama "doğrulanamadı" → geri-yükle "aktif satın alma yok" → bir süre sonra "test onaylanmadığı için iptal, acknowledge sağlayın" iade maili. Bu ZİNCİR: doğrulama 401 → sunucu acknowledge YAPAMIYOR → Google 3 günde otomatik iade. "Test onayı ver" diye elle adım YOK; 401 çözülünce acknowledge otomatik olur. **PLAN:** Başkan yarın (izin yayıldıktan sonra) yeni test satın alması yapacak → loglara bakılıp doğrulanıp doğrulanmadığı görülecek. Yarın da 401 verirse → propagation değil, Play Console "Cloud projesini API erişimine bağlama" (linkage) adımına bakılacak. **NOT:** iletisim@mevzujsps.com Hostinger'da yönetiliyor (başkan kutuya erişiyor, doğrulama kodlarını aldı).
>
> ### ▶ 3 Tem SABAH — ÖDEME KÖK NEDEN (servis hesabı Play izni) + ses ipucu fix
> **Tester bulgusu (gerçek para):** Evsaf → premium'dan test ürünü aldı, Google "başarılı" dedi ama uygulamada "satın alma doğrulanamadı", ürüne tekrar tıkla → "bu öğe zaten sizde var", taç YOK, kilitler açılmadı; dün 36 TL ödeyen tester de müşterek kilitli. **KÖK NEDEN (Management API `satin_alma_log` + `ham_yanit` ile KESİN):** `dogrula-satinalma` Google Play Developer API'ye satın almayı sorunca **HTTP 401 permissionDenied** ("The current user has insufficient permissions") dönüyordu → hak HİÇ yazılamıyor (`uyelik_haklari` BOŞ) → doğrulama başarısız → finishTransaction olmuyor → Google "zaten sizde var" diyor + kilit açılmıyor + taç yok. **Kod DOĞRU; sorun Play ayarı:** servis hesabı `play-dogrulama@mevzu-jsps.iam.gserviceaccount.com` Play Console'a kullanıcı olarak eklenmiş ama **HİÇBİR izni yoktu** (Uygulama izinleri boş). **ÇÖZÜLDÜ (Brave CDP otomasyonu):** Kullanıcılar ve izinler → servis hesabı → Yönet → **Hesap izinleri** sekmesi → **"Finansal verileri, siparişleri ve iptal anketine verilen yanıtları görüntüleme"** (Satın Alma İşlemleri API erişimi) + **"Siparişleri ve abonelikleri yönetme"** (acknowledge için) işaretlendi → Değişiklikleri kaydet → reload sonrası ikisi de işaretli KALDI (kaydedildi teyit). **TEST:** başkan uygulamada premium ekranından "satın alımları geri yükle" → doğrulama tekrar tetiklenir → bu sefer 401 yerine geçmeli → hak yazılır → kilit açılır + taç çıkar. (Google izin yayılımı bazen birkaç dk-saat sürebilir; hemen olmazsa biraz bekle.) **NOT:** Bu, "Google satıcı ödeme yöntemini doğrulayamadı / hesap 31 Tem'de kalkacak" uyarısından AYRI konu — o hâlâ başkanın banka/kimlik işi. **SES İPUCU FIX (`kod`, tsc 0):** TCK'da kart açılınca "bu kart nasıl çalışır" ipucu gelirken kullanıcı "Anladım" demeden ses otomatik başlıyordu → ses artık ilk-açılış ipucu (`kartIpucu`) kapanana kadar mount EDİLMEZ (akis.tsx satır ~514: `ogrenebilir && !kartIpucu`); "Anladım" → ipucu kapanır → ses başlar. **Bir sonraki build'e girer (telefonda görünmesi için).**
>
> ### ▶ 3 Tem GECE — vCode 15 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅ (final iş listesi)
> Başkan uzun final iş listesi verip yattı ("hepsini hallet, build al, kapalı teste yükle"). **Yapılanlar (docs/FINAL_IS_PLANI.md F1–F9):** (F1) 3 genel deneme verisi `scripts/genel-deneme-uret.mjs` → `src/assets/genel-denemeler.ts` (3×50=150 soru, benzersizlik teyitli). (F2) `sinav.ts` genel deneme mantığı + **2 PUAN** (`puanlaSinav` → puan=doğru×2, `getGenelDenemeSorulari`); `sinav.tsx` genel mod (`genel` param, sanal law_id=-no, tüm kartlar havuzu), skor "X/100 puan". (F3) **hatalı soru özeti** (açılır liste) + her yanlışta doğru cevap + açıklama + **"İlgili kartı çalış" → /akis** (kaynak madde↔kart eşleşmesi); `HataKart` komponenti. (F5) `tatbikat.tsx` **Talim/Tatbikat segment** (müşterek altı) → Tatbikat = Genel Deneme 1/2/3 listesi (kilit + son puan). (F6) **KILIT_AKTIF=true** (kilit simgeleri aktif; ⚠️ testerların erişmesi için Play ürünleri + ödeme profili doğrulaması ŞART). (F7) branş "çok yakında + %50 indirim" (mevzuat/tatbikat/paywall). (F8) bildirim ikonu beyaz silüet (bildirim-ikon.png). **Auth düzeni:** Google butonu ÜSTTE (birincil), "ya da e-posta ile" ayırıcı, Apple KALDIRILDI, `prompt=select_account` (hesap seçici hep çıkar). **Simge:** adaptive icon (mevzu-icon-adaptive.png, güvenli-alan ortalı). **app.json:** versionCode 15, version 1.0.5. **Build:** `bash scripts/build-aab.sh` → 47MB `D:\buildler\mevzu-vCode15.aab` (Genel Deneme + GENEL_DENEMELER + KILIT_AKTIF bundle teyit). **Brave CDP otomasyonu:** Alpha kapalı test → yeni sürüm (release 16) → AAB yükle (setInputFiles input[accept=.aab], timeout 120s) → İleri → Kaydet → Yayın özeti → "değişikliği incelemeye gönder" onaylandı. **Durum: "İncelenmekte olan değişiklikler"** (15/1.0.5, Kapalı test-Alpha; Google incelemesinde). **ERTELENEN (bir sonraki build — FINAL_IS_PLANI F4/F8-7/F8-5):** zayıf mevzi Talim/Tatbikat AYRIMI (schema v25 migration + 4-dosya senkron; şu an genel deneme yanlışları tek zayıf havuza düşüyor), indirme %→MB gösterimi, tutorial spotlight yeniden tasarımı. **BAŞKANDA (kod değil, ACİL):** ödeme profili doğrulama (hesap 31 Tem'de kalkacak uyarısı Play üstte kırmızı), 1 TL test ürünleri, ekstre işyeri adı + fatura adresi, fatura iletişim e-postası.
>
> ### ▶ 2 Tem GECE — vCode 14 KAPALI TESTE YÜKLENDİ + İNCELEMEYE GÖNDERİLDİ ✅ (Brave otomasyonu)
> vCode 14 (1.0.4) AAB `bash scripts/build-aab.sh` → 47MB `D:\buildler\mevzu-vCode14.aab` (aktifHaklar+getAvailablePurchases bundle teyit). **Brave CDP + playwright-core otomasyonuyla Play Console'a yüklendi:** Brave --remote-debugging-port=9222 --restore-last-session ile yeniden başlatıldı → play.google.com/console/u/6 (dev.ademyilmaz, "Mevzu" hesabı) → MEVZU-JSPS HAZIRLIK → Kapalı test → Alpha → yeni sürüm oluştur (release 15) → AAB yükle (setInputFiles input[accept=.aab], timeout 120s; filechooser gerçek tarayıcıda takıldı, doğrudan input çözdü; önceki denemelerden 2 duplicate oluştu ama geçerli App bundle 14 işlendi) → İleri → Kaydet → Yayın özeti → "Değişikliği incelemeye gönder" onaylandı. **Durum: "İncelenmekte olan değişiklikler"** (14/1.0.4, Kapalı test-Alpha; Google incelemesi ~7 güne kadar; "Değişiklikleri kaldır" ile geri çekilebilir). İçerik: premium taç+Üyeliğim+geri-yükle fix, tanıtım turu, zayıf teyit, yasal metinler. **Onaylanınca testerlara düşer** → arkadaş Play'den 14'ü indirip ödeme+taç+giriş dahil test edebilir. **NOT:** Play Console'da "ödeme hesabında acil sorun" bildirimi görüldü (başkanın ekstre/adres/ödeme profili konusuyla ilgili olabilir — bakılmalı). Brave CDP modunda kaldı (başkan isterse kapatıp normal açar).
>
> ### ▶ 2 Tem GECE — ÖDEME BUG KÖK NEDENİ ÇÖZÜLDÜ (GOOGLE_SA bozuk secret) + premium sahiplik gösterimi
> **Tester bulgusu:** tek seferlik ürün aldı → Google başarılı → uygulamada "ödeme doğrulanamadı" → geri-yükle "yenilendi" dedi ama sahiplik görünmedi. **KÖK NEDEN (Management API ile `satin_alma_log` + `uyelik_haklari` sorgulanarak KESİN bulundu):** `dogrula-satinalma` Edge Function'da `GOOGLE_SA` secret'ı **bozuk JSON** kaydedilmişti → `JSON.parse` satır 46'da patlıyordu ("Expected property name at position 1") → her doğrulama 500, hak HİÇ yazılmıyordu (uyelik_haklari boş). **DÜZELTİLDİ:** servis hesabı dosyası (`C:\...\mevzu-jsps-0857dbdd570f.json`, play-dogrulama@mevzu-jsps) Management API `POST /secrets` ile DOĞRU yazıldı (PowerShell kaçış sorunu elle atlanarak; body 2474 char, önceki bozuk 449444'tü). **TEYİT BEKLİYOR:** tester Premium ekranını açınca doğrulama tetiklenmeli → log + tablo kontrol edilecek. **KOD DÜZELTMELERİ (commit, tsc 0):** (1) **paywall geri-yükle GERÇEK doğrulama** — önceden `restorePurchases` yalnız native liste getirip koşulsuz "yenilendi" yeşili gösteriyordu (yanıltıcı); artık `getAvailablePurchases` → her satın almayı sunucuda doğrula → gerçek hak varsa başarı + finishTransaction, yoksa "bulunamadı". (2) **Kral tacı** 👑 — Karargah sağ üstünde altın taç (premium ise), dokununca Üyelik. (3) **Evsaf "Üyeliğim" kartı** — aktif paketler (Müşterek/Branş), tip (ömür boyu/yıllık), yıllıkta yenilenme tarihi. (4) uyelik-context `aktifHaklar` + urunler.ts `urunBilgi`. **BAŞKAN PANEL İŞİ (araştırılacak):** ekstrede "Google Adem YILMAZ" işyeri adı + fatura ev adresi gizleme (Play ödeme profili). **Bunlar bir SONRAKİ build'de** (Apple butonu kaldırma + istenirse kilit ile birlikte).
>
> ### ▶ 2 Tem GECE — vCode 13 AAB HAZIR (bugünkü tüm işler + tanıtım) → kapalı teste yüklenecek
> `bash scripts/build-aab.sh` → **BUILD SUCCESSFUL 3m35s, 47MB** → `D:\buildler\mevzu-vCode13.aab`. **Bundle içi teyit:** versionCode 13/1.0.3 ✓, tanıtım turu (`jsps.tanitim.tamam`) ✓, bağlamsal ipucu (`jsps.ipucu`/`kart-akisi`) ✓, yeni gizlilik metni ("ad, soyad, telefon") ✓, KILIT_AKTIF kodu var (değer false → testerlar tüm içeriğe erişir). **İçerir (2 Tem günü boyunca):** yasal metin düzeltmeleri (Google reddi/KVKK riski kapandı) + Google dahil tüm yollarda rıza kutusu, Edge premium kapısı+token güvenliği (KOD; deploy başkanda), UX/a11y, bildirime tıklama, takdir belgesi eşiği, zayıf havuz objektif teyit, ilk-kullanıcı tanıtım turu (HERKESE bir kez). **Mail:** Resend bağlı+çalışıyor doğrulandı (smtp.resend.com, autoconfirm açık → kayıt maili beklemiyor). **KALAN (başkan):** (1) vCode 13'ü **kapalı teste** yükle (üretime DEĞİL) → test turu (2) Play Data Safety formu (checklist docs/YAYIN_ONCESI_DENETIM_2TEM.md) (3) test bitince üretim + kilit açma paketi (docs/v2/09 SQL + Edge deploy + KILIT_AKTIF). **BEKLENEN:** gerçek JSPS sınav tarihi (geri sayım hâlâ 2 Eyl placeholder). Üretim 14-gün kapısı AÇIK ama acele yok — önce kapalı test doğrulaması.
>
> ### ▶ 2 Tem GECE — İLK KULLANICI TANITIMI (tur + bağlamsal ipuçları) — başkan isteği
> Başkan: ilk yükleyen kullanıcıya uygulamanın her yerini tanıtan rehber gerek. **Karma yaklaşım uygulandı (tsc 0, AsyncStorage — yeni DB fn YOK):** (1) Onboarding `Tanitim` tek "değer" ekranından **çok-slaytlı tura** dönüştü: kapak + 4 tur slaytı (Karargah / kartlar-nasıl-çalışır / Talim-sınav / zayıf-mevzi-teyit), nokta göstergesi + "Geç" + "İleri/Başla" (mevcut değer ekranı kapak olarak korundu). (2) Yeni `components/tanitim/ipucu-overlay.tsx` (Modal, krem tema, ikon+madde listesi + "Anladım"). (3) **İlk kez kart akışı** açılınca bağlamsal ipucu (sesli anlatım / madde metni / kaydır / Öğrendim-Tekrar). (4) **İlk kez sınav** açılınca ipucu (şık seç / önceki / devam / takdir). (5) `lib/ipuclari.ts` — "görüldüyse bir daha çıkma" (cihaz-yerel, `jsps.ipucu.*`). **Not:** tanıtım turu yalnız yeni (girişsiz) kullanıcıda; ipuçları ilk-açılışta bir kez. Backlog'daki "tutorial ERTELENDİ" maddesi KAPANDI ([[kayit-tasarim-ve-ertelenenler]]). **Build 13 bu yüzden iptal edildi → tutorial dahil yeniden alınacak.**
>
> ### ▶ 2 Tem GECE — ZAYIF HAVUZ OBJEKTİF TEYİT (başkan tasarım kararı) + sınav tarihi bekleniyor
> Ters teşvik çözümü (başkan): "zayıf mevzileri çalıştıktan sonra sadece 'biliyorum' demek yetmesin; o kartların sorularını çözerek gerçekten öğrenip öğrenmediğini teyit et." **Uygulandı (`?` bu commit, tsc 0, şema DEĞİŞMEDİ):** yeni saf `teyitSorulari(kartlar)` (her zayıf kartın maddesine ait 1 soru → {soru, cardId}; kart→soru madde_no+birleşik üyeler ile KART_SORULARI[law_id] eşleşmesi, gerçek veri formatı doğrulandı 5237 m.1/1 ↔ kart m.1); `HatirlaQuiz`'e opsiyonel `onSonuc`+başlık (geriye uyumlu); `akis.tsx` zayıf Etüt bitince **TEYİT ekranı** → doğru `kaydetPerformans('quiz','dogru')` kart çıkışa sayılır, yanlış `'quiz','yanlis'` havuzda kalır. Mevcut 2-ardışık-iyi kuralı değişmeden çalışır: biliyorum+teyit-doğru→çıkar, zor veya teyit-yanlış→kalır. **NOT:** sistem "spesifik yanlış soru"yu tutmuyordu → zayıf kartın maddesine ait soruyla teyit (pratikte aynı, kart zaten o soru yüzünden zayıf). Ödül tarafı zaten objektifti; artık ceza/çıkış tarafı da objektif → öz-rapor kaçışı KAPANDI. **BAŞKAN TEST ETMELİ:** zayıf mevzi çalış→tur sonu teyit soruları→doğru yaptığın mevzi kapanıyor mu. **BEKLENEN:** gerçek JSPS sınav tarihi (geri sayım hâlâ 2 Eylül 2026 placeholder; başkan netleşince verecek → tek satır).
>
> ### ▶ 2 Tem GECE — FİNAL DERİN DOĞRULAMA (4 ajan) + gerçek düzeltmeler (başkan: "sistem gerçekten yazıyor mu")
> Başkan final öncesi "test edilmeyen ne kaldı, ikazlar/sistem gerçekten çalışıyor mu, metinlerde hata var mı — her detayı düşün." 4 ajanla UÇTAN UCA doğrulama (ödül-ceza mantığı + bildirim gerçekliği + yazılı metin + hesaplama/parite). **SONUÇ: sistem GERÇEK çalışıyor, sahte/placeholder kod YOK** — ödül-ceza kalıcı SQLite'a yazıyor, ceza kademeleri (Yazılı İkaz→Uyarı→Kınama→Aylıktan Kesme) gerçekten artıyor+kalıcı, geri-bes emri gerçek tarih farkı, zayıf havuz giriş(objektif)/çıkış(2 ardışık iyi), bildirim gerçekten zamanlanıyor (3 içtima+rastgele fırsat, günlük tekrar, isimli kanal, çift-bildirim yok), sayılar tutarlı (63-vs-60 kapalı), yazım temiz. **Düzeltilen gerçek bulgular (`3f...` bu commit):** (1) **Takdir Belgesi eşik uyumsuzluğu** — sınav sonucunda tek test %100'de tam sertifika görseli çıkıyordu ama sicile ancak kanunun TÜM testleri %100'de yazılıyordu → gösterim de tüm-test şartına bağlandı (tek testte yönlendiren tebrik, sertifika yok). (2) **Bildirime tıklama ölüydü** (dinleyici kaldırılmıştı; eski tester "gece bildirimi tıklayınca açmıyor" bulgusu) → `bildirimTiklamaDinle` eklendi, tıklayınca Karargah açılır (uygulama kapalıyken açılış dahil). (3) **Metin cilaları:** auth rıza uyarısı eğik→düz apostrof; giriş marka MEVZU-JSPS→MEVZU · JSPS (tutarlılık); askeri tona uymayan emoji (👏/🙏→🎖️). tsc 0. **BAŞKAN KARARI BEKLEYEN 2 konu:** (a) **Sınav geri-sayım tarihi = 2 Eylül 2026 PLACEHOLDER** (Karargah'ta canlı gösteriliyor — gerçek JSPS tarihi girilmeli); (b) ters teşvik: zayıf havuz çıkışı öz-rapor ("biliyorum") tabanlı → teorik ceza kaçışı (ödül tarafı objektif/sağlam) — kabul mü, sıkılaştıralım mı?
>
> ### ▶ 2 Tem AKŞAM — YAYIN ÖNCESİ 4 KOLLU DENETİM + tüm "delikler" kapatıldı (başkan emri)
> Başkan: "yayına almadan önce her açıdan kontrol — güvenlik, kullanışlılık, görsel/tasarım, aklıma gelmeyen her şey." 4 bağımsız ajan denetimi (güvenlik + tasarım/UX + mağaza/yasal + kod sağlamlık), 29 Haz değerlendirmenin ÜSTÜNE sonradan eklenen ekranlara odaklı. Rapor: **docs/YAYIN_ONCESI_DENETIM_2TEM.md**. Başkan "benlik olanları ayır, diğer tüm delikleri kapatalım" → kod/metin delikleri kapatıldı (4 commit, tsc 0):
> **(1) YASAL — Google reddi/KVKK riski (`108eb1c`):** yasal metinler eski "hesapsız/offline" dönemindeydi, uygulama artık ad/soyad/telefon/DT/cinsiyet topluyor → çelişki = en sık red. Gizlilik metnine toplanan veriler+IP+satın alma eklendi; "şifre saklamıyoruz"/"yalnız Google giriş" yanlış iddiaları düzeltildi; **Google DAHİL tüm giriş yollarında KVKK rıza kutusu zorunlu** (önceden yalnız e-posta kayıtta); 18 yaş + abonelik/otomatik-yenileme/iade maddeleri; docs/index.html+sartlar.html senkron; MAGAZA_LISTELEME Data Safety tablosu "veri topluyor"a düzeltildi (+doğru cevap checklist'i).
> **(2) UX/a11y (`a421455`):** bildirim "Kaydet" butonu kırmızı→lacivert; onboarding ham İngilizce hata maskelendi; **hesap değişiminde favori+arama geçmişi de temizleniyor** (önceki kullanıcı sızıntısı — ilerleme zaten temizdi); bildirim stepper hitSlop+a11y etiket+dar-ekran sarma; izin reddinde "Ayarları aç"; Talim yükleme spinner; indir/paywall/geri-bildirim dokunma alanı+a11y; dekoratif arkaplan okuyucudan gizlendi.
> **(3) GÜVENLİK — kilit-açma paketi (`1b49d74`):** imzali-url+gorsel Edge Function'a **premium kapısı** (KILIT_AKTIF env; şu an yok/kapalı → testerlar etkilenmez, kilit açılınca `secrets set KILIT_AKTIF=1`); dogrula-satinalma'da **token yeniden-kullanım engeli** (makbuz paylaşımı→çoklu premium kapandı) + docs/v2/09 partial-unique index; Edge hata mesajları maskelendi. **DEPLOY+SQL BAŞKANDA** (kilit açma anında).
> **(4) TEMİZLİK (`0f0a58c`):** yetim giris-formu.tsx silindi; package.json 1.0.0→1.0.2; expo ^54→~54; config.ts+CLAUDE.md yorumları/palet güncel.
> **BAŞKANDA (kod değil):** Play Data Safety formu (checklist raporda), mağaza metni+gizlilik URL girişi, IBAN/Google doğrulaması, Edge deploy+09 SQL (kilit açınca). **Bu tur JS/metin → telefonda görünmesi için sonraki build'e (vCode 13) girer.**
>
> ### ▶ 2 Tem AKŞAMA DOĞRU — vCode 12 PLAY'E GÖNDERİLDİ ✅ (kapalı test Alpha, incelemede)
> Başkan onayıyla **vCode 12 (1.0.2) Google Play Console'a yüklendi ve incelemeye gönderildi** — mevcut **Kapalı test - Alpha** kanalı (yeni kanal AÇILMADI, mevcut kanala yeni sürüm). Geliştirici hesabı **dev.ademyilmaz@gmail.com** (u/6; mayibey DEĞİL — memory [[play-gelistirici-hesabi]]). Brave CDP otomasyonuyla: yeni sürüm oluştur → AAB yükle (12/1.0.2, arm64-v8a, API24+/SDK36, cihaz desteği vCode11 ile aynı) → sürüm notları (TR changelog: Google giriş/aksan-arama/bildirim) → İleri → tek uyarı (R8 mapping dosyası yok = zararsız, vCode11'de de vardı) → Kaydet → Yayın özeti → hızlı-kontroller geçti → "incelemeye gönder" onaylandı. Durum: **"İncelenmekte olan değişiklikler"** (Google incelemesi ~7 güne kadar; onaylanınca testerlara düşer). "Değişiklikleri kaldır" ile geri çekilebilir. Yükleme+gönderim TAMAM.
>
> ### ▶ 2 Tem ÖĞLEDEN SONRA — vCode 12 PRODUCTION AAB HAZIR (tüm fixler) + bildirim kanalları
> Başkan "tüm her şeyi yapalım" onayı → (1) **Bildirim isimli kanallar** (`5413cff`): test bulgusu bildirimler genel fallback kanalında+sessiz düşüyordu (channelId verilmiyordu) → 3 isimli içtima kanalı (Sabah/Gece/Fırsat) + Test kanalı, HIGH önem (sesli/heads-up), her bildirime channelId. (2) **versionCode 11→12, version 1.0.1→1.0.2**. (3) **vCode 12 PRODUCTION AAB** `bash scripts/build-aab.sh` → BUILD SUCCESSFUL 15m53s, 47MB → `D:\buildler\mevzu-vCode12.aab`. **Bundle içi teyit:** aksan-fold (akliniza civileyin) ✓, isimli kanal (ictima_sabah) ✓, Google retry (verifier×3) ✓, FLAG_SECURE aktif (TEST_MODU env pakete GİRMEDİ, preventScreenCapture var) ✓, versionCode 12 ✓. KILIT_AKTIF=false (testerlar tüm içeriğe erişir — memory kararı). **KALAN (başkan):** AAB'yi Play Console'a yükle (kapalı test) — yayına-basma tek-yön kapı, başkan onayı/kararı; istenirse Brave otomasyonuyla yükleme sürülebilir. **TCK m.25:** içerik kapsamı kararı (bug değil) — kod işi değil, fabrike EDİLMEDİ.
>
> ### ▶ 2 Tem ÖĞLE — ANDROID EMÜLATÖR TAM TURU + 2 fix (TAM RAPOR: docs/YAYIN_ONCESI_TEST_RAPORU.md)
> Emülatörde (Pixel 7/A14, test-modu APK: vCode 11 + tüm fixler + EXPO_PUBLIC_TEST_MODU=1) uçtan uca test. **Sağlam:** giriş/kayıt/onboarding, hesap silme→reaktivasyon→bulut geri-yükleme, kartlar (şifreli görsel yerelde çözülüp filigranlı render), ses (yerel mp3), Talim tam sınav→skor ekranı, öğrenme döngüsü (yanlış→zayıf havuz→Etüt), bildirim (test bildirimi düştü), **ÇEVRİMDIŞI (uçak modu: kartlar+ses offline çalışıyor)**, FLAG_SECURE üretimde aktif doğrulandı. **2 FİX:** (1) `0aaca1a` Google giriş "ilk denemede olmuyor" (arkadaş bulgusu) → kök: fresh-install'da exchangeCodeForSession PKCE verifier'ı okuyamadan sessizce düşüyor → **tek basışta iç-retry 3x/500ms**. (2) `c654f65` **aksan-duyarsız arama** (kisisel↔kişisel, gorev↔görev) → trKucuk aksan katlar; madde-etiket+civileAyikla uyarlandı; Node testi PASS. **Küçük gözlem:** bildirim genel kanalda+sessiz (v2 isimli kanal önerisi). **Geliştirme önerileri + başkana 4 soru raporda.** Test-modu şalteri: `1e47557`. Kritik/engelleyici hata YOK — yayına hazır. **KALAN karar: vCode 12 derle+yükle mi?** (tüm fixleri telefona taşır).
>
> ### ▶ 2 Tem SABAH — Başkan onaylı 2 iş: görev→sunucu + başlık tekrarı (özet)
> **(1) Branş/rütbe SUNUCUYA taşındı** (`c2aa9fe`): `profiles.brans/rutbe` kolonları (docs/v2/08, dashboard'dan çalıştırıldı) + `gorevGetir/gorevKaydet` (auth.ts; kaydet SEÇİM ANINDAKİ hesabı doğrular — çıkış+farklı-giriş yarışında çapraz yazma İPTAL) + context'lerde girişte sunucu ESAS (doluysa cihaza iner, boşsa cihazdaki temizlenir → GorevAdim bir kez sorar). Web'de canlı doğrulandı: iki hesap ayrı seçimlerle (jandarma/sb vs personel/uzmerb), geçişte otomatik doğru değer. **Mevcut kullanıcılar bir sonraki girişte BİR KEZ yeniden seçer** (sunucu boş olduğundan) — testerlara söylenebilir. **(2) "KVKK m.4 — Madde 4" tekrarı** (`f4493d2`): yeni saf `lib/madde-etiket` (jenerik "Madde N" başlığı numara zaten görünüyorsa gizlenir) → ara + günün maddesi + zayıf listesi; ayrıca aramada özet/ayırt kartı temsilci olamaz (başlık "m.8 ayırt" ham anahtar sızdırıyordu; normal kart öncelikli).
>
> ### ▶ 2 Tem GECE — Otonom gece testi: 3 kritik fix + tam tur (özet; TAM RAPOR: docs/GECE_TEST_RAPORU.md)
> Başkan emriyle uygulama tarayıcıdan (localhost:8081, Brave CDP + playwright-core) uçtan uca gezildi; Supabase paneli de kullanıldı. **(1) Web Google giriş donması ÇÖZÜLDÜ** (`9604222`): dönüş adresi platforma göre — native sabit `mevzu://` KORUNDU, web'de `window.location.origin`; Supabase Redirect URLs'e `http://localhost:8081(/**)` eklendi (listede yalnız :3000 varmış). **(2) Web'de bomboş kart ÇÖZÜLDÜ** (`7b53e69`): private bucket sonrası web public URL'den 400 alıyordu → yeni `lib/imzali-cache` (debounce'lu toplu imzalı-URL) + resolver'larda web dalı + `useImzaliTazele`; görsel+ses web'de canlı doğrulandı. **(3) HESAP DEĞİŞİMİNDE İLERLEME BULAŞMASI ÇÖZÜLDÜ** (`a83d6b9`, EN KRİTİK): farklı hesapla girişte önceki kullanıcının srs/sicil/sınav verisi yeni hesaba geçip ONUN bulutuna yazılıyordu (canlı kanıtlandı). Yeni public DB fn **`ilerlemeSifirla`** (4-DOSYA SENKRON tamam) + `senkron.ts` sahiplik modeli: girişte sahip-farklıysa yerel temizlik; `senkronKaydet` bu kopyada `senkronYukle(uid)` başarıyla bitmeden PUSH ETMEZ (`aktifVeriSahibi` modül hafızası — web OAuth popup'ı ikinci kopya açıp depo anahtarını erken yazabiliyor, o yarış da kapalı). Kirlenen bulut kayıtları elle temizlendi; mayibey 17/565 korunuyor, test hesabı 0'dan başlıyor. **(4) Metin fixleri** (`0135511`): /giris "isteğe bağlı" (yanlış), gizlilik "Formspree" (yanlış beyan → Supabase), TEŞHİS kutusu "ekle"→"listede OLMALI". Sağlam çıkanlar + başkana 4 soru raporda. Build ALINMADI (emir); fixler sonraki build'e (vCode 12 listesine) girer.
> ### ▶ 2 Tem — Silip-kur takılması (branş/rütbe boş) + boş beyaz kart (indirme kapısı) (özet)
> Başkan/tester iki gerçek bulgu (v10'da doğrulandı, silip-yeniden-kur senaryosu):
> **(1) Mevzuat/Talim sonsuz "Yükleniyor":** Kök neden — branş/rütbe YALNIZ cihazda (AsyncStorage) tutuluyor, sunucu profilinde değil. Uygulama silinince sıfırlanıyor; tekrar girişte sunucu profili (ad/soyad/telefon…) TAM geldiği için onboarding'in branş/rütbe adımı ATLANIYOR → `mevzuat.tsx`/`tatbikat.tsx` `if(!brans) return` → `laws` null kalıp ekran "Yükleniyor"da donuyordu. **Çözüm (`_layout.tsx` + `onboarding.tsx`):** kök gate'e `gorevEksik = !brans||!rutbe` eklendi → giriş yapmış + profili tam ama görev bilgisi boş kullanıcı onboarding'e; yeni **`GorevAdim`** (yalnız branş/rütbe SecimKutu'ları, kişisel bilgi sormaz) tek seferde seçtirir → seçince context güncellenir, gate ana ekrana atar. Tanıtım artık yalnız `girisGerek && !tanitimGecildi` (silip-kuran girişli kullanıcıya tanıtım tekrar gösterilmez). **Kalıcı kök çözüm (İLERİ İŞ):** branş/rütbeyi Supabase profiline de yaz (kolon eklemesi + profilKaydet/profilGetir) → silip-kurunca hiç sormadan gelir; şimdilik tek-seferlik yeniden-seçim yeterli.
> **(2) Boş beyaz kart:** Ara→"Sık Açılan Mevzuatlar" kısayolu ve Patika ekranı, içerik inmemiş kanunun kartına indirme KONTROLÜ OLMADAN gidiyordu (indirme kapısı yalnız Mevzuat listesi + Ara sonuç kartındaydı) → görsel çözülemeyip boş beyaz ekran. **Çözüm (`patika.tsx`):** indirme kapısı en dibe, PATİKAYA taşındı (`akisAc` = kart akışının tek önü). İçerik inmemişse yüzdeli "İndir ve aç" modalı → biter bitmez kart açılır; "Arka planda indir" iptal eder; hata→"Tekrar dene". `indirGerek = klasor && indirmeDestekli && ICERIK_TABANI && !kanunIndirilmisMi` (gömülü içerikli build/web'de kapı devre dışı → davranış değişmez). Tek düğüm + bölüm düğümleri hepsi `akisAc`'tan geçer → hiçbir yoldan boş kart çıkmaz. tsc 0. **JS fix → yeni build (vCode 12) gerekli.**
>
> ### ▶ 2 Tem — Günün maddesi (özet-sızma + doğrudan aç) + Patika başlık tam sığma (özet)
> Başkan/tester bulguları: **(1)** Karargah "Günün Maddesi"nde özet/ayırt birleşik kartları çıkıp ham anahtar sızıyordu ("Özet — ...ayirt"). Fix (`index.tsx`): aday filtresine `_(ayirt|ozet)(_|$)` deseni eklendi → yalnız NORMAL tek-madde kartları aday. **(2)** Günün maddesine tıklayınca tüm yönetmelik patikada madde 1'den açılıyordu → artık `/akis?lawId&kart=<id>` ile DOĞRUDAN o maddenin kartı açılır. **(5)** Patika üst kanun-adı `numberOfLines={1}` ile kesiliyordu → `numberOfLines={2}` + `hecele()` (heceli sarma) → tam sığar. tsc 0. **Ek:** Auth ekranı sağlayıcı butonları moda göre → giriş modunda "Google/Apple ile giriş yap", kayıt modunda "…ile kaydol" (`saglayici-butonlari.tsx` `kayit` prop + auth-ekrani `kayitMi`). **Not (tester eski build):** ilk-girişte-butonlar-pasif (görsel yüklenmeden) = vCode 11 render-reset ile çözülü; zayıf 104-vs-0 = calisilabilirZayif ile Karargah sayacı akışla hizalı (vCode 11). "Bazı kanunlarda Başla yok" + "madde 1 boş beyaz ekran" = spesifik kanun/build teyidi gerek (yeni build'de tekrar bakılacak).
> Sunucu-tarafı satın-alma doğrulaması bağlandı (kilit HÂLÂ KAPALI). **Ödeme profili:** IBAN girildi, Google doğrulaması bekliyor. **Servis hesabı:** Google Cloud'da `play-dogrulama@mevzu-jsps.iam.gserviceaccount.com` oluşturuldu + JSON anahtarı indirildi + Play "Kullanıcılar ve izinler"de finansal-veri + sipariş-yönetim yetkisi verildi (NOT: Play "API access" sayfası artık yok, Google kaldırdı → servis hesabı Google Cloud + Play Users&permissions yoluyla). **Supabase secret GOOGLE_SA** eklendi (CLI; eski SUPABASE_ACCESS_TOKEN expire'dı → kullanıcı yeni sbp_ üretti, .env güncellendi). **`dogrula-satinalma` Edge Function DEPLOY edildi** (proje vwmjrvolkbiofpkzzwef; canlı, yetkisiz POST→401 doğrulandı). **KALAN (final):** (1) kapalı testte gerçek satın-alma denemesi (2) `KILIT_AKTIF=true` + build → kilidi aç. Test bitmeden AKTİF ETME.
>
> ### ▶ 1 Tem — TESTER BULGULARI ESKİ BUILD'DEN + vCode 11 (google-giriş sabit mevzu://) (özet)
> Çok sayıda tester bulgusu geldi (google giriş beyaz-ekran, sınav 1/32 bölünmüyor, sınav 1.sorudan, kilitlenme, sayaç 63/60, buton kilidi, gece-bildirim, sorular 2-kez). "vCode 10 kullanıyorlar" sanıldı ama **AAB'leri açıp bundle'da string aradım:** vCode 8'de Premium(paywall)=0, vCode 10'da=1 → **testerlar aslında vCode 8'de** (app.json version="1.0.0" hepsinde aynı → numara yanıltıcı; teyit: Evsaf→Ayarlar→"Premium/Abonelik" satırı YOK = eski build, kullanıcı doğruladı). Yani bulguların ÇOĞU zaten çözülü, sadece eski build ellerinde. **Google giriş EK düzeltme:** vCode 10'da deep-link fix vardı; buna ek olarak `auth.ts` dönüş adresi `makeRedirectUri` yerine **sabit `mevzu://`** (expo-dev-client kurulu → makeRedirectUri bazı yapılarda exp+ şeması üretip allowlist ile eşleşmeyip web-Site-URL'ine [beyaz ekran] düşürüyor olabilir → sabit adres allowlist ile birebir). **vCode 11 üretildi** (`D:\buildler\mevzu-vCode11.aab`, bundle'da Premium+mevzu:// doğrulandı) = v8'den beri TÜM fixler. **KALAN (kullanıcı):** vCode 11'i kapalı teste yükle + testerlar SİL-yeniden-kur → gerçek yeni-build bulgularını topla. Belirsiz 2 konu (gece-bildirim tıkla-açmıyor, sorular 2-kez) muhtemelen eski-build; veride/kodda soru-mükerrer YOK (doğrulandı).
>
> ### ▶ 1 Tem — Zayıf mevzi sayacı tutarsız (63 vs 60) + görsel yüklenmeden buton kilidi (özet)
> **(1) Sayaç:** Karargah "63 zayıf mevzi" derken içeri girince "1/60" gösteriyordu. Kök: Karargah `getZayifKuyruk()` tam uzunluğunu (63), akış ise sunucu modunda SADECE indirilmiş kanun kartlarına filtreliyordu (60). **Çözüm:** ortak yardımcı `calisilabilirZayif()` (`gorsel-kaynak.ts`) → hem Karargah hem akış AYNI filtre → tutarlı. **(2) Görsel yüklenmeden buton:** `ogrenebilir` gate'i (Öğrendim/Tekrar/ses/madde/swipe) zaten vardı ama `gorselGorundu` reset'i `useEffect`'te (render'dan SONRA) → kart değişiminde bir kare önceki kartın 'true'su taşınıp butonlar erken açılabiliyordu. **Çözüm:** render anında sıfırla (React "önceki state'i render'da ayarla" deseni, `gorunenKartId`) → yeni kartta görsel yüklenene kadar KESİN kilit. tsc 0.
> **NOT — Bazı tester bulguları ESKİ BUILD kaynaklı (vCode 10'da/şu an ZATEN çözülü):** Google giriş "web/beyaz ekran açılıp giriş yapmıyor" = vCode 10 deep-link fix; sınavda "1/32" (bölünmüyor) = testlere-bölme (20'şer) vCode 10'da var. Testerlar YENİ build'e geçmeli.
>
> ### ▶ 1 Tem — Kart akışı BİR ANDA KİLİTLENİYOR → görsel çözmeyi JS thread'den kaldır (özet)
> Tester: kartları geçerken uygulama bir anda kilitleniyor (ileri-geri gitmiyor, ses kesiliyor, sesli-anlatım/madde-metni tıklanmıyor; yalnız hata-bildir çalışıyor). **Kök neden:** şifreli görsel çözme (`aesCoz`+`bytesToB64`, `lib/sifreleme.ts`) SENKRON ~0.5-1sn/görsel; `gorselOnCoz` her kart değişiminde mevcut+önceki+sonraki = **3 görseli AYNI ANDA** çözüyordu → JS thread ~2-3sn BLOKE → tüm UI (jest/dokunma/ses kontrolü) donuyor. **Çözüm (`gorsel-coz.ts`):** komşu ön-yükleme artık (1) `InteractionManager.runAfterInteractions` (swipe/animasyon bitince), (2) TEK TEK kuyrukla + aralarında `setTimeout(0)` nefesi (dokunmalar işlensin), (3) hızlı geçişte yalnız EN GÜNCEL komşular kuyrukta. Normal hızda komşular boşta önden çözülüp önbelleğe girer → sonraki karta geçince ANINDA gelir (bloke yok); yalnız çok hızlı geçişte tek-kart gecikmesi kalır. Mevcut kartın kendi çözmesi StudyCard'da (değişmedi). Kalıcı tam çözüm native AES (ertelendi). tsc 0.
>
> ### ▶ 1 Tem — GOOGLE GİRİŞ DÖNÜŞÜ ÇALIŞMIYORDU (deep-link) → çift-yol fix + vCode 10 (özet)
> Cihaz testi: Google ile giriş → web açılıp kapanıyor, ana ekrana dönüyor; **Supabase'e kullanıcı DÜŞÜYOR ama uygulama İÇERİ GİRMİYOR**. **Kök neden:** Android'de OAuth redirect app'e `mevzu://?code=...` **deep-link** olarak düşüyor → `WebBrowser.openAuthSessionAsync` 'dismiss' dönüyor → `code` hiç `exchangeCodeForSession`'a girmiyor → oturum kurulmuyor (ama Google/Supabase kaydı oluşmuş). Bilinen Expo+Supabase Android tuzağı; kod yalnız openAuthSession sonucuna bakıyordu. **Çözüm (çift-yol):** `auth.ts` `oturumKoduIsle(code)` (dedup: set + tek-uçuş → çift işlenmez) + `oauthUrlIsle(url)` (şifre-yenileme HARİÇ, code varsa oturuma çevir); `gmailIleGiris` artık oturumKoduIsle kullanıyor + 'dismiss'te sessiz dönüyor. `_layout.tsx` **Linking.addEventListener('url')** + getInitialURL → `oauthUrlIsle` → redirect app'e deep-link düşse bile oturum kurulur, onAuthStateChange(SIGNED_IN) tetiklenir → içeri girer. Yeni user profil-adımına, mevcut user app'e gider. tsc 0. **JS fix → yeni build gerekli: vCode 10.** (Supabase Site URL/Redirect `mevzu://` allowlist zaten vardı — sorun istemci-tarafı redirect yakalamaydı.)
>
> ### ▶ 1 Tem — KİLİTSİZ FİNAL AAB vCode 9 ÜRETİLDİ (47MB) + build Metaspace OOM fix (özet)
> `bash scripts/build-aab.sh` → **ilk deneme battı:** `react-native-screens compileReleaseKotlin` → `OutOfMemoryError: Metaspace` (Kotlin in-process → tüm modül sınıf metaverisi daemon metaspace'inde birikir; `withYerelBuild.js` metaspace **512m** dardı, heap 5120m yeterliydi). **Fix:** metaspace 512m→**1024m** (kalıcı, plugin'de). **İkinci deneme BAŞARILI (4m18s):** AAB `android/app/build/outputs/bundle/release/app-release.aab` **47MB** → `D:\buildler\mevzu-vCode9.aab` kopyalandı. Kilitsiz (KILIT_AKTIF=false), vCode 9. **KALAN (kullanıcı):** (1) Play Console'a vCode 9 AAB'yi yükle (kapalı test) (2) testerlar uygulamayı SİLİP yeniden kursun (tam sıfırdan) + Supabase Auth'tan eski test kullanıcıları sil (cascade: profil/ilerleme/geri-bildirim/üyelik) → sıfırdan test.
>
> ### ▶ 1 Tem — KİLİTSİZ FİNAL BUILD hazırlığı (KILIT_AKTIF=false, vCode 9) (özet)
> Başkan kararı: kilitsiz final sürüm bas → testerlar son testleri yapsın → SONRA kilit + yayın. **`KILIT_AKTIF = true → false`** (tek şalter; `kanunErisilebilir` hep true → hiçbir içerik kilitlenmez, testerlar her şeye erişir; paywall ekranı yalnız Ayarlar'dan bilgi amaçlı erişilir, hiçbir yeri zorlamaz — gating boşlukları da şalter kapalı olduğu için önemsiz). **app.json versionCode 8 → 9** (Play daha yüksek ister; vCode 8 kullanıldı). AAB: `bash scripts/build-aab.sh` (strip→prebuild--clean→gradle bundleRelease). tsc 0. **KİLİDİ AÇMAK İÇİN (yayın öncesi):** `KILIT_AKTIF=true` + gating boşluklarını tamamla (arama→indir-aç→/akis ve /akis doğrudan premium kilidini kontrol etmiyor) + GOOGLE_SA + dogrula-satinalma deploy + gerçek cihaz satın-alma testi.
>
> ### ▶ 1 Tem — Arama sonucu Mevzuat'a atıyordu → İNDİR VE AÇ (aranan karta git) (özet)
> Tester: arama sonucuna basınca ilgili karta değil Mevzuat'a gidiyor. **Kök neden:** `ara.tsx ac()` — kanun indirilmemişse (sunucu-içerikli gerçek build'de çoğu öyle) "Kanun indirilmemiş" uyarısı → **"Mevzuat'a git"** (tester #5'te bozuk-kart yerine eklenmişti); `/akis` zaten `kart` param'ıyla doğru karta atlıyor, sorun sadece bu kapıda. **Çözüm:** Alert artık **"İndir ve aç"** → `indirVeAc()`: yüzdeli indirme MODALI (durum yöneticisi `kanunIndirBaslat`+`indirmeDinle` — arka-plan duraklat/devam ile uyumlu) → **biter bitmez aranan KARTA gider** (Mevzuat'a atma yok). Hata → "Tekrar dene"; "Arka planda indir" modalı kapatır (indirme sürer) + `acilacakRef` guard → arka plana alınca bitince otomatik-zıplama YOK. Sadece UI (ara.tsx), DB'ye dokunulmadı. tsc 0.
>
> ### ▶ 1 Tem — Geri-besleme emrine KALAN SÜRE + ceza uyarısı (Evsaf) (özet)
> Başkan: emri yerine getirmek için kalan süre + "yapmazsan ikaz alırsın" mesajı motive eder. Evsaf → Ödül-Ceza Sicili'ndeki **emir kartı** (`sicil.tsx SicilBolum`) zenginleştirildi: **kalan gün** (bugünISO→sonTarih farkı, min 0) vurgulu geri-sayım çipi ("Görevi tamamlamak için X gün kaldı" / 0 ise "SON GÜN!") + **sıradaki ceza kademesini** açıkça yazan uyarı ("… kapatmazsan **{siradakiCeza}** cezası alırsın", siradakiCeza = KADEME_AD[min(kademe+1,4)] → kademe 0'da Yazılı İkaz). Sadece UI/metin — ceza mantığı (ZAYIF_ESIK=3, PENCERE_GUN=3, kademe merdiveni) DEĞİŞMEDİ, DB'ye dokunulmadı. tsc 0.
>
> ### ▶ 1 Tem — TALİM: kanun sınavları 20'şerlik TESTLERE bölündü (Test 1/2/3) (özet)
> Başkan: uzun kanunda tek seferde 50 soru yorucu → her testi ~20 soru, Test 1/2/3. **Kararlar:** test boyut **20** (son parça <8 ise öncekine katılır → tek-soruluk test olmaz, 41→20+21); **ödül: kanunun TÜM testleri %100 → Takdir Belgesi** (test bazlı). **Saf mantık** (`lib/sinav.ts`): `TEST_BOYUT`, `testSinirlari` (STABİL dilim → Test N hep aynı sorular), `testSayisi`/`testSoruSayisi`/`getTestSorulari` (dilim içi karıştırma). **DB göçü v24** (4-DOSYA SENKRON): `sinav_sonuclari`'ya **`test`** sütunu (0 tabanlı) → schema (SinavSonuc+CREATE_SQL+ALTER idempotent) + types + database.d.ts + native (ekle/get/import) + web (parite). `ekleSinavSonucu(lawId, test, dogru, toplam, tarih)`. **Ödül (sicil-servis):** eski "herhangi %100" yerine kanun→aced-test-set; `set.size >= testSayisi(law)` → kanun takdiri (aced = dogru===toplam VE toplam===testSoruSayisi → bölme-öncesi/eski kayıt bölünmüş kanunda sayılmaz). **Devam (sinav-ilerleme):** anahtar artık `lawId.test` → her testin yarımı ayrı saklanır. **sinav.tsx:** `test` parametresi, `getTestSorulari`, header'da "TEST k/N". **Talim (tatbikat.tsx):** kanun satırı → çok testliyse AÇILIR liste (Test 1..N, her biri soru sayısı + kendi son skoru + aced ise ✓); tek testli (≤20) doğrudan açılır; kilitliyse paywall. sonucMap law→(test→sonSonuc). tsc 0. (getSinavSorulari yalnız kart-akışı sonu mini-quiz'de kaldı — kasıtlı, tüm kanundan rastgele.)
>
> ### ▶ 1 Tem — Paywall "EN AVANTAJLI" → Ömür boyu + Evsaf'a geri-bildirim girişi (özet)
> Başkan: (1) paywall'da "EN AVANTAJLI" rozeti YILLIK'taydı, mantıken **ömür boyu** (tek ödeme, uzun vade) daha avantajlı → vurgu (koyu buton + rozet) Ömür boyu'na alındı, alt yazı "tek seferlik · hep senin". (2) **Geri-bildirim nereye düşüyor:** FORMSPREE_ENDPOINT boş → Supabase **`geri_bildirim`** tablosu (user_id, tip, mesaj, card_id, madde_no, baslik, kanun, created_at; RLS insert-own, okuma app'te yok) → **Supabase Dashboard → Table Editor → geri_bildirim**'den takip. Karttaki bildirim card_id+madde+kanun taşır (hangi kart belli). (3) **Evsaf'a "Hata / Öneri Bildir" satırı** eklendi (Ayarlar satırı altına) → `/geri-bildirim` (kart-bağımsız; aynı tabloya kart bilgisi olmadan düşer). tsc 0.
>
> ### ▶ 1 Tem — TALİM (sınav): ÖNCEKİ SORUYA DÖNÜŞ + KALDIĞIN YERDEN DEVAM (özet)
> Tester bulgusu: sınavda (1) önceki soruya dönüp ne işaretlediğini göremiyor, (2) ekrandan çıkınca baştan başlıyor. **Kök neden:** `sinav.tsx` durumu tek-yönlü (`index`+`secilen`+`cevaplar` push) + tamamen ekran hafızasında (çıkınca kaybolur, `yukle` sıfırlar); ayrıca `getSinavSorulari` her açılışta soruları KARIŞTIRIYOR. **Çözüm:** durum modeli **`secimler: (number|null)[]`** (soru başına seçilen şık) → tek kaynak; `secilen`=secimler[index], `cevaplar` bundan türetilir (useMemo). **(1) Önceki:** alt bara "Önceki" butonu (`geri` index--) → cevaplı soru salt-görüntüleme (şıklar kilitli, işaretlediği + doğru + açıklama görünür). **(2) Devam:** yeni `lib/sinav-ilerleme.ts` (AsyncStorage, key `jsps.sinav.ilerleme.<lawId>`) → **karıştırılmış soru sırası + işaretler + konum** birlikte saklanır (sıra sabit kalsın); `yukle` açılışta yarım sınav varsa KALDIĞIN YERDEN sürdürür; her cevap/gezinmede kaydeder; sınav bitince (skor efektinde) siler; **"Tekrar çöz"→`yenidenBasla`** (kaydı sil + YENİ karıştırılmış sınav). İlk cevapta zayıf-havuz logu bir kez (kilit sonrası tekrar yok). Yeni public DB fn YOK (AsyncStorage yerel; yarım sınav buluta senkron edilmez). tsc 0.
>
> ### ▶ 1 Tem — İNDİRME ARKA-PLANDA İPTAL OLUYORDU → DURAKLAT-DEVAM ET (özet)
> Tester bulgusu: kanun indirilirken uygulama arka plana alınınca indirme İPTAL oluyordu ("İndirilemedi"). **Kök neden:** `lib/indirme.ts` per-dosya `downloadAsync` paralel havuzu; arka planda OS ağ isteklerini keser → `indirTekRetry` 3 denemeyi ANINDA tüketip fırlatıyor (arka plandayken hepsi hemen düşüyor) → tüm `kanunIndir` reddediliyor → hook `durum='yok'` + Alert. Ayrıca ilerleme ekran state'indeydi (sekme değişince/dönünce kaybolur). **Çözüm (ön plan servisi İZNİ ALMADAN — duraklat-devam modeli):** (1) `onPlanBekle()` (AppState 'active' bekler); `indirTekRetry` artık her denemeden önce ön planı bekler, hata olduğunda uygulama arka plandaysa **başarısızlık saymaz** → ön planı bekleyip devam eder (yalnız ÖN PLANDAYKEN oluşan gerçek hatalar 3'te pes eder); denemeden önce yarım/bozuk dosyayı siler (temiz iner, devam-edilebilir korunur). (2) **Modül-içi reaktif durum yöneticisi** (`aktif` Map + `promiseler` tek-uçuş + `dinleyiciler`): `kanunIndirBaslat()` (çift indirme yok, yüzde yayar), `indirmeDurumuAl`/`indirmeDinle`. Hook artık bunu dinliyor → yüzde & durum ekran hafızasında değil modülde; sekme değişip dönünce / arka plandan gelince KAYBOLMAZ. **Sonuç:** arka plana alınca indirme DURAKLAR, öne dönünce kaldığı yerden OTOMATİK sürer; iptal + ilerleme kaybı yok. Yeni public DB fn YOK. tsc 0. **Kalan edge (nadir):** uygulama arka planda TAMAMEN öldürülürse indirme durur (per-dosya atlama sayesinde tekrar bas → kaldığı yerden); tam arka-plan indirme ancak ön plan servisi izniyle olur (bilerek almadık).
>
> ### ▶ 1 Tem — KİLİT (GATING) SİSTEMİ + tek ana şalter (ŞU AN AÇIK, önizleme için) (özet)
> Başkan: "kilidi koy, ödeme ekranının görünüşüne bakayım, sonra kaldırırız; build almayacağım." **Tek ana şalter** `KILIT_AKTIF` (`constants/urunler.ts`) → `kanunErisilebilir` İLK satırda kontrol eder: `false`=her şey açık, `true`=ödeme kilidi. **Tek satırla aç/kapa** (tüm ekranlar bu fonksiyondan geçer). ŞU AN **`true`** (başkan önizlesin diye). Kilit noktaları (blok bilgisi olan yerler): **(1) Mevzuat** satırı → kilitliyse "Kilidi Aç" altın çip + dokununca `/paywall` (indir/başla yerine); **(2) Patika** → kanunun bloğu yüklenince kilitliyse yol yerine yeni `components/premium/kilit-karti.tsx` (altın kilit + "Kilidi Aç" → paywall + "TCK ücretsiz" notu) — Karargah/arama/günün-maddesi hep patikadan geçtiği için tek noktadan kapsanır; **(3) Talim** satırı → kilitliyse sınav yerine paywall + kilit ikonu/"Kilidi Aç". TCK + denemesi hep ücretsiz (`ucretsizKanun`). Branş içeriği zaten "Yakında" (kilit görünmez). **Kilit sadece premium'a bağlı** (indir/çalış hazırlık kilidi ayrı, dokunulmadı). Yeni public DB fonksiyonu YOK → 4-dosya senkron gerekmedi. tsc 0. **AÇIK EDGE (final gating pass'te kapanacak, önizleme için önemsiz):** arama sonucundan doğrudan karta git (`/akis?kart`) patikayı atlar. **KALDIRMAK İÇİN:** `KILIT_AKTIF = false` (tek satır) + yeni build. Test edenler eski sürümde → etkilenmez.
>
> ### ▶ 1 Tem — PAYWALL EKRANI + SATIN-ALMA AKIŞI (expo-iap client) — kilit KAPALI (özet)
> Finale hazırlık: ödeme altyapısı (urunler.ts + satinalma.ts + uyelik-context + Edge `dogrula-satinalma`) zaten vardı; eksik olan **istemci paywall + satın-alma akışı** yazıldı. Yeni `src/app/paywall.tsx`: **expo-iap `useIAP`** hook'u → bağlanınca `fetchProducts` (tek-seferlik `in-app` + abonelik `subs` AYRI çağrı) → 2 KATEGORİ kartı (Müşterek / Branş), her biri **Yıllık (abonelik, "EN AVANTAJLI")** + **Ömür boyu (tek-seferlik)** plan butonu, Play `displayPrice` gösterir. Satın alma: `requestPurchase` (subs'ta Android `offerToken` `subscriptionOfferDetailsAndroid[0]`'dan, platform-daraltmalı) → native `onPurchaseSuccess` → **`satinAlmaDogrula(productId, purchaseToken)`** (Supabase Edge → Google API doğrular, hak SUNUCUDA yazılır — istemci "premium" DİYEMEZ) → `finishTransaction({isConsumable:false})` → `useUyelik.yenile()`. Sahip olunan kategori "Aktif" rozeti + planlar gizli. "Satın alımları geri yükle" (`restorePurchases`+`yenile`). Robustluk: **web'de** (dev) satın alma yok bilgi notu (ayrı `<PaywallIcerik>` bileşeni → useIAP hook web'de mount olmaz); **connected=false** (Expo Go / Play yok) → "Play bağlantısı kurulamadı" uyarısı + butonlar pasif; `user-cancelled` sessiz; `hazir=false` → "üyelik yakında" notu. Marka: Arma + krem premium kartlar + lacivert vurgu plan. Giriş noktası: **Ayarlar → "Premium / Abonelik"** (`/paywall`). `_layout` Stack'e route eklendi. **GATING (kilit) HÂLÂ KAPALI** (bilerek — `kanunErisilebilir` hiçbir yerde zorlanmıyor; kapalı testte testerlar tüm içeriğe erişsin) → final yayında aktive edilecek. Yeni public DB fonksiyonu YOK → 4-dosya senkron gerekmedi. tsc 0. **KALAN (kilit için, final):** (1) `kanunErisilebilir` ile Mevzuat/akış/Tatbikat gating (2) GOOGLE_SA servis hesabı gizli + `dogrula-satinalma` deploy (3) gerçek Android derlemesinde uçtan-uca satın-alma testi (kapalı test hesabı).
>
> ### ▶ 1 Tem — TESTER ROUND-2 (ses/görsel kök çözümü) + ÖDEME ÜRÜNLERİ + vCode 8 (özet)
> **Tester round-2 (3 paralel ajan derin analiz):** (1) "BÜTÜN SESLER ROBOT" kökü → `akis.tsx` sesVar kontrolü sadece GÖMÜLÜ (`KART_SESLERI`) bakıyordu; strip'li build'de gömülü BOŞ → hep TtsBar (robot). Fix: `sesVarMi()` (indirilmiş+uzak+gömülü 3-adım) → gerçek mp3 çalıyor (SesOynatici zaten sesKaynak ile indirilen dosyadan). (2) Görsel tekrar girince "kayboluyor" → LRU önbellek MAX 10→**80** (bir kanunu kapsar). (3) Boş ekran "bozuk" sanılıyor → "Görsel hazırlanıyor… bozuk değil bekle" + büyük spinner. (4) Geri-besleme'de görsel/ses yok → zayıf kuyruk sunucu modunda SADECE indirilmiş kanun kartlarıyla filtrelendi. (5) Tatbikat→**Talim** (sekme+ekran adı; route 'tatbikat' içeride; ileride genel JSPS denemesi=ayrı Tatbikat). **NOT:** görsel çözme hâlâ ~1sn/kart (AES+Hermes); native AES ile ~5x mümkün (ertelendi). **ÖDEME MODELİ DÜZELTİLDİ → müşterek/branş AYRI (4 ürün):** musterek_yillik/omurboyu + brans_yillik/omurboyu; uyelik-context kategori-bazlı (kanunErisilebilir(klasor, blok)). Kullanıcı Play'de 4 ürünü oluşturdu. Filigran "outline" eklendi (beyaz+lacivert kenarlık → açık zeminde de görünür, KVKK kartında doğrulandı; deploy'lu). **GATING (kilit) BİLEREK ERTELENDİ → final yayında** (testerlar kapalı testte tüm içeriğe erişsin; altyapı hazır, sadece aktive edilecek). **vCode 8 AAB (47MB)** üretildi (fast-path: strip+bundleRelease, native cache) → `D:\buildler\mevzu-vCode8.aab`. **KALAN:** kullanıcı vCode 8 yükler → testerlar test eder (eski indirmeleri sil+indir). Sonra: paywall+gating (final), Google servis hesabı (GOOGLE_SA) + dogrula-satinalma deploy.
>
> ### ▶ 30 Haz — TESTER FİXLERİ + ÖDEME SİSTEMİ BAŞLADI + vCode 6 AAB BUILD (özet)
> **Tester fixleri (5/6):** #1 giriş'te "hesap yok" mesajı (eposta_kullanimda) · #2 şifremi-unuttum (kod doğru; Supabase redirect URL `mevzu://sifre-yenile` eklendi → kök sebep) · #3 18 yaş sınırı (doğum-seçici buYil-18 + onboarding yasHesapla kontrolü) · #4 ekran-kaydı engeli GLOBAL (root _layout, gerçek build'de) · #5 aramada indirilmemiş kanun→indir-uyarısı · #6 satın-alma logları=ödeme fazında. Ayrıca **geri-bildirim butonu HER kartta** (FORMSPREE koşulu kaldırıldı, Supabase'e yazıyor). **ÖDEME MODELİ (kilit):** Yıllık abonelik + Ömür boyu (2 ürün, ikisi=tam erişim); ÜCRETSİZ=sadece TCK+denemesi; branşa-özel fiyat=İLERİDE. **expo-iap v4.3.5** (DİREKT Google Play Billing, 3. taraf YOK) + config plugin → BILLING izni. **Backend:** `docs/v2/07_uyelik.sql` (uyelik_haklari + satin_alma_log + premium_mi RPC); `constants/urunler.ts` (premium_yillik/premium_omurboyu, UCRETSIZ=['tck']); `lib/uyelik-context.tsx` (UyelikProvider/useUyelik → premium + kanunErisilebilir); Edge Function `dogrula-satinalma` (purchase token→Google androidpublisher API→hak yazar+log; GOOGLE_SA gizli gerek). **vCode 6 AAB BUILD = BAŞARILI (47MB!):** strip(icerik:manifest)→prebuild--clean→gradle bundleRelease. OOM çözüldü (parallel kapalı + kotlin in-process + NODE_OPTIONS=8GB; withYerelBuild'e kalıcı + `scripts/build-aab.sh`). AAB: `android/app/build/outputs/bundle/release/app-release.aab`. **KALAN (kullanıcı):** AAB'yi yükle · Play'de 2 ürün oluştur (premium_yillik/premium_omurboyu) · Google servis hesabı kur (GOOGLE_SA secret). **SONRAKİ (ben):** satın-alma akışı (expo-iap client) + paywall + gating entegrasyonu (TCK hariç kilitle) + dogrula-satinalma deploy.
>
> ### ▶ 30 Haz — SERVING GÜVENLİĞİ (P1+P2) + YAYIN DENETİMİ + DESTEK E-POSTASI (özet)
> **Serving P1 (deploy'lu):** "icerik" bucket PRIVATE; Edge Function `imzali-url` (15dk TTL signed URL, JWT'li) → ses indirme bundan. **Serving P2 — SUNUCU PİKSEL-FİLİGRAN (deploy'lu, ÇALIŞIYOR):** Edge Function `gorsel` (storage'tan çek → resvg [Inter font, unpkg] ile yayılı/soluk e-posta katmanı → magick composite → webp; npm:@imagemagick/magick-wasm [imagemagick_deno 'require' hatası verdi] + wasm CDN'den). Test 216KB→236KB (filigran bindi). `indirme.ts` görseli `gorselFiligran()` ile bu fn'den indirir (auth header'lı) → **cihaza FİLİGRANLI gelir, temiz kopya hiç oluşmaz** ("client filigran bypass" endişesi KÖKTEN çözüldü). **Client overlay filigran KALDIRILDI** (StudyCard) → sadece sunucununki. **YAYIN DENETİMİ (3 ajan, eski/yanlış bulgular elendi):** app.json `usesNonExemptEncryption:false` (AES beyanı) + POST_NOTIFICATIONS + allowBackup=false; impersonation: "MEVZU JSPS BİRLİĞİ"→"MEVZU · JSPS", onboarding "askeri"→"ödül-sicil", **tegmen.png göğsündeki "JANDARMA" yazısı kaldırıldı** (kumaş kopyala+feather), disclaimer giriş+tanıtıma; sahte geri-bildirim→Supabase `geri_bildirim` tablosu (docs/v2/05); mağaza metinlerindeki "%100 çevrimdışı/veri toplanmaz" yanıltıcı iddia düzeltildi; eski GIZLILIK_POLITIKASI.md ARŞİV. **HESAP SİLME:** sadece uygulama içi + tam otomatik (e-posta yöntemi kaldırıldı); `docs/v2/06_hesap_silme_cron.sql` (pg_cron 30g auto-kalıcı-sil); `docs/hesap-sil.html` (Play zorunlu URL). **DESTEK E-POSTASI:** dev.ademyilmaz@gmail.com → **iletisim@mevzujsps.com** (Hostinger, 6 dosya/19 yer). **GitHub Pages:** legacy Jekyll build 'errored' → **Actions workflow ile deploy** (.github/workflows/pages.yml, docs/ statik). Canlı: `mayibey.github.io/gorselhafizateknikleriylejps/` (+/hesap-sil.html, /sartlar.html). tsc 0, hepsi push'lu. **KALAN (kullanıcı):** (1) Supabase'de 05+06 SQL çalıştır (2) Play Console: Data Safety + export-compliance + gizlilik-URL + hesap-silme-URL (3) logo #5 (4) cihaz testi: filigran görünüyor mu + kanun yeniden-indir. **SONRAKİ FAZ: ÖDEME sistemi.**
>
> ### ▶ 30 Haz — İÇERİK→SUNUCU + AES + SES = KOD OLARAK TAMAM (özet)
> **Görsel:** sunucu (Supabase Storage, 565 webp public) + kanun-kanun indir → **AES-256 şifreli** diskte → gösterirken bellekte çöz (data-URI, preload) → filigran **e-posta**. **Ses:** sunucu (Supabase, 566 mp3) + indir (ŞİFRESİZ — anlatım; koruma serving fazında imzalı-URL) → ses-oynatici+hook'lar `sesKaynak`. **İndirme:** `kanunIndir` görsel(şifreli)+ses(düz) indirir; Mevzuat satırında İndir/%/İndirildi/Sil + **zorunlu indir-önce-çalış**. **WebP:** 835MB→79MB (assets/kartlar 127MB). **Binary-strip:** `npm run icerik:manifest` → görsel+ses require map BOŞ → bundle'daki **755MB içerik pakete girmez, app ~MB** (`icerik:tam` ile dev'e dön). **Kripto Node'da doğrulandı** (byte-özdeş PASS). tsc 0. **R2:** içerik yüklü ama serving PARKTA (r2.dev SSL gelmedi + custom domain "zone not valid"=DNS Hostinger'da). Aktif serving=Supabase. **KALAN:** (1) cihaz testi [kullanıcı, 1dk] (2) **serving güvenliği** = private bucket + imzalı URL + **sunucu-tarafı piksel-filigran** (kullanıcının "filigran bypass edilir" endişesini kökten çözer; overlay-filigran sadece ekran-foto'ya karşı, dosya baytları temiz; sunucu-filigran ~$0 Supabase Edge free tier) (3) ödeme/gating (4) field: #5 logo #6 görsel-madde #7 ses. **Firebase göçü İPTAL** (Supabase'de kalındı).
>
> ### ▶ 30 Haz — İÇERİK→SUNUCU fazı BAŞLADI (adım 1: istemci zemini, depo-agnostik)
> Keşif: `docs/DEGERLENDIRME/11_SUNUCU_ASSET_OFFLINE.md` (kırmızı-takım onaylı) baz alındı. İçerik APK'da: `assets/kartlar` 883MB (565 kart: 371 png+194 webp) + sesler 628MB ≈ 1.5GB. Mimari: **hibrit — DB/Auth/Edge = Supabase, ağır binary = Cloudflare R2 (egress ücretsiz)** önerisi. **B1 kritik:** `seed.ts` şemayı `Object.keys(KART_GORSELLERI)`'den türetiyor → anahtar listesi gömülü KALMALI. **Adım 1 yapıldı (güvenli, davranış değişmedi):** codegen (`gorsel-registry-uret.mjs`) artık 3 export üretiyor → `KART_GORSEL_YOLLARI` (anahtar→uzantılı yol, KT3 ext sorunu çözüldü), `KART_ANAHTARLARI` (seed buna repoint edildi), `KART_GORSELLERI` (require map; `GORSEL_MANIFEST_ONLY=1` ile boş üretilebilir → binary pakete girmez). Yeni `lib/gorsel-kaynak.ts` (TEK çözümleyici: `ICERIK_TABANI` boşsa yerel require, doluysa uzak `{uri}`; expo-image memory-disk zaten cache'liyor). study-card+akis repoint. config'e `ICERIK_TABANI` (EXPO_PUBLIC_ICERIK_TABANI). tsc 0. **SONRAKİ:** depo kararı (R2 vs Supabase Storage) + manifest/upload script + ICERIK_TABANI doldur + test → sonra ses (expo-file-system indirici) + binary çıkar.
>
> ### ▶ 30 Haz — İÇERİK→SUNUCU + AES at-rest ŞİFRELEME (büyük faz, devam)
> **Pipeline tamam:** depo-agnostik resolver + manifest codegen → indirme motoru (expo-file-system, kanun-kanun, devam-edilebilir) → Mevzuat **İndir** butonu (paylaşılan hook + satır gate "zorunlu indir-önce-çalış"). **WebP:** 371 PNG→WebP (sharp q80) = **835MB→79MB** (assets/kartlar 880→127MB); codegen tümü .webp. **Depo:** Supabase Storage (565 webp public, ÇALIŞIYOR) + R2 (Cloudflare, içerik yüklü ama r2.dev SSL gelmedi + custom domain "zone not valid" çünkü mevzujsps.com DNS Hostinger'da → R2 serving PARKTA, custom domain için DNS Cloudflare'e taşınmalı). `ICERIK_TABANI`=Supabase public. **AES-256 at-rest (TAM):** @noble/ciphers (aes-js 0.5MB/s yavaştı; Hermes JIT yok → ~320ms/250KB, native gerekmedi). indir→şifrele (üzerine yaz, diskte düz YOK), göster→çöz (data-URI, diske düz YAZILMAZ) + LRU önbellek + komşu-preload (akis) → kart açılış beklemesi gizli. Anahtar **expo-secure-store** (donanım keystore; yayında sunucu/user-anahtarına çevrilecek). **Filigran→user e-postası** (#13, sızıntı hesaba izlenir). **Node round-trip DOĞRULANDI** (şifrele→diske→oku→çöz byte-özdeş PASS). tsc 0. **KALAN:** (1) cihaz testi [kullanıcı: TCK sil→tekrar indir→çalış, "çözülüyor"→görsel] (2) **binary-strip** [build'de `GORSEL_MANIFEST_ONLY=1 node scripts/gorsel-registry-uret.mjs` → app 127MB→~MB] (3) compliance (Data Safety ağ/indirme) (4) private bucket+imzalı URL (ödeme fazı) (5) ses offline. **Firebase göçü:** PARKTA (ChatGPT 2. görüş bekleniyor); plan docs/FIREBASE_GECIS.md.
>
> ### ▶ 30 Haz — Güvenlik (sızmış-şifre) + auth temizlik/optimizasyon
> **Sızmış-şifre kontrolü:** Supabase "leaked password protection" Pro'ya kilitli → aynısı kodda. `lib/sifre-guvenlik.ts` (saf-JS SHA-1, test vektörleriyle doğrulandı + HIBP range API k-anonymity: şifre GÖNDERİLMEZ, hash ilk-5 hane). Kayıt + şifre-yenileme akışlarında sızmış şifre reddedilir; ağ hatasında fail-open. sifre-yenile ≥6→≥8+harf+rakam'a yükseldi. **Güvenlik denetimi (kod):** service_role uygulamada YOK, .env gitignore'lu, anon-only, 2 tablo da RLS açık → DB dump yolu kapalı. **Optimizasyon:** auth PNG'leri 5.4MB→273KB (apple/google 2.1MB→4KB, tegmen 1.2MB→265KB; System.Drawing yüksek-kalite). **Temizlik:** kullanılmayan `@react-native-community/datetimepicker` paketten + app.json plugin'den kaldırıldı. tsc 0. Supabase compromised-password toggle GEREKMEZ (kod hallediyor).
>
> ### ▶ 30 Haz — Auth premium CİLA (ajan denetimi %70→ düzeltmeler) + başkan fix'leri
> Kalite-denetim ajanı (referans 1/2/3 vs kod) %70 verdi → P0-P3 uygulandı: **DekoratifArkaplan** (svg krem/altın köşe blobları + noktalar, auth+profil+tanıtım), **AnaButon** (expo-linear-gradient lacivert→lacivert2 + glow gölge, tüm ana butonlar), **Arma** (altın defne çelengi + kalkan-yıldız), girdi+sağlayıcı butonlarına hafif gölge + yükseklik 52→54, karakter 148×188→158×202 + right -16, başlık-form boşlukları açıldı (gap 12), stepper çizgi 3px + aktif daireye halo. **Başkan fix'leri:** "Profilini Tanıyalım"→"Tamamlayalım"; native takvim İngilizce ay veriyordu → yeni **DogumTarihiSecici** (TR Gün/Ay-Türkçe/Yıl 3-sütun modal, native DateTimePicker SÖKÜLDÜ-kullanılmıyor); ProfilAdim'da **stepper EN ÜSTE** alındı (karakter "Tamamla"yı kapatıyordu — düzeldi) + Kişisel Bilgiler ince kart içine. tsc 0. ⚠ **"Kaydedilemedi" hatası = SQL eksik:** kullanıcı `docs/v2/03` VE `04` SQL'ini çalıştırmalı (profil kolonları yoksa profilGetir/Kaydet patlar). datetimepicker paketi artık unused (app.json plugin kaldı; cleanup notu).
>
> ### ▶ 30 Haz — PREMIUM AUTH redesign (referans tasarımlara göre, gece otonom)
> Başkan: masaüstü `giriş kayıt/` (1,2,3 + tegmen + apple/google) referansları → KREM temaya uyarla, harika yap, veri bağla, Evsaf'a kişisel bilgiler, "hayatının maçı". **Yapıldı:** Varlıklar `assets/images/auth/` (tegmen/google/apple) + `src/assets/auth-gorselleri.ts`. Yeni `components/auth/`: `auth-girdi` (ikon+göz), `adim-gostergesi` (1-2-3 stepper), `karakter-figur` (teğmen kemer-üstü kırpık, cover+top), `saglayici-butonlari` (Google çalışır + Apple "iOS'ta yakında"), `auth-ekrani` (premium giriş/kayıt: arma + karakter + temalı girdiler + şartlar checkbox + Google/Apple + güvenlik notu + mod toggle + dev TEŞHİS). Onboarding yeniden: tanıtım→AuthEkrani→**ProfilAdim premium** (stepper adım2, Kişisel Bilgiler: ad/soyad yan yana + telefon + **doğum tarihi** [@react-native-community/datetimepicker spinner] + **cinsiyet** segmented) →branş→rütbe. **Veri:** `04_profil_genisletme.sql` (dogum_tarihi, cinsiyet) + auth.ts Profil/Cinsiyet/profilKaydet(nesne)/profilTamMi (gate yeni alanlarla). **Evsaf:** `KisiselBilgiler` kartı (ad/soyad/e-posta/telefon/doğum/cinsiyet). tsc 0. **Kullanıcı ADIMI: `docs/v2/04_..sql` çalıştır.** giris-formu.tsx artık orphan (AuthEkrani değiştirdi). ⚠️ NOT: karakter "JANDARMA" üniformalı → impersonation riski (geçmiş red), başkan onayı/karar gerek. Apple logosu gümüş-parlak (Apple kuralı: resmi siyah/beyaz logo gerek, yayında değiştir).
>
> ### 📋 BACKLOG (planlandı, ertelendi — başkan "unutma" dedi; bkz. memory [[kayit-tasarim-ve-ertelenenler]])
> - **İlk-açılış TUTORIAL (interaktif tanıtım turu):** ne nerede / nasıl kullanılır coach-mark turu (Karargah/Mevzuat/patika/akış/Tatbikat/Evsaf). Yayın-öncesi UX paketi; AsyncStorage "gösterildi" bayrağı.
> - **SMS doğrulama** → anti-piracy fazına ertelendi (telefon ALANI şimdi toplanıyor; doğrulama ödeme/Integrity ile). Twilio/Netgsm — ücretli.
> - **Şık "E-postan onaylandı" web ekranı** → `mevzujsps.com/onaylandi` (Expo Go'da boş sayfa normal; gerçek build + web ile). Aynısı şifre-yenileme dönüşü.
> - **Markalı gizlilik URL** → `mevzujsps.com/gizlilik`; `config.ts GIZLILIK_URL/SARTLAR_URL` doldur + Play Data Safety.
> - **Marka e-postası** yasal metinlerde `dev.ademyilmaz@gmail.com` → `iletisim@mevzujsps.com`.
> - **Apple ile giriş** → iOS fazı.
>
> ### ▶ 29 Haz — Üyelik: Registration redesign (bir-email-bir-hesap + ad/soyad/telefon + doğrulama)
> Başkan: mükerrer hesap olmasın + kişisel bilgi (ad/soyad/telefon ZORUNLU) al + saçma giriş engelle (TR telefon, şifre ≥8). `docs/v2/03_profil_ve_dedup.sql`: profiles'a ad/soyad/telefon + `eposta_kullanimda()` SECURITY DEFINER (mükerrer engel). Yeni `lib/dogrulama.ts` (adHatasi/telefonHatasi[TR normalize 05XXXXXXXXX]/sifreHatasi[≥8+harf+rakam]/epostaHatasi). `giris-formu.tsx`: kayıtta e-posta+şifre doğrulama + signup öncesi `epostaKullanimda` → varsa "zaten kayıtlı, giriş yap" (mükerrer engel). `auth.ts`: epostaKullanimda/profilGetir/profilKaydet/type Profil. `onboarding.tsx`: yeni **ProfilAdim** (ad/soyad/telefon, doğrulamalı) — sıra: tanıtım→giriş→**profil**→branş→rütbe; profil eksikse zorunlu. tsc 0. **Kullanıcı ADIMLARI:** (1) `docs/v2/03_..sql` çalıştır. (2) Supabase Users'tan mevcut 2 MÜKERRER hesabı ELLE sil (verisi olanı tut). Telefon=ZORUNLU (başkan kararı); SMS doğrulama ertelendi.
>
> ### ▶ 29 Haz — Üyelik: Şifre yenileme akışı (app tarafı) + SMTP gerçeği
> ### ▶ 29 Haz — Üyelik: Şifre yenileme akışı (app tarafı) + SMTP gerçeği
> **ÖNEMLİ:** Supabase Pro e-posta sorununu ÇÖZMEZ — dahili SMTP hem Free hem Pro'da test-amaçlı (saatte birkaç mail). Çözüm: **özel SMTP (Resend önerildi, ücretsiz 3k/ay)** → Supabase Auth → Emails → SMTP Settings. Bu = senin kuracağın (mailin GİTMESİ). **App tarafı (mailin GERİ DÖNMESİ) yazıldı:** `auth.ts sifreSifirla` redirectTo `mevzu://sifre-yenile` (path); yeni `kurtarmaKoduDegistir` (exchangeCodeForSession) + `yeniSifreBelirle` (updateUser). Yeni `app/sifre-yenile.tsx` (code'u oturuma çevir → yeni şifre + tekrar → kaydet → app'e gir). `_layout`: Stack'e eklendi + gate istisnası (sifre-yenile rotası giriş kontrolünü atlar, kurtarma oturumu orada kurulur). expo-router deep-link `mevzu://sifre-yenile?code=` ekrana yönlendirir. tsc 0. **E2E test SMTP kurulunca:** şifremi unuttum → mail → linke tıkla → app yeni-şifre ekranı → kaydet → girişli.
>
> ### ▶ 29 Haz — Üyelik: E-posta/şifre girişi (Google'a ek)
> Paylaşılan `components/giris-formu.tsx` (Google + e-posta/şifre; giriş/kayıt toggle + şifremi unuttum; dostça TR hata eşlemesi). `auth.ts`: epostaGiris (signInWithPassword) / epostaKayit (signUp, doğrulama e-postası) / sifreSifirla (resetPasswordForEmail). Onboarding `GirisAdim` artık GirisFormu kullanıyor (eski Google-only sadeleşti). `/giris` not-logged-in dalı gate yüzünden erişilmez → dokunulmadı. tsc 0. **Kullanıcı ADIMI (test için):** Supabase → Auth → Providers → Email zaten açık; "Confirm email" AÇIK ise kayıtta doğrulama e-postası gelir (varsayılan SMTP sınırlı). Hızlı dev testi için Confirm email kapatılabilir. **YAYIN:** özel SMTP (Resend/SendGrid) şart (varsayılan SMTP üretimde yetersiz). Apple girişi iOS fazında.
>
> ### ▶ 29 Haz — Üyelik: BULUT SENKRON (ilerleme → hesap) — giriş artık gerçekten işe yarıyor
> Mandatory login sözünü ("ilerlemen kaybolmaz") gerçek yaptı. **JSON snapshot** yaklaşımı: yerel ilerleme (srs/performans/sicil/sınav/günler/geri-bes) → Supabase `kullanici_ilerleme.veri jsonb`. `docs/v2/02_bulut_senkron.sql` (tablo + RLS). **4-DOSYA SENKRON:** `types.ts IlerlemeSnapshot` + Backend.ilerlemeDisaAktar/ilerlemeIceAktar; `database.d.ts`; native (SQLite, SRS `ON CONFLICT max(kutu)`); web (Map/array, parite). **Merge stratejisi:** SRS kutu ASLA geri gitmez (max); log/sicil/sınav YALNIZ yeni cihazda (yerel boş) tam yüklenir → çoğaltma/çakışma yok. `lib/senkron.ts`: senkronYukle (girişte çek+birleştir) / senkronKaydet (upsert). **Bağlama:** auth-context girişte senkronYukle + çıkışta senkronKaydet; `_layout` AppState background→senkronKaydet. Hepsi try/catch (offline/tablo yok → yerelle çalışır). tsc 0. **Kullanıcı ADIMI: `docs/v2/02_bulut_senkron.sql`'i Supabase'de çalıştır.**
>
> ### ▶ 29 Haz — Üyelik: KVKK / gizlilik metni güncellendi (auth gerçeğine göre)
> Giriş zorunlu + kişisel veri (e-posta/hesap) işleniyor → eski "tamamen offline, hesap açmıyoruz, sunucuya gitmez" metni YANLIŞTI. `yasal-metin.ts GIZLILIK_METNI` + `SARTLAR_METNI` yeniden yazıldı: Google giriş zorunlu, e-posta/hesap kimliği Supabase'de (AB/Frankfurt), yurt-dışı aktarım = girişle AÇIK RIZA, 30g hesap silme, KVKK hakları. `docs/index.html` (barındırılan gizlilik, GitHub Pages) aynı şekilde güncellendi. **AÇIK İŞ (yayın öncesi):** `config.ts GIZLILIK_URL/SARTLAR_URL` boş bırakıldı (başkanın "markalı, mayibey'siz host" notu) → Play Data Safety için marka-host kararıyla doldurulacak + Play Console gizlilik URL'ine girilecek. tsc 0.
>
> ### ▶ 29 Haz — Üyelik: GİRİŞ ZORUNLU yapıldı (login gate)
> Başkan kararı: uygulama girişsiz açılmasın. Akış: **tanıtım → ZORUNLU giriş → branş → rütbe → app.** `_layout.tsx` RootNavigator guard'ına `girisGerek = hazir && !kullanici` eklendi → `eksik`'e dahil (auth yukleniyor da bekleme dahil). `onboarding.tsx`: yeni `GirisAdim` (atlamasız Gmail giriş + dev TEŞHİS kutusu); sıra tanıtım→giriş→branş→rütbe. **Güvenli fallback:** `hazir=false` (anahtar yok) ise gate kapalı, girişsiz çalışır. Oturum AsyncStorage'da kalıcı → ilk girişten sonra OFFLINE açılış çalışır (yalnız ilk açılış internet ister). tsc 0.
>
> ### ▶ 29 Haz — v2 BÜYÜK İŞ başladı: Üyelik (Google giriş AKTİF) + Hesap silme (30g soft delete)
> **Google ile giriş AKTİF** (`UYELIK_AKTIF=true`; Supabase vwmjr + Google OAuth web client + .env hazırdı; Expo Go'da uçtan uca çalıştı, e-posta görünüyor). **Hesap silme = 30 günlük YUMUŞAK silme** (başkan kararı, [[hesap-silme-30gun-soft-delete]]): `docs/v2/01_profiles_soft_delete.sql` (profiles tablo + RLS + signup trigger + `silme_talep_tarihi` + geriye dönük satır; pg_cron kalıcı-silme commented=sonraki). `auth.ts`: silmeTalepTarihiGetir/hesapSilmeTalebiKur/hesapGeriGetir. `auth-context`: girişte silme talebi varsa OTOMATİK reaktivasyon (`reaktiveEdildi`) + `hesabiSil`. `giris.tsx`: "Hesabı Sil" (Alert uyarı: 30g + satın almalar gider) + reaktivasyon bildirimi. tsc 0. **Kullanıcı ADIMI: `docs/v2/01_..sql`'i Supabase SQL Editor'da çalıştır.** Sıra: KVKK metni → bulut senkron → e-posta/şifre → Apple → içerik→sunucu → ödeme → gating → anti-piracy.
>
> ### ▶ 29 Haz — Orta iş: Aktif hatırlama (kart akışı sonu mini-quiz) [P1-a]
> Testing-effect: kart akışı ÖĞRENME modunda (kanun/bölüm) bitince, o kanunun sorularından **2-3 rastgele "Hatırlama" sorusu** çıkar, sonra mevcut "tamamladın" + sınav CTA. Zayıf modunda YOK. Yeni `components/card-flow/hatirla-quiz.tsx` (krem temalı — akış durumKolon'u krem; soru+şık+açıklama, salt self-test, puan cezası yok). `akis.tsx`: `ogrenmeModu` + `hatirlaSorular`/`hatirlaBitti` state + yükleme effect (`getSinavSorulari`, soru yoksa atla) + `bitti && hatirlaGoster` render dalı + loader reset. Yeni DB fonksiyonu yok → parite korunur. tsc 0.
>
> ### ▶ 29 Haz — Küçük iş: Onboarding tanıtım/değer adımı (P1-c)
> İlk açılışa branş seçiminden ÖNCE bir **tanıtım adımı** eklendi (`onboarding.tsx` `Tanitim` + `tanitimGecildi` state): marka + "Kanunları görselle, kalıcı öğren" + 3 değer (görsel kart / deneme sınavı+takdir / sicil-ödül) + "Başla". Ürünün ana farkı ilk ekranda görünür → drop-off azalır. tsc 0. (P1-a "aktif hatırlama" = kart akışını değiştiren ORTA iş, küçük değil → parkta.)
>
> ### ▶ 29 Haz — Küçük işler: sınav→zayıf köprüsü + test paneli __DEV__'e geri + belge metin/Türkçe
> **Sınav→eylem köprüsü (P1-b):** sınav sonucu <%100 ise `Sonuc`'ta **"Yanlış maddeleri çalış"** CTA → `router.replace('/akis?mod=zayif')` (yanlışlar zaten zayıf havuza düşüyordu → döngü kapandı). EmptyState yerine özel skor+3 buton düzeni. **Test paneli __DEV__'e geri alındı** (expo start'ta görünür, yayında gizli). **Belge metinleri:** "EĞİTİM KOMUTANLIĞI"→"MEVZU JSPS BİRLİĞİ" (impersonation riski), 3. şahıs→"tarafınıza", + Türkçe büyük harf düzeltmesi (`trUpper` i→İ; "TAKDIR"→"TAKDİR"). Hepsi tsc 0.
>
> ### ▶ 29 Haz — D1 polish + Yol haritası E (küçük işler)
> **Evsaf belge modalı (D1 polish):** `takdir-belgesi.tsx` genelleştirildi (ortak `BelgeCerceve` + `TakdirBelgesi` sınav ekranı + `SicilBelgesi` kayıttan render; ödül=altın/madalya, ceza=kırmızı/kalkan). Evsaf'ta sicil kaydına basınca inline metin yerine **görsel belge MODALI** açılır.
> **E1 — FLAG_SECURE (güvenlik):** `EKRAN_KORUMA_AKTIF=false` kaldırıldı; yeni `lib/ekran-koruma.ts useEkranKoruma()` hook'u kart görseli görünen `/akis` + `/sesli-nobet`'te ekran görüntüsü/kaydı engeller (Android FLAG_SECURE). Sicil/takdir/ayarlar screenshot'lanabilir kalır (sertifika paylaşımı serbest).
> **E2 — Branş "çok yakında" şıklaştırıldı:** yeni `components/ui/yakinda.tsx` (altın YAKINDA rozeti + marka tonu); mevzuat + tatbikat branş boş-durumu `hammer-wrench/Çok yakında` yerine premium `Yakinda` (shield-star + "Branş eğitimi/sınavları yolda"). Gizlenmedi (kapsam görünür).
> **E3 — kalan altın-yazı kontrastı:** yeni `altinMetin #8A6510` token'ı (krem üstünde ≈4.5:1 AA). Krem ekranlardaki `color="altinKoyu"` YAZILAR → `altinMetin` (tatbikat/patika/mevzuat/index/sinav). `altinKoyu` ikonlarda + KOYU akış ekranında DEĞİŞMEDİ (koyu üstünde kontrastı korumak için). tsc 0.
> **Büyük işler (offline→sunucu, üyelik, ödeme) BEKLİYOR — başkan kararı.**
>
> ### ▶ 29 Haz — Yol haritası D1: Ödül revize — Takdir Belgesi (sınav %100 → görsel sertifika)
> Başkan kararı V5 (ödül/ceza komple revize). **Keşif bulgusu:** CEZA sistemi (geri-bes emri + 3 günlük pencere `PENCERE_GUN=3` + kademeli askeri ceza Yazılı İkaz→Uyarı→Kınama→Aylıktan Kesme) ZATEN başkanın tarif ettiği gibi → DOKUNULMADI. **D1 yapıldı (ÖDÜL trigger değişti):** Takdir artık "tüm kartlar kutu≥4" yerine **kanunun deneme sınavı %100 (dogru===toplam)** geçilince veriliyor. `sicil-servis.ts` ödül bloğu `getSinavSonuclari`'dan besleniyor (getStudyCards/OGRENILDI_KUTU çıktı); `odulDegerlendir`+başarı/üstün merdiveni AYNEN (idempotent `takdir:<lawId>`). `sicil.ts` takdir metni güncellendi. Yeni **`components/sicil/takdir-belgesi.tsx`** (antetli görsel sertifika: madalya + EĞİTİM KOMUTANLIĞI + TAKDİR BELGESİ + kanun adı + tarih). `sinav.tsx`: %100 bitince kayıttan sonra `degerlendirSicil()` (takdir düşer) + Sonuç ekranında sertifika gösterilir. Yeni DB fonksiyonu YOK → 4-dosya senkron gerekmedi, web/native parite korunur. tsc 0. **Sıradaki olası polish:** Evsaf'ta takdir kaydına basınca tam sertifikayı tekrar aç.
>
> ### ▶ 29 Haz — Yol haritası C: "Aralıklı tekrar/SRS" mağaza iddiası kaldırıldı (kod KORUNDU)
> Başkan kararı: SRS'i kaldır (mağaza iddiası + ölü kod). **Keşif düzeltti:** box (`kutu`/`srsGuncelle`) ÖLÜ DEĞİL — tüm ilerleme (Mevzuat %, patika, Tatbikat hazırlık, zayıf havuz, sicil) buna bağlı + CLAUDE.md "srs.ts kutsal, dokunma". `getDailyQueue` (günlük yol) gerçekten erişilemez (plain `/akis` hiç çağrılmıyor) ama 4-dosya senkronlu kutsal alanda, getirisi ~0 → bırakıldı. **Yapılan:** yalnız YANILTICI MAĞAZA İDDİASI temizlendi — `MAGAZA_LISTELEME.md` + `PLAY_MAGAZA_GIRISI.md`'den "Aralıklı tekrar (Leitner/SRS)" + "Günlük çalışma kuyruğu" çıkıp yerine GERÇEK özellikler (deneme sınavları, patika, zayıf mevzi tekrarı, sesli anlatım). Yasal "tekrar sistemi" (generic, doğru) kaldı. Karar dosyası 00_KARARLAR_BASKAN V1 gerçeğe göre güncellendi. Kod değişmedi → tsc etkilenmez.
>
> ### ▶ 29 Haz — Yol haritası A+B: Tatbikat kilidi açıldı + m.25 içerik düzeltme
> Gece değerlendirmesi yol haritasından (sıralı): **A) Tatbikat deneme sınavı kilidi KALDIRILDI** (her kanun sınavı her zaman açık; kilit yerine "Hazırlık: X/Y kart çalışıldı" bilgi satırı; `tatbikat.tsx` onCalis/kanunaGit/satirKilitli temizlendi). **B) İçerik hatası m.25 DÜZELTİLDİ:** `Jandarma Teşkilat Yön m.25` küme-kartı m.25 yerine m.26+ metnini gösteriyordu (12_ICERIK_TAM_TARAMA B2); resmî m.25 metni (mevzuat 6845, tutuklu/hükümlü soruşturma-duruşmaya götürme) `MADDE_METINLERI`'ne override eklendi (KART_MADDE_METINLERI'nden önce gelir). tsc 0. Sırada C (SRS kaldır).
>
> ### ▶ 29 Haz — Bildirim: dakika hassasiyeti + 5sn test butonu
> Başkan: kullanıcı istediği saate (sadece saat değil DAKİKA da) bildirim ayarlayabilmeli + test. `BildirimAyar`'a `sabahDakika`/`geceDakika` (0-59, varsayılan 0) eklendi; `planla` `gunlukKur(saat, dakika, …)` ile zamanlar. `egitim-plani.tsx` `SaatSatir` artık SAAT + DAKİKA (5'er adım) iki stepper + büyük "HH:MM" gösterimi. Yeni `testBildirimi()` (TIME_INTERVAL 5sn) + "Test bildirimi gönder (5 sn)" butonu → kurulumu anında doğrula. Fırsat etiketi güncellendi (sabit 11-18 → "sabah-gece arası rastgele"). google-services.json kuruldu (proje mevzu-jsps-59639, paket doğru). tsc 0.
>
> ### ▶ 29 Haz — Bildirim GERÇEK yapıldı (lokal hatırlatma; FCM temeli) [V2 kararı]
> Başkan: bildirimi gizleme, gerçek yap, gerekirse google-services.json kur, KALICI çözüm. **Mimari:** lokal günlük hatırlatma ŞİMDİ (sunucusuz) + uzak push (FCM/Expo Push) için sağlam temel (v2). `npx expo install expo-notifications` (~0.32.17, SDK54; expo-asset override sağlam). `app.json`: expo-notifications plugin (color #0B1F3A) + `android.googleServicesFile=./google-services.json`. `withRemovedPermissions` POST_NOTIFICATIONS'ı SİLMİYOR (teyit, Android13+ izin korunur). **`lib/bildirim.ts` GERÇEK impl:** setNotificationHandler (foreground banner) + izinIste (POST_NOTIFICATIONS) + planla (cancelAll→Sabah/Gece DAILY trigger + Fırsat rastgele saat; web/dosyasız no-op) + uzakPushTokenAl (Expo token iskelesi, v2). VARSAYILAN_AYAR.aktif true. `_layout.tsx` açılışta `getAyar().then(planla)`. `egitim-plani.tsx` zaten planla çağırıyordu (artık gerçek). **MANUEL ADIM (başkan):** Firebase Console → Android app `app.mevzujsps.android` → google-services.json indir → repo köküne koy (gitignore'lu) → build. **Test:** Expo Go'da lokal bildirim çalışır (google-services.json gerekmez); native build için dosya ŞART. Rehber: `docs/BILDIRIM_KURULUM.md`. tsc 0. (⚠ build doğrulaması başkanda — ben build alamıyorum; sürüm/transitif çakışma olursa düzeltilir.)
>
> ### ▶ 29 Haz — Karargah geri sayım + kontrast (gece değerlendirmesi sonrası başkan kararları)
> Gece otonom değerlendirmesinin (docs/DEGERLENDIRME/) ardından başkan kararları → `docs/DEGERLENDIRME/00_KARARLAR_BASKAN.md`. Bu turda 2 uygulama değişikliği:
> **(1) Kontrast/okunabilirlik (değerlendirme #7):** `theme.ts` `solukMetin #8A7D62→#6E6047` (krem üstünde 3.67:1→≈4.6:1, WCAG AA) — uygulamanın VARSAYILAN ikincil yazı rengi, 60+ yerde (Evsaf/Karargah/Mevzuat/Ara/Giriş/Tatbikat/madde-sheet/Patika) tek dosyada düzeldi. `amber #B5791C→#8A5A12` (TEKRAR ZAMANI uyarı bandı). `altinKoyu` DOKUNULMADI (çoğu ikon/SVG rengi; yazı-olarak-altın per-callsite işi, sonraya). "bi yap görelim olmazsa döneriz" onayıyla.
> **(2) Karargah en üste JSPS sınavına CANLI geri sayım:** `index.tsx` `SinavGeriSayim` bileşeni (gün/saat/dakika/saniye, her saniye `setInterval` ile işler, koyu lacivert şerit + altın tabular rakamlar — komuta-konsolu aksanı). Hedef tek sabit: `SINAV_TARIHI = new Date(2026,8,2,10,0,0)` (2 Eyl 2026 ≈65 gün, PLACEHOLDER — başkan gerçek tarih/saati verince o satır değişir). tsc 0.
> **Bekleyen kararlar (00_KARARLAR_BASKAN):** SRS→kaldır, Bildirim→gerçek yap (sırada), Ödül/Ceza→komple revize (takdir belgesi + geri-besleme-görevi temelli ceza), Branş kalsın + Tatbikat kilidi açılsın, %100 offline iddiası kalkacak.
>
> ### ▶ 29 Haz — Tatbikat: kanun no kutusu tek satır
> Başkan: Tatbikat kanun listesinde soldaki monogram kutusunda kanun no'su (örn. 5237) kutuya sığmayıp alt satıra taşıyordu. `Monogram` AppText'ine `numberOfLines={1}` + `adjustsFontSizeToFit` + `monoNo` stili (width %100, textAlign center) → ara.tsx `sikMonoNo` deseni. Artık küçülerek tek satır sığar. tsc 0.
>
> ### ▶ 29 Haz — Tüm günlük sayaçlar yerel gece 00:00'da sıfırlanır (UTC bug)
> Başkan: sayaçlar (bugünün görevi / bugün çalışılan / Mevzuat son-çalışma vb.) gece 00:00'da sıfırlanmalı. **Kök neden:** `lib/srs.ts bugunISO()` `new Date().toISOString()` = **UTC tarihi** kullanıyordu → UTC+3 Türkiye'de "bugün" sınırı UTC 00:00 = yerel **03:00** (sayaçlar 03:00'da sıfırlanıyor, 00:00-03:00 arası dünün verisi). **Düzeltme:** yeni `yerelISO(d)` (getFullYear/Month/Date — yerel) + `bugunISO()` ve `sonrakiTarih()` artık yerel gün biçimler. bugunISO TEK kaynak → tüm günlük sayaçlar/streak/performans-tarih/sınav-tarih yerel 00:00'da sıfırlanır. `oncekiGun`/`sicil.ts` saf takvim-günü string matematiği (yerel/UTC fark etmez) → dokunulmadı, yorum güncellendi. Web+native aynı saf fn → parite. UTC+3 01:00 senaryosu Node ile doğrulandı (eski 28, yeni 29). tsc 0.
>
> ### ▶ 29 Haz — Karargah sadeleşti: rol/kademe kaldırıldı + hedef display kaldırıldı
> Başkan istekleri: (1) Karargah üstündeki **rol(branş)/kademe(rütbe) dropdown'ları KALDIRILDI** — branş/rütbe artık YALNIZ Evsaf → Ayarlar'dan değişir (zaten `/brans-sec` + `/rutbe-sec` orada vardı). (2) Açılan üst alan **7-gün "TEKRAR ZAMANI" uyarı bandına** bırakıldı (banner zaten vardı, dropdown'lar gidince üste çıktı). (3) **"Bugünün görevi: 15 kart" sabit hedefi KALDIRILDI** — hem hero'daki "Günlük hedef" sütunu hem "BUGÜNÜN GÖREVİ" hedef bar/sayacı; aktivite istatistikleri (Zayıf mevzi · Bugün çalışılan) KALDI. Hedef (`gunlukKart`) hâlâ oturum limiti olarak çalışıyor (`akis.tsx slice`) + kullanıcı Ayarlar → Eğitim Planı stepper'ından (5–50) ayarlar; Karargah'ta artık sabit gösterilmiyor.
> Temizlik: index.tsx'ten ölü `Dropdown`/`Bar` bileşenleri + ilgili state (acikDD/hedef/branches/brans/rutbe) + importlar (useBrans/useRutbe/RUTBELER/getAyar/getBranches/Branch) + ölü stiller (rolKademe/dd*/track/fill) kaldırıldı. tsc 0.
>
> ### ▶ 28 Haz — Deneme soruları tazelendi: 601 → 862 (+261)
> Başkan fabrikadaki `SORULAR.json`'lara yeni sorular ekledi. `npm run soru:uret` yeniden çalıştırıldı → `src/assets/kart-sorulari.ts` 25 kanun · **862 soru** (önceki 601, +261). Yeni dosya/kanun YOK (hâlâ 25 müşterek, branş soruları yok); mevcut dosyalara eklendi. Doğrulama: hiçbir kanunda düşüş yok, tekrar `soru_id` yok, tüm sorular geçerli (dogru 0-tabanlı index şık aralığında + ≥2 şık), tsc 0. Codegen idempotent — sınav UI/zayıf-havuz/skor mantığı değişmedi, yalnız registry tazelendi.
>
> ### ▶ 28 Haz — KART EKRANI UX: ok kaldır + boşluğa dokun-kapat + sabit madde kutusu + Etüt→Geri Besleme
> Başkan istekleri (4):
> **(1)** Kart ekranındaki sol/sağ **gezinme ok butonları KALDIRILDI** (`akis.tsx`) — gezinme yalnız yatay swipe ile. İlgili `okBtn/okSol/okSag/okPasif` stilleri de silindi.
> **(2)** Ses/madde paneli AÇIKKEN görselin **boş bir yerine dokununca panel KAPANIR** — panel'den önce `panelKapatKatman` (absoluteFill, zIndex 4) backdrop Pressable → `setAcikPanel('yok')`. Panel zIndex 5 olduğundan panel kontrolleri/✕ çalışır; backdrop yalnız `gorselAlan` içinde olduğu için 🎧/📄 ikon satırı + footer (kardeş View'lar) panel açıkken de tıklanır (yalnız görsele dokununca kapanır).
> **(3)** Madde metni kutusu artık **HER ZAMAN AYNI yükseklik** (`panelMadde` `maxHeight:'60%'` → `height:'60%'`) → kısa/uzun fark etmez; uzun kanunlarda metin panel-içi ScrollView (scroll bar, `showsVerticalScrollIndicator`) ile kendi içinde kayar, kutu/ekran büyümez. (İç ScrollView zaten flex:1 idi → sabit yükseklikte doğal dolar.)
> **(4)** Karargah hero **"ETÜT" → "GERİ BESLEME"** (`index.tsx`, etiket + accessibilityLabel). İşlev (zayıf havuz) ve "Zayıf Mevziler" başlığı/açıklaması AYNI. Evsaf'taki geri besleme (ödül/ceza, GeriBesDurum) bölümü DOKUNULMADI (başkan "olduğu gibi kalsın" dedi). Not: iç değişken/terminoloji "Etüt"/"zayif" olarak korundu (yalnız görünen ad değişti).
> Doğrulama: tsc 0. Telefon görsel teyidi bekliyor.
>
> ### ▶ 28 Haz — AYIRT/ÖZET KARTLARI PATİKADA KENDİ DÜĞÜMÜ (rozet kaldırıldı)
> **Bağlam:** Önceki "içerir: m.21–22" rozeti yanıltıcıydı — ayırt kartı tek-madde düğümüne (max üye, örn. Madde 22) bağlanıp üstüne rozet konuyordu; o düğümde madde 22'nin kendi kartı da olduğundan rozet sanki düğüm 21+22'yi kapsıyormuş gibi görünüyor, ayırt kartı ayrı görünmüyordu (başkan testte yakaladı: "bu sistem tamamen hatalı").
> **Çözüm:** Her ayırt/özet kartı artık patikada **KENDİ düğümü**: "Madde 35–36 ayırt" / "Madde 247–250–252 özet" / tek-üyeli "Madde 4 ayırt" — kapsamdaki **en büyük üye maddenin HEMEN ARDINA** (35–36 ayırt → madde 36'dan sonra = "37. sıra"). Düğüm adı görsel anahtarından (`tck_ayirt_m35_36`) yeni `lib/birlesik.ts ayirtOzetBilgi` (tek-üyeli DAHİL + tip) + `birlesikDugumAd` ile üretiliyor. **Rozet kaldırıldı** (`patika.tsx` birlesik alanı/render/stil + `birlesikUyeler` importu).
> **Mimari:** `seed.ts` `_patikaBaglama`+`SEED_BOLUMLER`+`SEED_BOLUM_KARTLARI` → tek birleşik `_patika` builder (maddeler + ayırt düğümleri araya örülü; bağsız kartlar kanun sonu "Özet" düğümü; id=law*1000+sıra). `birlesikUyeler` (çok-üyeli, tek-üyeli null) DOKUNULMADI → kanunKuyrugu sıralaması + sınav `eslesenKartIdleri` BOZULMADI. **Migration v23** (SCHEMA_VERSION 22→23): bolumler/bolum_kartlari DELETE+reseed (salt referans; srs/cards/kart_performans/sinav_sonuclari TAM korunur). Web SEED'den okuduğu için parite otomatik — yeni public DB fonksiyonu YOK, 4-dosya senkron tetiklenmedi. 23 ayırt/özet kartı (10 çok-üyeli + 13 tek-üyeli, tüm kanunlar) doğrulandı; TCK düğüm sırası simüle edildi (Madde 22→**21–22 ayırt**→23, Madde 36→**35–36 ayırt**→37, vb.). tsc 0.
>
> ### ▶ 28 Haz — TATBİKAT 2: ZAYIF HAVUZ + KALICI SKOR + SEKME GÖRÜNÜR + ESKİ QUIZ SİLİNDİ
> Başkan onayıyla Tatbikat'a 3 iş eklendi. **KRİTİK DÜZELTME:** Tatbikat sekmesi `(tabs)/_layout.tsx`'te `href:null` ile GİZLİYDİ (önceki turda fark edilmemişti → yeni ekran görünmüyordu); `href:null` kaldırıldı → sekme görünür (Karargah·Mevzuat·Ara·**Tatbikat**·Evsaf).
> **(İŞ 1) Yanlış cevap → ZAYIF HAVUZ (commit B):** `sinav.tsx`'te bir soru YANLIŞ cevaplanınca `soru.kaynak`'tan ("5237 m.21-22" gibi) madde no'ları çıkarılıp o kanunun kartlarından (panel + birleşik ayırt/özet) eşleşenler bulunur → her biri için `kaydetPerformans(id,'quiz','yanlis')` → kart Karargah→Etüt (zayıf akışı) ve "Zayıf mevzi" sayacına düşer (mevcut `performans.ts kotuMu` mekanizması; Etüt'te 2 ardışık doğruyla çıkar). DOĞRU cevap LOGLANMAZ (havuz çıkışı yalnız Etüt). SRS kutusuna dokunulmaz. Saf yardımcılar `lib/sinav.ts`: `kaynakMaddeNolari` (regex `m\.(\d+(?:-\d+)*)`; alt-fıkra /2 ve Ek/Geçici atfı yok sayılır) + `eslesenKartIdleri` (birleşik kart üyeleri `lib/birlesik`'ten → "m.22" sorusu m.21-22 ayırt kartını da yakalar). Node ile gerçek kaynak verisine karşı test edildi (9 format + birleşik/özet eşleşme → hepsi geçti).
> **(İŞ 2) Kalıcı sınav skoru (commit C):** Yeni tablo `sinav_sonuclari (id, law_id, dogru, toplam, tarih)`. **Migration v22** (SCHEMA_VERSION 21→22): yalnız `CREATE TABLE IF NOT EXISTS` — referans veri reseed EDİLMEZ, kullanıcı verisi (srs/kart_performans) TAM korunur. `CREATE_SQL`'e de eklendi (fresh install). **4-DOSYA SENKRON yapıldı:** `ekleSinavSonucu(lawId,dogru,toplam,tarih)` + `getSinavSonuclari()` → `types.ts` (Backend) + `database.d.ts` (public) + `database.native.ts` (gerçek SQL, ORDER BY id) + `database.web.ts` (bellek-içi dizi, aynı sıra) + `SinavSonuc` tipi schema.ts'te. Sınav bitince `ekleSinavSonucu(lawId,dogru,toplam,bugunISO())` BİR KEZ (kaydedildiRef guard). Tatbikat satırında "Son deneme: X/Y" (getSinavSonuclari law_id→en güncel; yoksa gizli).
> **(İŞ 3) Eski quiz silindi (commit A):** `src/app/quiz.tsx` + `src/lib/quiz.ts` SİLİNDİ; `_layout.tsx`'ten `quiz` rotası kaldırıldı; `performans.ts` yorumundaki `quiz.ts` atfı `sinav.ts` yapıldı. Kalan referans yok (grep temiz). Yeni `/sinav`+`lib/sinav.ts` KALIR.
> **Doğrulama:** `npx tsc --noEmit` 0 hata; `npm run soru:uret` 25 kanun/601 soru (idempotent, diff yok); 4-dosya senkron teyit (web+native aynı imza). Migration eklemeli (veri korunur). Yanlış→zayıf akışı saf-fonksiyon testiyle (Node) doğrulandı.
>
> ### ▶ 28 Haz — TATBİKAT (KANUN BAZLI DENEME SINAVI) AÇILDI
> Fabrikadaki küratörlü deneme soruları uygulamaya alındı + "Yakında" olan Tatbikat sekmesi aktif edildi.
> **(1) Soru registry codegen (commit A):** Yeni `scripts/soru-registry-uret.mjs` + `npm run soru:uret` → fabrikadaki 25 müşterek `*_SORULAR.json`'u okuyup law_id'ye anahtarlı, tip-güvenli `src/assets/kart-sorulari.ts` üretir (görsel/ses/madde registry deseni). Klasör→law_id ÖNEK eşleştirme (" TAMAM" soneki toleranslı; JSON'daki `klasor` alanı kırpık/tutarsız olduğu için ona güvenilmez). Şık "A) " önekleri ayıklanır, doğru cevap 0-tabanlı index'le tutulur (şık SIRASI korunur — bazı açıklamalar harfe atıf yapıyor). `dogru_yanlis` soruları 2 şık, gerisi 5 şık — UI ikisini de işler. **SONUÇ: 25 kanun · 601 soru entegre, 0 atlanan/geçersiz.** DB'ye GİRMEZ → 4-dosya senkron TETİKLENMEZ.
> **(2) Sınav UI + kilit + yönlendirme (commit B):** Saf `lib/sinav.ts` (sinavVarMi/sinavSoruSayisi/getSinavSorulari[Fisher-Yates, enjekte RNG]/puanlaSinav — SRS'e dokunmaz). Yeni `app/sinav.tsx` rotası (mevcut `/quiz` BOZULMADI; eski oto-üretilen quiz kullanılmıyor): soru + şıklar (A-E harfli) → seç → doğru(yeşil)/yanlış(kırmızı) + AÇIKLAMA kutusu → ilerle → sonda skor (%≥70 kupa). `_layout.tsx`'e `sinav` Stack.Screen eklendi. **Tatbikat sekmesi** yeniden yazıldı: Müşterek/Branş segmented (Mevzuat deseni); soru havuzu olan + rütbe kapsamındaki müşterek kanunlar listelenir; kilit = Mevzuat'taki "Tamamlananlar" ile BİREBİR (bölüme bağlı kart kümesi + kutu≥1 → tamam). Kilitliyse "Önce tüm kartları çalış (X/Y)" → kanunun patikası; açıksa "Deneme Sınavı · N soru" → /sinav. Branş → "Çok yakında". **akis.tsx bitiş ekranı:** kanun/bölüm modu bitince (zayıf/günlük hariç) o kanunun sınavı varsa "Deneme sınavına gir" birincil CTA (geri ikincil olur); bölüm modunda law_id biten kuyruktan türetilir.
> **Kararlar:** (a) Sınav rotası AYRI yeni dosya (`/sinav`) — `/quiz` ve `lib/quiz.ts` dokunulmadan korundu. (b) Sınav performans LOGLAMAZ: SORULAR.json `kart_id`'si (string "5237TCK MADDE 001-1") DB numeric card.id'ye güvenilir eşlenemediğinden, yanlış kart ilişkisi/zayıf havuz kirlenmesi riski yerine "salt ölçüm" tercih edildi. (c) Kilit tanımı Mevzuat ile aynı tutuldu → Mevzuat'ta "tamam" görünen kanunun sınavı açık (tutarlı UX). `npx tsc --noEmit` 0 hata; `npm run soru:uret` 25 kanun/601 soru.
>
> ### ▶ 28 Haz — BİRLEŞİK KART GATING + ADLANDIRMA + MADDE METNİ TARAMA
> Başkan/kullanıcı 3 sorun bildirdi; her birine ayrı commit (keşif→fix).
> **(1) Gating — birleşik kart erken geliyordu (commit A):** Ayırt/özet kartı içerdiği TÜM maddeler görülmeden ekrana düşüyordu (örn. m.24-25 ayırt, kullanıcı 24'ü görünce 25'i görmeden geliyordu). Kök neden: `seed.ts _patikaBaglama` kartı `madde_no`'daki İLK maddeye bağlıyordu. **Çözüm:** yeni saf `lib/birlesik.ts` (gorsel anahtarından üye madde no'larını çıkarır — çok-üyeli ayırt/özet → `[a,b,..]`, tek-üyeli/normal → null). `_patikaBaglama` iki-geçişli: normal kartlar önce, birleşik kartlar kapsamdaki EN BÜYÜK üye maddenin düğümüne (2. geçiş → düğüm-içi sırada da en sona). `kanun-kartlari.ts kanunKuyrugu` sıralaması birleşik kart = `max(üye)+0.5`. Hem web hem native aynı saf fonksiyon/seed → parite. Şema/migration YOK, 4-dosya senkron YOK. Çok-üyeli birleşik kart az (~10: tck m21-22/m247-250-252/m317-325/m35-36, kabahatler, ililaresi m57-58, özet m247-266/m37-44). tsc 0.
> **(2) Birleşik kart adlandırma — patikada tek madde gibi görünüyordu (commit B):** Patika düğümünde birleşik kartlar tek madde gibiydi. **Çözüm:** `patika.tsx` düğüm adının altına **"içerir: m.21-22"** rozeti (altinKoyu) — üye maddeler `lib/birlesik`'ten. `BolumDugum.birlesik` alanı, yükleyicide `getCardsByBolum` kartlarından hesaplanır. Ayrıca `seed.ts` ÖZET başlığı artık TÜM üye maddeleri gösterir (`m.247-250-252 özet`, eskiden ilk-son aralığı `m.247-266`); ayırt zaten tümünü gösteriyordu. tsc 0.
> **(3) Madde metni eksik kartlar — script onarımı + yeniden üretim (commit C):** Görseli olup kanun metni olmayan **91 kart** vardı (TCK/Kabahatler tam). Kök neden: `scripts/madde-metni-uret.mjs` klasör eşlemesi eskiydi (kaynak klasörler artık `" TAMAM"` ekli → 13 kanun "KLASÖR YOK"). **Çözüm:** (a) klasör eşlemesi ÖNEK toleranslı (`<anahtar> ...` ile başlayanı bulur); (b) üretim **MERGE-SAFE** — önceki üretimde olup kaynak yeniden düzenlendiği için bu turda parse edilemeyen anahtarları KORUR (regresyon/metin kaybı YOK; 6 anahtar korundu). Sonuç: **eksik 91→76, 0 regresyon, +15 gerçek tek-madde metni** (eklenenler elle doğrulandı, yanlış-çıkarım yok). Kalan 76 kart `scripts/_eksik_madde_metni.txt`'te listelendi → parser `bloke` mantığının yanlış-pozitifi (örn. "GEÇİCİ Olarak Kontrol" başlığını "Geçici Madde" sanıp eliyor) veya kaynakta 📜 blok yokluğu. Bunların kapatılması RİSKLİ per-law parser işi veya elle giriş (ayrı içerik turu; uydurma yapılmadı). tsc 0.
> **(3b) Parser ID-sonek revizyonu (eksik 76→64):** `bloke` kararı artık başlık AÇIKLAMASINDAN değil, backtick ID'nin `MADDE <NN>-` SONRASI SONEK'inden veriliyor. Eski sürüm tüm başlığı büyütüp `/GEÇ|EK|AYIRT/` arıyordu → "GEÇİCİ Olarak Kontrol" (Disiplin m.11), "m.18 — Ek" (Resmî Yazışma m.18), "…Ayrı" gibi GERÇEK madde açıklamalarını aggregate sanıp eliyordu. Yeni: yalnız sonek `AYIRT/OZET/CETVEL/TABLO/KARŞILAŞ/EK/GEÇİCİ` ise bloke; madde aralığı sonekleri (`20-21`, `12-13-1`) İLK maddeye yazılır (placeholder'dan iyi). MADDE'siz sentetik ID'ler (`OZET-SUREC`, `DEVAMSIZLIK-AYIRT`) yalnız aggregate KELİMESİYLE elenir → Jandarma gibi ID'siz kanunlar BOZULMAZ. **Sonuç: 91→64 (27 kart kurtarıldı), 0 kayıp, 0 yanlış metin.** Merge-safe diff doğrulandı: 15 ekleme + 6 mevcut anahtar GÜNCELLENDİ (hepsi iyileşme — Disiplin m.12'den yanlış yapışan CETVEL tablosu temizlendi, m.4 60→414 kr tamamlandı vs.). Kalan 64 (57 normal + 7 ayırt) çoğunlukla kaynakta 📜 yok (gerçek içerik boşluğu). tsc 0.
> **(3c) Yerel ham mevzuattan 29 madde metni eklendi (eksik 64→35):** Başkan "elle uğraşma, sen bul-teyit-koy" dedi. Eksik maddelerin resmî tam metni `D:/JSPS Fabrika/.../_txt/*.txt` (proje zaten bunlardan çıkarıyor — en güvenilir yerel kaynak) dosyalarında VAR. `scripts/cikar.mjs` (scratchpad) madde sınırlarını (`MADDE N-`, EK/GEÇİCİ/`20/A` dahil) ayıklayıp dipnot bloklarını + yapışık sonraki-başlık + inline dipnot rakamı + sayfa-no artığını temizledi → 29 madde (Disiplin 27/28/34, Terörle 15/19/20/21/22, Sözleşmeli Sb/Asb 8/13/15/16/17/18, E-İmza 2, 6284 Aile 7/10/12/17/18, OHAL 22/23, Jandarma m.1, Resmî Yazışma 2/22/24/27/32, KV Silme Yön 8). Hepsi ELLE birebir doğrulandı (tam/temiz). El-yazımı `MADDE_METINLERI`'ne eklendi (regenerasyondan etkilenmez, öncelikli). **Eksik 64→35.** Kalan 35'in yerel _txt'i YOK (6136 + yönetmelikler: Hizmet Esasları 9, Jandarma Teşkilat 7, Personel 7, 6136 Ateşli 5, 6284 Uyg 4, 2521 Tüfekler 2, Sözleşmeli Yön 1) → mevzuat.gov.tr'den alınacak. tsc 0.
> **(3d) Kalan 36 madde mevzuat.gov.tr/jsga.edu.tr resmî PDF'lerinden → EKSİK 0 (565/565 TAM):** Yerel _txt'i olmayan kanun/yönetmelikler için resmî PDF curl ile indirildi → pdftotext → sınır ayıklama (dipnot/sayfa-no/yapışık-başlık temizliği). **Her kaynak, MASTER'da zaten kartlı (registry'deki) bir maddeyle ÇAPRAZ DOĞRULANDI** → yanlış yönetmelik elendi (Hizmet Esasları için 38397/34826/20169741 yanlış çıktı, m.5/6/7/8 birebir eşleşen jsga.edu.tr PDF'i doğru kaynak oldu; doğru Jandarma Teşkilat=20169741, Personel=20169742, 2521=10666 m.1/4/7/8/11/12 eşleşti). Eklenen 36: 6136 Ateşli (1,6,7,9,14), 6284 Uyg Yön (1,8,11,35), Sözleşmeli Yön (6), Personel Yön (9,11,16,21,30,32,53), 2521 Tüfekler (10,13), Jandarma Teşkilat (8,18,19,21,38,45,67,70), Hizmet Esasları (4,12,16,20,21,25,28,30,32). Hepsi el-yazımı `MADDE_METINLERI`'ne (öncelikli, regenerasyondan etkilenmez). **SONUÇ: madde metni eksik kart 91→0; 565 görselin TAMAMI gerçek resmî metne sahip.** Çıkarıcılar scratchpad'de (cikar.mjs/cikar2.mjs); WebFetch küçük-modeli telif gerekçesiyle reddedince PDF yerel indirilip pdftotext ile çözüldü (Türk mevzuatı kamuya açık, FSEK m.31). tsc 0.
>
> ### ▶ 24 Haz — UI YENİLEME (1): PATİKA DARK-FIRST
> **Bağlam:** Kapsamlı UI yenilemesinin ilk adımı — `patika.tsx` dark-first yeniden stillendi. **Davranış DEĞİŞMEDİ:** veri çağrıları (`getBolumler`/`getCardsByBolum`/`bolumIlerleme`), `aktifIndex` türetmesi ve `/akis` navigasyonu (bölüm→`bolumId`, bölümsüz→`lawId`, geri→`back()`) birebir korundu — sadece görünüm.
> **Yapılan:** (1) `react-native-svg` kuruldu (`npx expo install`). (2) **theme.ts**'e dark token'lar **additif** eklendi (açık marka paleti DOKUNULMADI): `zeminKoyu/yuzeyKoyu/yuzeyKoyuSoluk/kenarlikKoyu/metinAcik/metinSolukAcik/altinAcik/yesilAcik`. (3) Merkez omurga yerine **react-native-svg kıvrımlı bezier yol** (düğüm merkezlerinden geçer; geçilen segment dolu altın/yeşil, ileri segment kesikli soluk). (4) **4 düğüm durumu** mevcut veriden türetildi: tamam (yeşil kenar+check), aktif (altın hero+play+"buradasın" pill+pulse), başlanmış (altın halka+%), başlanmadı (soluk yüzey+nötr nokta; **kilit YOK, hâlâ tıklanabilir**). (5) **Gerçek-veri üst bar:** seri (`hesaplaStreak`), hazırlık% (`hesaplaIstatistik`), bölüm şeridi (kanun adı `getLaws` + tamam/toplam bölüm) — uydurma can/elmas yok. (6) Hafif animasyon (RN Animated, web'de native sürücü kapalı): sıralı fade+scale + aktifte pulse.
> **Doğrulama:** `npx tsc --noEmit` 0 hata. Yeni public DB fonksiyonu YOK → 4-dosya senkron tetiklenmiyor. **Telefon/web görsel teyidi bekliyor** (dev-client'a svg için rebuild gerekir).
> **FAZ 9A (26 Haz) — kart ekranı yeni düzen:** Sekme çubuğu KALDIRILDI → görselin sağ-alt köşesinde 2 dikey toggle ikonu (🎧 Sesli Anlatım = `sesAcik`, 📄 Madde Metni = `maddeGenis`; aktif=altın dolu, pasif=altın çerçeve). Ses kontrol paneli (TtsBar) `sesAcik` ile display-toggle (mount KALIR). **Ses OTOMATİK başlar**: TtsBar mount effect'ine `oynat(0)` eklendi (kart açılınca 0. cümleden; TtsBar key=card.id → kart değişince eski unmount/stop, yeni mount/auto-start → üst üste binmez). Madde paneli artık her zaman görünür önizleme (numberOfLines 2↔30, başlığa dokun→genişle, "Tam metni aç →" sheet 9B). `aktifSekme` state→`sesAcik`+`maddeGenis`. Görsel full-bleed + #2/#3 perf fix + filigran/GorselZoom + footer DOKUNULMADI. #4 (oran) DOKUNULMADI. tsc 0 hata. ⚠️ web autoplay politikası TTS'i ilk dokunuşa kadar engelleyebilir (native sorunsuz). #4 keşif: 522 görselin %96.6'sı 4:5 değil (çoğu 2:3 1024×1536 + 0.758 1092×1440) — içerik işi.
> **FAZ 9A düzeltme (26 Haz):** (1) Madde paneli artık DEFAULT KAPALI — `{maddeGenis ? ... : null}` ile koşullu; açılınca tam metin (numberOfLines 30) + "kapat" başlığı; kapalıyken sadece görsel+footer. (2) 🎧/📄 ikonları `bottom: 12`→`bottom: '16%'` (alt rozet/konuşma-balonu şeridinin üstünde, okSag ile çakışmaz). TTS/ses-otomatik/sesAcik/swipe/footer DOKUNULMADI. tsc 0 hata.
> **Mevzuat Müşterek/Branş seçici (27 Haz):** Mevzuat ekranı en üste segmented seçici (`blok` state, default 'müşterek'). **Müşterek** → mevcut liste (favoriler+arama+çip+Devam Et+kanunlar) aynen. **Branş** → içerik henüz üretilmediği için `DurumKutu` "Çok yakında / Branşına özel mevzuat içeriği hazırlanıyor." Mevcut müşterek mantığı (getLaws/ilerleme/favori/devamet) DOKUNULMADI; sadece üst seçici + branş dalında placeholder. tsc 0 hata.
> **Karargah: "Zayıf mevzi" kutusu tıklanır (28 Haz):** 3 kutudan üçüncüsü (Zayıf mevzi sayacı) artık Pressable → dokun → Etüt (zayıf akışı, `/akis?mod=zayif`); 0 ise pasif. tsc 0.
> **Kart ekranı: görsel yüklenmeden HİÇBİR ŞEY çalışmasın (28 Haz):** Tekrar Hatırlat (kilitsizdi) + 🎧/📄 panel butonları da `ogrenebilir` ile kilitlendi (disabled + soluk). Artık Öğrendim/Tekrar Hatırlat/swipe/oklar/🎧/📄/ses-otomatik HEPSİ görsel ekranda görünene kadar pasif. ogrenebilir karta göre olduğundan zayıf/görev (mod=zayif) modlarında da otomatik geçerli. tsc 0.
> **Karargah: görevler bitince "Mevzuat'tan çalış" (28 Haz):** Etüt (zayıf havuz) boşken hero düz metin/tıklanamazdı → artık **Pressable**: "GÜNÜ TAMAMLADIN 🎖️ / Tüm görevleri yaptın / Tekrar edilecek mevzi kalmadı — Mevzuat'tan yeni konu çalış ›" → dokun → Mevzuat sekmesi (`router.push('/mevzuat')`). Kitap ikonu. tsc 0.
> **Ara: sık açılan kanun no tek satır (28 Haz):** Sık Açılan Mevzuatlar kutusundaki 4 haneli no (5237 → "523"+"7") alt satıra kayıyordu → numara AppText'ine `numberOfLines={1}` + `adjustsFontSizeToFit` + `sikMonoNo` (width %100, textAlign center) → kutuya küçülerek tek satır sığar. tsc 0.
> **Ses, görsel yüklenmeden başlamasın (28 Haz):** Ses oynatıcı (SesOynatici/TtsBar) görsel ekranda GÖRÜNENE KADAR mount EDİLMİYOR (`ogrenebilir` false iken render edilmez) → otomatik başlama görsel yüklenince (onLoad→gorselGorundu) başlar; görselsiz kartta hemen. Mount olunca display-toggle korunur (panel kapalı/madde'ye geçince ses kesilmez). tsc 0.
> **Etüt = zayıf havuz + İLERLEME Kalan% (28 Haz):** (1) **Etüt boş ama zayıf var** sorunu: Karargah Etüt `getDailyQueue` (Leitner due) idi → "tekrar hatırlat" kartı due-tarihi 2 gün sonraya kaydığı için bugün Etüt'e düşmüyordu. Etüt artık **`getZayifKuyruk` (zayıf havuz)** → hero "Zayıf Mevziler / Eksik ve zorlandığın kartları tekrar et", onPress `/akis?mod=zayif`. Karargah'taki ayrı GERİ BESLEME kartı (artık hero ile aynı) ve `zayifSayi`/`zayifKartlar` importu kaldırıldı; görev/kutu etiketleri "Zayıf mevzi". (2) **Evsaf İLERLEME:** "Öğrenilen" (+tıkla-liste, studied/ogrAcik state, ogr* stilleri) tamamen kaldırıldı → 3 metrik: **Çalışılan · Hazırlık% · Kalan%** (=100−hazırlık). Hazırlık = (çalışılan−zayıf)÷toplam. CLAUDE.md Etüt değişmezi güncellendi (getZayifKuyruk). tsc 0.
> **Mevzuat: kanun başına "en son çalışma" satırı (28 Haz):** Her `KanunSatir`'a "En son N gün önce / dün / bugün çalıştın" satırı eklendi; hiç çalışılmamışsa **"Henüz başlamadın" (kırmızı + uyarı ikonu)**. Veri `kart_performans`'tan türetiliyor (mevzuat yukle'de law_id→son calisma tarihi `sonCalisma` map; `sonGun()` ile bugüne fark). Yeni tablo/migration yok. tsc 0.
> **Etüt/hatırlatma revizyonu — kutu UI kaldırıldı + unutma banner'ı (28 Haz):** (1) **Karargah unutma banner'ı:** ≥7 gündür çalışılmamış (ama daha önce çalışılmış) kanunlar HERO üstünde "TEKRAR ZAMANI" bandında listelenir ("…en az 7 gündür tekrar yapmadın, unutmamak için tekrar et"); her satır → o kanunun patikası. Son-çalışma tarihi `kart_performans`'tan TÜRETİLİR (yeni tablo yok). (2) **Kutu dağılımı UI tamamen kaldırıldı** (Evsaf KUTU DAĞILIMI grafiği + KutuGrafik + stiller). (3) **Öğrenilen/Hazırlık kutusuz, performans temelli** yeniden tanımlandı: Öğrenilen = çalışılmış AMA zayıf havuzda olmayan (son 2 denemesi iyi) kart; Hazırlık = öğrenilen÷toplam. "Öğrenilen→tıkla→liste" korundu (zayıf-dışı kartlar). (4) **#6:** zayıf havuzdan çıkış artık **2 ardışık doğru** ister (`performans.ts`). NOT: srs kutu MOTORU görünmez kaldı (ödül-sicil OGRENILDI_KUTU + Etüt-due hâlâ kullanıyor); kullanıcıya hiçbir yerde "kutu" görünmüyor. Deneme sınavı + soru→madde etiketi YARIN; Etüt→zayıf repoint deneme gelince. tsc 0.
> **TÜM kanunların sesi tamam (28 Haz):** Kalan 9 kanunun mp3'leri de üretildi → 25/25 kanun gerçek seslendirmeye sahip. `ses-yerlestir.py` TARGET 25 kanuna genişletildi; 566 mp3 yerleşti (TOPLAM ~565 görsel ↔ 566 ses, 25/25 BİREBİR). Yeni gelen seslerde 6 kanunun repo görselleri eskiydi (disiplin 126, eimza, resmiyazisma, aileuyg, hizmetesas, personelyon) → `icerik-yerlestir.py` ile güncel kaynaktan resync + `gorsel:uret`; SEED_KAPSAM (12/21/22/23 değişti, 14/15 aynı) güncellendi. Diğer yeni sesli kanunlar (kabahatler 62, ohal 8, jandteskyon 39 vb.) görselleri zaten uyumluydu. **Migration v21** (cards/bolumler/bolum_kartlari DELETE+reseed; SRS korunur), `SCHEMA_VERSION=21`. Kart akışı SesOynatici zaten gorsel_yolu ile çalıştığından kod değişmedi. tsc 0.
> **Ses hızı tek liste + korunur (28 Haz):** Hız seçenekleri iki oynatıcıda farklıydı (mp3/SesOynatici 4: 1/1.25/1.5/2x; TTS/TtsBar 3: 1/1.25/1.5) → karta göre farklı görünüp "cihaza göre değişiyor + stabil değil" izlenimi veriyordu. Tek kaynak `lib/ses-hiz.ts` (`SES_HIZLARI=[1,1.25,1.5,2]` + `sesHizDurum.idx`) → ikisi de AYNI 4 seçenek; seçilen hız `sesHizDurum.idx`'te modül-seviyesinde tutulduğu için kart/oynatıcı değişince SIFIRLANMIYOR (korunur). SesOynatici hızı `[player,hizIdx]` effect'inde `setPlaybackRate(rate,'high')` ile uygulanır (yeni kartta da geçerli). tsc 0.
> **Etüt = sadece tekrar (yeni kart çıkmaz) + Hedef kısaltma fix (28 Haz):** Kullanıcı kararı: Karargah Etüt'ü SADECE öğrendiklerini tekrar olsun, yeni kart Mevzuat→patikada öğrenilsin. `getDailyQueue` artık `gunlukKuyruk(..., 0)` çağırır → yeni kart YOK (queue.ts saf fonksiyon dokunulmadı; yeniLimit param'ı getDailyQueue'dan kaldırıldı — 4-dosya senkron: types/d.ts/native/web). Karargah: hero "Öğrendiklerini tekrar et (aralıklı tekrar)" + "X kartın tekrarı geldi"; boş durumda "Etüt boş — Mevzuat'tan yeni kart öğren"; BUGÜNÜN GÖREVİ'nden "Yeni" sayacı kaldırıldı (Bekleyen tekrar + Bugün çalışılan). Ayrıca hero "Hedef" değeri "Bugünkü görevi tamamla" (… kesiliyordu) → "Günlük hedef: X kart". CLAUDE.md değişmezi güncellendi. tsc 0.
> **Karargah "Etüt" + Evsaf bilgi metinleri (28 Haz):** Kullanıcılar sistemin nasıl çalıştığını anlasın diye: (1) Karargah hero "Devam Et / Kart Akışı" → **"GÜNLÜK ETÜT / Etüt"** + açıklama "Öğrendiklerini tekrar et + yeni kart öğren" + sayaç "X kart hazır (vakti gelen tekrarlar + yeni)". (2) Evsaf'ta yeni `BolumBaslik` (başlık + ⓘ, dokun→açılır bilgi): İLERLEME (Çalışılan/Öğrenilen/Hazırlık tanımları) ve KUTU DAĞILIMI (Leitner kutu mantığı + nerede sorulduğu: Karargah→Etüt, sık/seyrek aralık 1·2·4·7·14·30). Mantık değişmedi, sadece açıklama/etiket. tsc 0.
> **Evsaf: Öğrenilen → tıkla → liste (28 Haz):** Evsaf/İLERLEME kartında "Öğrenilen" stat'ı tıklanır (chevron + lacivert) → kutu ≥ OGRENILDI_KUTU(4) kartların listesi açılır (madde_no — başlık + kutu rozeti, kanun→madde sıralı). `studied` filtrelenip `ogrenilenler` state'inde tutulur. Hazırlık mantığı (Leitner kutu 1-6, Öğrendim→+1/Tekrar→1, kutu≥4=öğrenildi, hazırlık%=öğrenilen/toplam) AÇIKLANDI — kullanıcı bu bölümün tasarımını beğenmedi, redesign için ne istediği soruluyor. tsc 0.
> **Aktif sekmeye tekrar dokun → en üste kaydır (28 Haz):** `Screen` (scroll=true) ScrollView'ına `useScrollToTop` (@react-navigation/native) + ref eklendi → kaydırıp aynı sekmeye (ör. Mevzuat) tekrar basınca içerik en üste döner. Tüm Screen-tabanlı kaydırılabilir sekmeler kazandı (Ara kendi FlatList'i, scroll=false → kapsam dışı). tsc 0.
> **Mevzuat uzun kanun adı düzeni + Türkçe heceleme (28 Haz):** Uzun adlı kanunlarda kalp+Başla butonları sağda yer kaplayınca isim dar sütuna sıkışıp yukarıdan-aşağıya 8 satır yazılıyordu. DÜZELTME: `KanunSatir` DİKEY karta çevrildi — üst (monogram + ad BOYDAN BOYA, `kanunAd` flex:1), ilerleme (metin+bar), alt sağ (`satirAlt` justify flex-end → kalp + Başla/Devam/tik). Yeni `lib/hece.ts` `hecele()` (TDK kuralı: 2 ünlü arası son ünsüz sonraki heceye) → uzun kelimelere yumuşak tire (U+00AD) ekler → satıra sığmazsa Türkçe hece sınırında "-" ile bölünür (Kuv-vet-le-rin-de, Söz-leş-me-li…). KanunSatir + DevamEtKart adlarına uygulandı. tsc 0.
> **Mevzuat filtre çoklu-sekme + tam kanun adı (28 Haz):** (1) Kanunların birden çok filtre sekmesinde görünmesi: asıl sebep **Devam-Et hero kartı filtreden bağımsız HER sekmede en üstte** görünüyordu → artık YALNIZ "Tümü" görünümünde (`aktifCip==='tumu' && !favoriAcik`). (2) Filtre tutarlılığı: filtre eskiden YUVARLANMAMIŞ yüzde, KanunSatir YUVARLANMIŞ kullanıyordu → sınır durumda (199/200=%99.5 → satırda ✓ ama filtrede devam) tutarsızlık. Artık ikisi de TAM SAYI sayımı (`lawDurum`: calisilan>=toplam→tamam, >0→devam, =0→bos) → 3 kategori KESİN ayrık + görünümle birebir. (3) Kanun adları artık kısaltılmıyor (KanunSatir + DevamEtKart `numberOfLines` kaldırıldı) → tam ad kutuya sarar. tsc 0.
> **Kart kaydırma da görsel-gör kilidi (28 Haz):** "Görsel görünmeden Öğrendim basılamaz" kuralı artık SWIPE ve sol/sağ OK'lara da uygulanıyor — `ogrenebilir` (=!gorselVar||gorselGorundu) false iken `kartKaydir` onEnd erkenden döner + oklar `disabled`+soluk. `ogrenebilir`/`c`/`gorselVar` türetmeleri gesture'dan ÖNCE'ye alındı (kapsam için). NOT: bağsız-kart/patika "Özet" düğümü sorunu (önceki commit d49f4d5) ZATEN GENELDİ — 17 kanunda gizli genel-özet kartı vardı (jandarmakanun 21, kabahatler 5, atesli 5, ililaresi 4, …), hepsi otomatik Özet düğümüne bağlandı; ek kod gerekmedi. tsc 0.
> **Patika "Özet" düğümü — bağsız kartlar görünür (28 Haz):** 2803 gibi kanunlarda genel-özet/ek-geçici özet kartları hiçbir madde düğümüne bağlı değildi → patikada GÖRÜNMÜYORDU (ör. 2803: 31 kart ama patikada 10). DÜZELTME: `seed.ts` `_patikaBaglama` — madde-bağlı olmayan TÜM kartlar kanunun yeni **"Özet" düğümüne** (id=law*1000+999, sira 999=en son) bağlanır → patikada görünür + ilerleme paydasına girer → artık 31/31. Kartlar DEĞİŞMEDİ (yalnız bolumler/bolum_kartlari) → **migration v20** sadece bolumler/bolum_kartlari tazeler, **SRS TAM korunur** (önceki resync'lerin aksine). `SCHEMA_VERSION=20`. patika `dugum.bolum.ad`'ı doğrudan gösterdiği için "Özet" düğümü sorunsuz; getBolumKartIds artık tüm kartları kapsar (önceki ilerleme fix ile uyumlu). Web parite (paylaşılan seed). tsc 0.
> **Mevzuat ilerleme sayım fix (28 Haz):** Tamamlanan kanun "0/31" görünüyor + hem Tamamlananlar hem Başlamadıklarım'da çıkıyordu. KÖK NEDEN: `getLaws.kartSayisi` TÜM kartları sayıyordu (genel-özet/bağlanmayan dahil) ama patika yalnız bölüme bağlı kartları çalıştırıyor → payda şişip %100 imkânsızdı (ör. jandarmakanun 31 kart ama 10 düğüm + 21 genel-özet). DÜZELTME: yeni `getBolumKartIds()` (4-dosya senkron: types/d.ts/native/web) = patikada çalışılabilir (bölüme bağlı) kart kümesi. mevzuat.tsx: toplam (payda) = law başına BAĞLI kart sayısı (`toplamMap`), çalışılmış (pay) = yalnız BAĞLI + kutu≥1. KanunSatir/DevamEtKart `toplam` prop'u alır (law.kartSayisi yerine). Artık tüm patika bitince bağlı/bağlı = %100 → Tamamlananlar; mutually-exclusive filtreler doğru. (Not: v19 resync eski SRS'i orphan etti → o kanunlar yeniden çalışılınca doğru sayılır.) tsc 0 hata.
> **Sesli anlatım hız seçeneği (28 Haz):** `SesOynatici`'ya hız butonu eklendi (1x/1.25x/1.5x/2x döngü, `HIZLAR`). `player.setPlaybackRate(rate, 'high')` + mount'ta `player.shouldCorrectPitch=true` → hızlanınca konuşma tizleşmez (doğal kalır). Kontrol satırı 4 butona göre `space-between`. Yalnız gerçek-ses oynatıcısı (mp3) — TtsBar'da zaten hız vardı. tsc 0 hata.
> **Kart "Öğrendim" kilidi (28 Haz):** Görsel ekranda görünmeden "Öğrendim" basılamıyordu artık — `gorselGorundu` state (kart değişince sıfır), StudyCard `onGorundu` prop'u `<Image onLoad>` VE `onError`'da tetiklenir (hata olsa da soft-lock olmaz). Görselsiz/registry'siz kartta beklenecek görsel yok → hemen açık (`ogrenebilir = !gorselVar || gorselGorundu`). Footer Öğrendim: kilitliyken `disabled` + soluk + ikon `eye-off-outline` + etiket "Önce görseli gör". Tekrar Hatırlat dokunulmadı (görmeden de "tekrar göster" denebilir). SRS/cevapla/modal/queue mantığı değişmedi. tsc 0 hata.
> **Yayın İş Planı V2 (27 Haz):** 6 maddelik yayın yol haritası `docs/YAYIN_IS_PLANI_V2.md`'ye yazıldı. Kararlar: depolama=**Supabase Storage**, ödeme=**doğrudan Play Billing** (RevenueCat yok), gating=en son. Fazlar: 0 hesap açılışları (Supabase/OAuth/Play ödeme profili) · 1 içerik tamamla (kalan sesler+10 görsel) · 2 içerik→Storage + imzalı URL/cache (643MB bunlu çöz) · 3 Gmail giriş aktivasyonu (iskele hazır) + gizlilik/Data Safety güncelle · 4 Play Billing (react-native-iap + Edge Function makbuz doğrulama) · 5 gating. Kritik: app'te 643MB gömülü içerik → sunucuya taşımak yayın ön şartı. Auth iskelesi zaten kurulu/uykuda (UYELIK_AKTIF=false). **SIRA GÜNCELLENDİ (28 Haz): içerik hâlâ üretiliyor → içerik→Storage + ödeme + gating EN SONA; önce Faz 1 (kalan sesler+10 görsel) + Faz 3 (Gmail giriş).** Sıradaki teknik adım: Faz 3 (Gmail aktivasyon).
> **16 kanun gerçek ses + görsel resync (27 Haz):** 16 müşterek kanunun insan-seslendirme mp3'leri (214 dosya) `assets/sesler/<slug>/`'a **görsel anahtarıyla** yerleştirildi (`scripts/ses-yerlestir.py` — `icerik-yerlestir.py`'nin AYNI `parse()`/çakışma mantığı → ses anahtarı = gorsel_yolu birebir). 7 kanun zaten güncel kaynakla birebirdi; **9 kanunun repo görselleri `uretilen_gorseller`'den ESKİYDİ** → o 9 kanun (ililaresi, terorle, jandarmakanun, ailekoruma, sozlesmeliasb, kvksilme, atesli, tufekler, sozlesmeliyon) `icerik-yerlestir.py` ile güncel kaynaktan yeniden import edildi (eski 112 webp→yeni 129 png), `gorsel:uret`+`ses:uret` (539 görsel, 215 ses). **16/16 kanunda ses↔görsel TAM eşleşme** (gorselsiz_ses=0, sessiz_gorsel=0). Patika kapsamı (`SEED_KAPSAM` Object.assign, 9 kanun) `_icerik_rapor.json` kapsamından güncellendi. `icerik-yerlestir.py` " TAMAM" ekli klasörleri bulacak şekilde yamandı (`gercek_folder`). **Migration v19** (cards/bolumler/bolum_kartlari DELETE+reseed; SRS korunur) + `SCHEMA_VERSION=19`. Web parite (aynı SEED_*). Kart akışı: `SesOynatici` zaten `KART_SESLERI[gorsel_yolu]` ile çalıştığından kod değişmedi. tsc 0 hata. (TCK görselleri+7 kanun webp kaldı; diğer 9 png — `gorsel:uret` ikisini de destekler.)
> **TCK gerçek ses (27 Haz) — kart akışında mp3 oynatma:** TCK için 54 insan-seslendirme mp3 (kaynak `…01_5237_TCK TAMAM\ses dosyaları`) `assets/sesler/tck/`'e **görsel anahtarıyla** kopyalandı (ör. `5237TCK MADDE 001-1.mp3`→`tck_m1_1.mp3`, `…021-22-AYIRT`→`tck_ayirt_m21_22`, `…037-44-OZET`→`tck_ozet_m37_44`); ses↔görsel anahtarları **54/54 birebir** (diff temiz). `npm run ses:uret` → `kart-sesleri.ts` (55 kayıt: 54 tck + 1 4733). Yeni `components/card-flow/ses-oynatici.tsx` (expo-audio): otomatik başlar, GERÇEK ilerleme/seek + play/pause + ±10sn, bitince `onBitti`→"geçelim mi?" modalı (TtsBar paritesi). akis.tsx ses panelinde: kartın `KART_SESLERI[gorsel_yolu]`'su VARSA `SesOynatici` (gerçek ses), YOKSA `TtsBar` (robotik TTS) — anahtar `gorsel_yolu` (DB/seed/migration DEĞİŞMEDİ). TCK görselleri+patika kapsamı zaten `uretilen_gorseller` (54 png) ile birebir uyumluydu → görsel/patika DEĞİŞMEDİ. Diğer kanunlar (mp3 yok) TTS'te kalır; Sesli Nöbet ekranı (ses_yolu DB) kapsam dışı. tsc 0 hata. (Ses ~57MB repo'ya eklendi.)
> **Ara Prompt A (27 Haz) — "Aklına Çivile" gerçek arama + "Kartlar" çipi gerçek anlatıma bağlandı:** `lib/ara.ts`: `araIndeksHazirla` 3. arg `sesAl(gorselYolu)` aldı; `AraKayit`'e `anlatim`(=GERÇEK seslendirme metni, eski placeholder `c.anlatim_metni` BIRAKILDI) + `civile`(=ses metninin "Aklınıza çivileyin: …" kısmı, `civileAyikla` ile ayıklanır) + `anlatimK`/`civileK`. `AraKapsam`'a `'civile'` eklendi; `araKanunlar`: 'kart'→`anlatimK` (gerçek ses), 'civile'→`civileK`, 'hepsi' ikisini de kapsar (civile ⊂ anlatim → çift saymamak için civile yalnız 'civile' kapsamında toplama/skora girer, 'hepsi'de sadece snippet tercihi). Snippet kaynağı metin>çivile>anlatım>başlık. `ara.tsx`: `KART_SES_METINLERI` import; `araIndeksHazirla(cards, maddeMetni, g => KART_SES_METINLERI[g]??null)`; çip eşlemesi `aktifCip==='civile'?'kart'` düşürmesi KALDIRILDI → `'civile'→'civile'`. PHRASE eşleştirme (q bütün indexOf) KORUNDU — AND = Prompt B. DOKUNULMADI: srs/queue/kanun-kartlari, nav, son-aramalar, popüler/sık-açılan, UI. tsc 0 hata.
> **MODAL Parça 1 (26 Haz) — ses bitince "geçelim mi?" modalı + bugün sorma:** Yeni `lib/modal-erteleme.ts` (son-aramalar deseni, AsyncStorage key `jsps.modalErteleme`, DB yok): `getErteleme()` (epoch ms, yoksa 0), `erteleBugun()` (yarın 08:00 timestamp sakla: `new Date()` +1 gün, `setHours(8,0,0,0)`), `ertelemeAktifMi()` (`Date.now() < getErteleme()`). akis.tsx: `modalAcik`+`bugunSorma` state; `anlatimBitti` true olunca (TtsBar `onBitti` doğal bitiş — durdur/seek/kart-değişim tetiklemez) `ertelemeAktifMi()` AKTİF DEĞİLSE modal açılır. Modal = **ekran-içi absoluteFill overlay** (GorselZoom deseni, RN Modal DEĞİL → AkisScreen unmount olmaz, TtsBar mount kalır, ses kesilmez). 3 buton: **Öğrendim** → `cevapla('biliyorum')`+kapat, **Tekrar Hatırlat** → `cevapla('zor')`+kapat, **Kartı inceleyeceğim** → sadece kapat (cevapla YOK, index değişmez). **Bugün tekrar sorma** checkbox: işaretliyse hangi butona basılırsa `erteleBugun()`. Footer ESKİ "anlatimBitti" varyantı ("Sıradaki konu X" + tek buton) KALDIRILDI → footer artık ses bitince de NORMAL (Öğrendim/Tekrar Hatırlat); geçiş sorusu modala taşındı. Orphan `siradaki`/`devamBtn` stilleri silindi. DOKUNULMADI: cevapla/recordReview/SRS, queue/index/swipe, TTS çekirdek+ses-otomatik, görsel/oran, filigran, üst header. Sıradaki kanun = Parça 2. tsc 0 hata.
> **Kart footer metin (26 Haz):** Sol buton "Bildim" → "Öğrendim" (sadece görünen string; ikon `check-bold`, `onPress cevapla('biliyorum')`, SRS, swipe DOKUNULMADI). "Tekrar Hatırlat" aynen. tsc 0 hata.
> **BUG FIX (26 Haz) — akis web crash (resolveAssetSource):** 2fa2442'de görsel doğal-oran hesabı `RNImage.resolveAssetSource` ile yapılıyordu; bu API **react-native-web'de YOK** → web'de `AkisScreen` render'ında "TypeError: resolveAssetSource is not a function" → "Bir şeyler ters gitti" crash. ÇÖZÜM: resolveAssetSource tamamen kaldırıldı → oran **expo-image `onLoad`'dan** alınıyor (`e.source.width/height`, web+native AYNI, crash yok). akis: `oran` state (index değişince null→fallback), `StudyCard onOran={setOran}`, `gorselBoyut` artık `oran`'a bağlı; StudyCard `<Image onLoad>` gerçek boyutu bildirir. İlk frame fallback (`gorselSarOran` 0.8), yüklenince doğal oran. 2fa2442'nin ses-panel-yükseklik + tam-metni-scroll düzeltmeleri BOZULMADI. tsc 0 hata. (NOT: konsoldaki "button cannot be descendant of button" — Mevzuat KanunSatir iç içe Pressable — hydration uyarısı, crash sebebi DEĞİL, ayrı iş.)
> **Kart düzeltme (26 Haz) — ses panel yüksekliği + akıllı madde scroll + görsel doğal oran:** (1) **Ses paneli içeriğe sarılır**: `panel`'den sabit `height:'54%'` kaldırıldı → yükseklik içeriğe göre; ses paneli artık sadece TtsBar kontrolleri kadar yüksek (eski kocaman boş kutu gitti). Madde paneli `panelMadde` (`maxHeight:'60%'`) ile maks yüksekliğe oturur. (2) **"Tam metni aç" → akıllı "Devamını gör" + panel-içi scroll**: ayrı `MaddeMetniSheet` KALDIRILDI (import+state+JSX). Madde metni kutu içinde scroll (`maddeScrollRef`); `onLayout`/`onContentSizeChange` ile içerik vs görünür yükseklik → `maddeUzun`. Kısa metinde buton YOK; uzun metinde "Devamını gör →" (basınca ~1 ekran aşağı `scrollTo`), `onScroll` ile sona gelince (`maddeSonda`) buton gizlenir. Kutu BÜYÜMEZ (maxHeight sabit), metin içeride kayar. (3) **Görsel kutusu doğal orana uydu**: StudyCard sabit `aspectRatio:0.8` KALDIRILDI → görsel kutuyu tam doldurur (`width/height:100%`). akis'te `gorselBoyut` = `RNImage.resolveAssetSource` ile görselin doğal oranı + `gorselAlan` onLayout ölçüsünden contain-fit (alan genişliğine kadar büyür, yüksekliği aşmaz; çok uzun görselde genişlik orana göre azalır). Kutu görsel oranında → letterbox/kırpma YOK; footer/ikon örtülmez. Oran/ölçü yokken `gorselSarOran` (aspectRatio 0.8) fallback. DOKUNULMADI: ses-otomatik/TtsBar çekirdek, queue/index/swipe, cevapla/SRS, perf(#2/#3), filigran, GorselZoom, footer, üst header(9A-3), panel üste-binme. tsc 0 hata.
> **FAZ 9A-3 (26 Haz) — kart düzeni sadeleştirme + panel görsel üstüne biner:** (1) **Üst header sade**: ✕ (sol) + ortada `${index+1}/${length} · %${yuzde}` + ince bar (5px); başlık ve Müşterek rozeti KALDIRILDI (görsel içinde künye zaten yazıyor) → kazanılan alan görsele. İlerleme verisi korunur. (2) **🎧/📄 görselin ALTINDA**: eski görsel-içi absolute `yanIkonlar` (sağ-alt, içeriği örtüyordu) kaldırıldı → görsel altında yan yana 2 eşit buton (`ikonSatir`/`ikonBtn`; aktif altın dolu, pasif altın çerçeve) → görsel üstü TAMAMEN temiz, çakışma kökten bitti. (3) **Panel görselin ÜSTÜNE biner**: `panel` absolute overlay (görsel alanının alt %54'ü, üst yarı+künye görünür kalır, sağ üst ✕). (4) **TEK panel alanı**: `sesAcik`+`maddeGenis` ikili state → tek `acikPanel: 'yok'|'ses'|'madde'` (🎧→ses, 📄→madde, biri açılınca diğeri kapanır; aktif ikona/✕ tekrar bas→yok). ⚠️ **Ses kesilmez**: TtsBar her zaman mount (panel içinde display-toggle), otomatik başlama korunur. (5) **Tek ekran sabit**: dış `ScrollView` KALDIRILDI → flex column (`gorselAlan` flex:1 overflow hidden + ikon satırı + footer). Madde metni panel İÇİNDE kaydırılır (dış ekran uzamaz). DOKUNULMADI: TTS/ses-otomatik, queue/index/swipe, cevapla/SRS, görsel perf(#2/#3)/oran(#4), filigran, GorselZoom, footer, MaddeMetniSheet(9B). tsc 0 hata.
> **akis bug fix #1/#2/#3 (26 Haz):** (#1) TtsBar panelindeki çift "Sesli Anlatım" başlığı (sekme zaten etiketliyor) kaldırıldı — panel `ust` satırı + stili silindi. (#2+#3) Görsel yükleme: StudyCard `<Image>`'a `recyclingKey={card.id}` (kart değişince ESKİ görsel tutulmaz → stale yok, header-görsel senkron) + `cachePolicy="memory-disk"` + `transition={150}` + nötr placeholder bg (`kremZemin`). akis.tsx: komşu kartların (index±1) görseli opacity-0 tam-ölçü Image ile ÖNDEN decode/cache (prefetch eşdeğeri — bundle require asset'leri Image.prefetch string almıyor). DOKUNULMADI: queue/index/swipe, cevapla/SRS, sekme/ses/madde, filigran, GorselZoom, aspectRatio (#4 ayrı). tsc 0 hata. #4 (görsel oran) + #5 (madde metni) = İÇERİK işi, ayrı.
> **FAZ 8B (25 Haz) — Ara ekranı mockup birebir + tüm butonlar bağlı:** `lib/ara.ts`'e `AraKapsam` ('hepsi/metin/madde/kart') + kart `anlatim_metni` indeksi (PHRASE eşleştirme korundu, sadece NEREDE arandığı daralır). ara.tsx krem premium: arama kutusu (altın terazi+büyüteç+input) · 4 filtre çipi (Kanun metni→metin, Madde no→madde, Kartlar→kart, **Aklına Çivile→kart** [ayrı veri yok, isim kalır, sahte değil]; aktif çip kapsamı daraltır, tekrar basınca hepsi) · **Son Aramalar** (8A gerçek geçmiş, çip+Temizle→sonAramalariTemizle) · **Popüler** (sabit tek-kelime: kasten/taksir/izin/yetki/görev/silah → setSorgu) · **Sık Açılan Mevzuatlar** (sabit law_id [1,26,12,2,25], ad getLaws'tan/fb gerçek isim, → /patika; CMK içeriksiz "kalsın" → boş patika) · alt ipucu + örnek çipler. Yazınca sonuçlar, silince öneriler. DOKUNULMADI: araKanunlar PHRASE mantığı, sonuç/snippet/nav, son-aramalar lib. tsc 0 hata.
> **FAZ 8A (25 Haz) — Ara son aramalar saklama:** `lib/son-aramalar.ts` (favori deseni, AsyncStorage key `jsps.sonAramalar`, DB yok): `getSonAramalar/sonAramaEkle/sonAramalariTemizle`. `sonAramaEkle`: trim, <2 harf eklenmez, TR-duyarsız tekrarsız (eskisini çıkar→başa al), max 8. `ara.tsx`: `sonAramalar` state (8B'de gösterilecek) + focus'ta `getSonAramalar` + **debounce** (sorgu ~700ms durunca, ≥2 harf → `sonAramaEkle`; her tuşta DEĞİL, spam yok). Mevcut arama mantığı (araIndeksHazirla/araKanunlar)/nav DOKUNULMADI. tsc 0 hata. Görsel "Son Aramalar" listesi + çip/popüler/sık-açılan → 8B.
> **FAZ 7B (25 Haz) — Karargah 3 öğe bağlandı:** (1) Bildirim ikonu (Pressable+hitSlop) → `/egitim-plani`. (2) Profil ikonu → `/ayarlar`. (3) Rol/Kademe salt-okunur chip → **inline dropdown** (`Dropdown` bileşeni): Rol=branş (`getBranches`→`branches` state) / Kademe=rütbe (`RUTBELER`); tetik (etiket+seçili değer+chevron) → açılır panel (kartKremi+gölge), seçince `setBrans`/`setRutbe` (context **anında** günceller, reload yok) + kapanır. `acikDD` state ile aynı anda yalnız biri açık; tekrar basınca/seçince kapanır. KARAR: absolute overlay yerine **inline expander** (RN zIndex/backdrop cross-platform tuzakları yok; içerik aşağı kayar). Karargah global verisi branştan bağımsız → refetch yok; Mevzuat/Patika sonraki odakta tazelenir. `Chip` bileşeni/stilleri kaldırıldı. Veri/nav/gradyan DOKUNULMADI. tsc 0 hata.
> **Karargah metalik-ish (25 Haz):** `expo-linear-gradient` kuruldu (native modül — dev-client rebuild ister). `ALTIN_GRADYAN = [altinAcik2, altin, altinKoyu] as const`. Hero bilgi 3 sütun → **koyu KUTU** (kartPanelKoyu + kartKenarKoyu, radius 12; ikon+değer üst, etiket alt). Play diski → **LinearGradient** disk (64px, gölge, lacivert play). Geri besleme ikonu → büyük **gradyanlı nişangah diski** (gbDisk 48px, gölge). Krem kartlara daha belirgin gölge (opacity 0.06→0.10). Halka yayı düz altın bırakıldı (abartma yok). Veri/nav/queue DOKUNULMADI. Kalkan eklenmedi (kaldırılmış kaldı). tsc 0 hata.
> **FAZ 7A (25 Haz) — Karargah mockup görünümü (görsel + gerçek veri):** Header sağ üst statik bildirim+profil ikonu (`headerSag`, işlevsiz placeholder) + "Bugünkü çalışma özetin burada." açıklama. HERO Devam Et: eyebrow + "Kart Akışı" + bekleyen kart + 3 bilgi satırı (Tahmini süre = bekleyen×1dk · Son konu = son 'calisma' perf satırı→madde_no GERÇEK · Hedef sabit) + sağda play-circle "Devam Et ›" → /akis. Bugünün Görevi (altın bar + Inter800 sayılar). Geri Besleme (zayifSayi). 3 kutu: Genel ilerleme **HALKA** (svg ring, altinKoyu) + Nöbet serisi (🔥) + **Bekleyen kart** (yeni, queue.length). Günün Maddesi + "İncele ›". Tüm veri GERÇEK/türetilebilir (uydurma yok; tahmini süre "tahmini" etiketli ×1dk). DOKUNULMADI: queue/stats/streak/zayifKartlar/gunMadde/nav/useFocusEffect; branş-rütbe chip (dropdown 7B'de). tsc 0 hata.
> **Patika PNG ayak izi (25 Haz):** Bot izleri SVG çizim → **gerçek PNG altın ayak izi**. `assets/images/ciftayak.png` (1254² şeffaf, sol yarı=sol ayak/sağ yarı=sağ ayak). `AyakIzi`: kap AYAK_W×AYAK_H(14×28) + içinde 2× genişlikte `<Image>` yatay kaydır (`left: sol?0:-AYAK_W`) + `overflow:hidden` → tek yarı; kap (x,y) merkezli + `rotate(aci)` (teğete paralel). `segmentPostallari` aynı yay-uzunluk/açı mantığıyla sol-sağ ALTERNATİF (çift k=sol, tek k=sağ; ±AYAK_OFSET); izSayisi clamp 2..6 (/32, perf). Ayaklar artık RN View (SVG dışında, konnektörün üstünde/düğümlerin altında). Eski SVG `PostalIzi` + `G`/`Rect` importları kaldırıldı (yalnız `Path` kaldı). Yalnız yürünmüş segmentte. tsc 0 hata.
> **Patika PNG arka plan (25 Haz):** Masaüstü `patikaarkaplan.png` (krem dağ/orman/pusula manzara) → `assets/images/patika-arkaplan.png`. patika harita alanına `expo-image` `<Image absoluteFill contentFit="cover" pointerEvents=none>` arka plan (kanun kartının altında, düğüm/postal/etiket üstte). Eski SVG `HaritaDokusu` (topografik/pusula/dağ çizimi) + kullanılmayan `Circle`/`Line` importları KALDIRILDI. Düğüm/bot izi/glow/aktifIndex/nav DOKUNULMADI. tsc 0 hata.
> **FAZ 6A+6B (25 Haz) — Patika mockup'a yaklaştırma (görsel):** (6A) `HaritaDokusu` SVG arka plan (deterministik topografik kontur + sol-alt pusula + sağ-alt dağ, opacity 0.06-0.09 altinKoyu, düğüm/postal altında, pointerEvents none); aktif düğüm iki katman altın glow + sağ-üst altın yıldız rozeti + lacivert etiket "Madde N"/"• ŞU ANKİ KONUM" (buradasın kalktı); tamam tiki yeşil→altinKoyu. (6B) `Screen`'e opsiyonel `headerSag` + `headerAltinCizgi` prop'ları (diğer ekranlar etkilenmez); patika header sağ-üst `scale-balance` altın ikon + header altı 1px altın çizgi; kanun şeridi → büyük `kanunKart` (gölge + kitap altın + ad Inter700 + X/Y kart). Mantık (aktifIndex/bolumIlerleme/streak/nav/segment) DOKUNULMADI. lucide yok → MCI ikonları. tsc 0 hata. Telefon görsel teyit bekliyor.
> **Akis TTS çökme fix (25 Haz):** Waveform'a dokununca çökme — web'de `e.nativeEvent.locationX` undefined → `undefined/W=NaN` → tüm clamp'ler NaN → `cumleler[NaN]` undefined → `Speech.speak(undefined).length` patlıyordu. Fix: `dalgaDokun`'da `Number.isFinite(lx)` guard + güvenli clamp; `cumleOku`/`oynat`'ta `cumleler[i]===undefined` guard (savunma). `sar` ◀/▶ zaten clamp'li. tsc 0 hata.
> **Akis TTS akıcılaştırma (25 Haz):** TtsBar waveform: statik (aktif+1)/N zıplaması yerine **Animated.Value `oranAnim`** — cümle okunurken playhead/dolgu, karakter sayısı/hız ile TAHMİNİ sürede (clamp 0.8–8sn, Easing.linear) yumuşakça akar; durunca donar, cümle değişince yeni cümle başından. Dolgu = iki katman (sönük taban Svg + animasyonlu genişlikli altın Svg overlay); playhead = Animated translateX çizgi+nokta. **Waveform dokunulabilir** (Pressable+locationX → oran → en yakın cümle → oynat). Saniye/saat YOK (cümle hassasiyeti, dürüst). DOKUNULMADI: cumlelereBol/Speech/rate/hız, ◀/▶ cümle, cevapla/SRS, sekme/görsel/footer. `Circle` importu kaldırıldı. tsc 0 hata.
> **Akis görsel başrol (25 Haz) — SADECE görsel/scroll:** Bug: görsel kart küçük dış ScrollView viewport'una sıkışıp kart-içi scroll bar çıkıyordu (alt blok + büyük ses paneli yer kapıyordu). Fix: **tek dış ScrollView** — görsel kart + alt blok birlikte akıyor, görsel kart İÇİNDE scroll YOK; `showsVerticalScrollIndicator={false}`. altBlok padding'i scrollContent'e devredildi. Ses paneli kompakt (DALGA_Y 46→38, sayaç+legend tek satır, padding/gap küçült). Madde önizleme 3→2 satır. Görsel doğal aspectRatio 0.8 ile tam boy = baskın öğe. DOKUNULMADI: swipe/Pan, cevapla/SRS, TTS mantığı, filigran, GorselZoom (dokun→zoom çalışır). tsc 0 hata.
> **FAZ 5A+5B (25 Haz) — Kart çalışma ekranı (akis) koyu premium:** (5A) theme.ts koyu kart token'ları (kartZeminKoyu/kartYuzeyKoyu/kartPanelKoyu/kartMetinAcik/kartMetinIkincil/kartKenarKoyu/altinAcik2) + FiligranOpaklik 0.15→0.22. akis.tsx KOYU: header (geri + madde/baslik + X/Y + altın Müşterek rozeti + ilerleme barı + altın %Z **konum** yüzdesi); görsel kart altın çerçeveli sarmal (StudyCard/GorselZoom korundu); Madde Metni krem kartı (önizleme 3 satır + "Tam metni aç"→sheet); alt butonlar "Bildim"(lacivert+altın daire check→'biliyorum') / "Tekrar Hatırlat"(krem+refresh→'zor'); yükleme/hata/boş/bitti krem sarmalda. (5B) TtsBar koyu premium: **TTS korundu** (expo-speech), **dekoratif** svg waveform (dolu = CÜMLE ilerlemesi, gerçek genlik/süre DEĞİL) + playhead; cümle sayacı (saat/süre YOK — dürüst); legend; ◀/▶ **cümle** navigasyonu; büyük dur/devam (altın); **hız 1x/1.25x/1.5x** (Speech rate, gerçek). DOKUNULMADI: swipe/Pan, cevapla/recordReview/SRS, kuyruk modları, FLAG_SECURE, cumlelereBol/Speech mantığı (sadece rate eklendi). Carousel yok (tek görsel). tsc 0 hata. Telefon ses/görsel teyit bekliyor.
> **Mevzuat görsel uyum (25 Haz) — SADECE görsel:** (1) %100 bar boş görünme bug fix: `Bar` dolum `width:'${w}%'` → **flex-oranlı** (barFill flex:w + spacer flex:100-w; barTrack flexDirection:row) → %100'de tam dolar. (2) "Başla" sönük yazı → **kutulu ikincil buton** (`baslaBtn`: kartKremi+kenarlik+Radius.m, içinde altın play + metin); %100'de altın tik korunur. (3) Devam Et kartına sağda **kitap dairesi** (`bookDaire` altinSolukYuzey + altinKoyu book ikon); CTA tam genişlik kaldı. (4) satirSag `gap two + marginLeft one` → kalp↔Başla ferah. Mantığa dokunulmadı. tsc 0 hata.
> **Mevzuat rötuş (25 Haz) — SADECE görsel/layout:** (1) ❗Çip layout bug fix: yatay `ScrollView` flex column içinde dikey DEV SÜTUN oluyordu → `cipScroll {flexGrow:0}` + `cipSeridi {flexDirection:'row'}` + çip sabit `height:44` + `alignSelf:'flex-start'`. (2) Favori filtresi açıkken çip şeridi gizli (`cipGoster && !favoriAcik`). (3) `%Z` bar sağına hizalı (`barYuzde {minWidth:40, textAlign:right, flexShrink:0}`). (4) Bar dolu rengi `ilerlemeDolu→altinKoyu` (#B88917, krem üstünde okunur); monogram no `altin` kaldı. (5) Satır play ikonu altın daire içinde (`altinSolukYuzey` dolgu + `altinKoyu` play). (6) Devam Et başlığı: bookmark ikon + "Tümünü gör >" (filtreleri sıfırlar). (7) Tab bar aktif sekme üstünde ince altın çizgi (`_layout` icon sarmalı, pasif saydam → ikon kaymaz). Mantığa dokunulmadı. tsc 0 hata.
> **FAZ 4+5 (25 Haz) — Mevzuat filtre çipleri + favoriler:** (4) İlerleme bazlı çipler (Tümü/Devam Ettiklerim 0<%<100/Bitmeyenler %<100/Tamamlananlar %100) — yatay şerit, filtre zinciri `musterek→ÇİP→arama→map` (elde süzme, yeni sorgu yok); arama yanı filtre butonu (56px, çip şeridini aç/kapatır). (5) **Favori kanun:** yeni `lib/favori.ts` (AsyncStorage key `jsps.favori`, JSON number[], `getFavoriler/setFavoriler/toggleFavori`; DB/migration yok). Satırda kalp ikonu (AYRI Pressable + `stopPropagation` → yanlışlıkla /patika YOK), dolu=altın favori. Sağ üstte "Favorilerim" altın çerçeveli filtre toggle (zincire `taban` olarak girer: `musterek→FAVORİ→ÇİP→arama`). Boş durumlar: "Bu filtrede kanun yok" / "Henüz favori kanun yok — kalbe dokun". DOKUNULMADI: getLaws filtreleri, /patika nav, ilerleme/çalışıldı % hesabı, Devam Et (3A). tsc 0 hata. **Kategoriler çipi → Faz 6** (Blok yalnız müşterek/branş; branş içeriği yok → veri sınırlı).
> **FAZ 3A+3B (25 Haz) — Mevzuat krem premium redesign + Devam Et:** (3A) `lib/devamet.ts` saf `sonCalisilanKanun(perf, cardLawMap, lawIlerleme)` → `yok/devam/siradaki/hepsiBitti` (kart_performans log'undan son 'calisma'; migration YOK). (3B) Mevzuat: `Screen` + Playfair başlık; açıklama satırı + arama pill (TR filtre, placeholder güncel); **Devam Et hero kartı** (72px lacivert monogram + altın no, ilerleme barı, altın CTA; 'yok'→gizli, 'siradaki'→"🎖️ bitti — sıradaki", 'hepsiBitti'→kutlama); **TÜM MEVZUAT** bölümü (terazi ikon + "Toplam N kanun") + monogram satırlar (56px lacivert kare, ilerleme barı track/dolu, **%100 → altın tik (yeşil DEĞİL)**, play+Başla/Devam+chevron). Veri: `getPerformans`+`getAllCards` eklendi (getLaws+getStudyCards yanına). Renkler theme token'ı, SVG ring kaldırıldı (bar = View). Çift scroll yok. DOKUNULMADI: getLaws filtreleri, /patika lawId nav, çalışıldı % hesabı, OGRENILDI_KUTU. tsc 0 hata. ÇİP/FAVORİ/KATEGORİ → Faz 4-5-6.
> **FAZ 1 (24 Haz) — KREM PREMIUM MARKA TEMELİ (app-wide):** Yeni kanonik marka kuruldu. (1) `theme.ts` Palette ChatGPT krem premium paletine güncellendi — lacivert `#0B1F3A`, altın **şampanya** `#C9A227` (eski `#E6C24A` gitti), krem `#F7F3EA`/`#FFFCF5`; yeni anahtarlar: lacivert2/anaMetin/ayirici/altinKoyu `#B88917`/altinSolukYuzey/ilerlemeDolu/ilerlemeTrack/kartGolge. ten/yesil/amber/kirmizi korundu. (2) **Inter + Playfair Display** kuruldu (`@expo-google-fonts/*`), `_layout.tsx` `useFonts` + `expo-splash-screen` (fontlar gelene kadar render yok → FOUT yok); onboarding guard/Stack bozulmadı. (3) `app-text.tsx` variant→font: dev/baslik **Playfair 700**, altBaslik Inter 800, govde Inter 700, kucuk Inter 500, etiket Inter 600 (bold → +1 ağırlık); **default color `lacivert`→`anaMetin`** (`lacivert` anahtarı duruyor, kırılma yok). (4) CLAUDE.md marka bölümü yeni değerlerle güncellendi (eski hex'ler geçersiz). NOT: önceki turda dark token'lar zaten silinmiş + patika/mevzuat krem'di → "dark dursun" notu geçersizdi; yeni hex'ler o ekranlara da aktı (şampanya altını). `FontFamily` export'u korundu (ara/tab/watermark kullanıyor). tsc 0 hata. **Faz 2-3:** ekran ekran premium cila + bileşenler. Telefon/web font teyidi bekliyor.
> **Takip 5 (24 Haz) — KREM PREMIUM'A GERİ DÖNÜŞ (dark-first iptal):** Tüm uygulama tek tip krem marka temasına çekildi. 6 commit: (1) `theme.ts` `altinKoyu '#C9A227'` accent (krem üstünde okunur altın). (2) StatusBar 3 yerde `light→dark` (_layout global + patika + mevzuat). (3) **patika** yerel DarkScaffold kaldırıldı → ortak `Screen` (geri butonu `onGeri` ile); dark token→krem map; SVG postal izi + aktif düğüm `altinKoyu`, yürünmemiş konnektör `kenarlik`. (4) **mevzuat** inline scaffold→`Screen`; lacivert monogram rozet + altın numara, halka yayı `altinKoyu`, başlanmadı→chevron. (5) Kullanılmayan 8 dark token (`zeminKoyu/yuzeyKoyu/yuzeyKoyuSoluk/kenarlikKoyu/metinAcik/metinSolukAcik/altinAcik/yesilAcik`) `theme.ts`'ten SİLİNDİ (grep: 0 kullanım). Artık `Screen.tsx` (krem) tek scaffold; patika+mevzuat ona bağlı. Çift scroll yok. Davranış (düğüm/postal/nav/çalışıldı %/liste/arama) korundu. tsc 0 hata. Telefon görsel teyit bekliyor.
> **Takip 4 (24 Haz):** **Mevzuat ekranı dark-first "monogram liste"** oldu (ortak `Screen` yerine kendi dark scaffold'ı — `Screen.tsx` DOKUNULMADI, diğer tab'lar etkilenmedi). Her satır: numara rozeti (ad regex `^(\d+)` → "5237"; yoksa kitap ikonu) + ad + "X/Y kart · %Z çalışıldı" + react-native-svg ilerleme halkası (track + altın yay; %100 → yeşil tik). İlerleme **tek `getStudyCards()`** + `law_id→kutu≥1` map (N-sorgu yok; toplam = `LawWithCount.kartSayisi`). Arama çubuğu (istemci-taraflı ad/no filtresi). Durum renkleri: tamam yeşilAcik / başlanmış altın / boş soluk. Kategori yalnız "MÜŞTEREK" (branş "yakında", kapsam dışı). DOKUNULMADI: getLaws filtreleri, `/patika` lawId nav, OGRENILDI_KUTU, yeni DB fonksiyonu/migration yok. tsc 0 hata. Telefon görsel teyit bekliyor (svg halka için dev-client rebuild).
> **Takip 3 (24 Haz):** "Hazırlık" göstergesi **"Çalışıldı %" (kutu≥1/toplam, EŞİKSİZ)** oldu — çalışınca anında hareket etsin. Karargah (global: `calisilanKart/toplamKart`, payda `getCardCount`) + Patika chip'i (artık **kanun-bazlı**: bölümlü → `dugum` toplamı, bölümsüz → `getCardsByLaw(id)` kutu≥1). İkisinde etiket "Hazırlık"→"Çalışıldı". **`OGRENILDI_KUTU`(=4)/`hazirlikYuzde` DOKUNULMADI** (sicil ödül `sicil-servis.ts` + kutu grafiği + Sicil ekranı aynen). Patika'dan kullanılmayan `getStudyCards/getCardCount/hesaplaIstatistik` import'ları temizlendi. tsc 0 hata.
> **Takip 2 (24 Haz):** Patika yürünen segmentlerde konnektör çizgisi yerine **postal izi** (bot tabanı) çiziliyor. Saf bezier helper'lar eklendi: `bezierNokta` (kübik formül) + `bezierAci` (teğet+90); segment boyuna orantılı 2–5 iz, kenarlardan içeri t, yürüyüş yönüne dik ±2.5px alternatif kaydırma; iz = iki `<Ellipse>` (ön taban+topuk) `<G translate+rotate>` içinde, altın fill. Yürünmemiş ara MEVCUT kesikli soluk konnektör kalır (postal yok). `Svg`'ye `Ellipse, G` import edildi. Düğümler/navigasyon/veri/üst bar/kart sayacı DOKUNULMADI. tsc 0 hata. Telefon/web görsel teyit bekliyor.
> **Takip (24 Haz):** Teşhis sonrası patika üst şerit sayacı **bölüm bazından KART bazına** çevrildi (`calisilanKart/toplamKart = Σ d.calisilan / Σ d.toplam`; `toplamKart===0 → "0 kart"`). Sebep: kartsız iskelet bölümler paydayı şişiriyordu ("0/48 bölüm" tıkanması) + çalışınca anında hareket etsin (`calisilan = kutu≥1`). Düğüm/yol/navigasyon/seri/hazırlık DOKUNULMADI. tsc 0 hata. (Not: Hazırlık % hâlâ `kutu≥4` eşikli → yavaş hareket eder; bu ayrı metrik, bilinçli.)
>
> ### ▶ 23 Haz — FİLİGRAN ZOOM FIX + vCode 6 BUILD
> **Sorun:** Karta tıklayınca açılan tam ekran yakınlaştırma (GorselZoom) overlay'i filigran (Watermark) içermiyordu → zoom modunda forensic filigran kayboluyordu. **Düzeltme (commit `a0393ba`):** `GorselZoom`'a `filigranMetin` prop'u; Watermark artık ImageZoom üstüne `pointerEvents="none"` ile çiziliyor (pinch/pan altıdaki görsele geçer, filigran ekranı sabit kaplar). Aynı metin kart+zoom'da ortak (`study-card.tsx`). tsc 0 hata.
> **BUILD ALINDI ✅ (23 Haz 12:37):** versionCode 5→6 (app.json). `npx expo prebuild -p android --no-install` → `gradlew :app:bundleRelease` → BUILD SUCCESSFUL (1m44s, Android Studio jbr). `D:\mevzu-yerel.aab` (170.7 MB, vCode 6). **Doğrulandı:** AAB protobuf manifest → versionCode=6, READ_MEDIA_IMAGES/VIDEO YOK, izinler temiz; imza **CN=MEVZU-JSPS** (keytool, aynı upload key).
> **BEKLEYEN (kullanıcı, Play Console):** `D:\mevzu-yerel.aab` (vCode 6) aynı kapalı test track'ine yeni sürüm olarak yükle → incelemeye gönder. (NOT: Play foto/video beyanı hâlâ soruyorsa eski sürümden gelen sticky form; beyanı "kullanılmıyor" olarak güncelle.)
>
> ### ▶ YARIN BURADAN DEVAM (19 Haz akşamı yedeklendi — tüm commit'ler origin/master'da)
> Kapalı test CANLI (vCode 5, 12/14 sayacı). Bugün **saha iyileştirme turu** yapıldı — canlı iş planı `docs/IS_PLANI.md` (14 madde, ✅'ler işli). **Biten (kod-only):** Arama sekmesi · #3 swipe · #4 kart düzeni (sabit 4:5 dikey kutu + contain + Sesli/Madde alta pinli — son commit `18c5019`; **telefon teyidi bekliyor**) · #8 Karargah cila · #9 rütbe (4 rütbe, müşterek matris kilitli) · #10 Ayarlar · #11 Evsaf · ölü kod temizliği.
> **YARIN İLK:** (1) kart düzenini telefonda teyit (kenar boşluğu/sığma) — gerekirse `study-card.tsx aspectRatio 0.8` ayarı **veya** kaynak görselleri 4:5 üret. (2) Kalan kümeler: **B** logo/ikon (#5,#12 — kullanıcı görseli) · **C** içerik (#6 patika görsel-madde hizalama, #7 ses) · **A** backend (#1 zorunlu Gmail, #2 bulut yedek, #13 kişiye-özel filigran — politika ağır, ayrı faz) · **F** tatbikat (#14) · branş rütbe matrisi (PDF branş bölümleri, `pdftotext -table -enc UTF-8`). Hâlâ kod-only: denetim **Tier 1** (bildirim no-op, branş vaadi) + **Tier 2** (patika N+1 perf).

> # 🔴 2. RED — YANILTICI İDDİALAR (Resmî Kaynak Linki Eksik) — 18 Haz
>
> **vCode 4 kapalı test REDDEDİLDİ (2. kez, FARKLI sebep).** Bu kez **"Yanıltıcı İddialar Politikası"** — alt başlık: **"Resmi Bilgilerle İlgili Kaynak Bağlantısı Eksik"**. Muafiyet ibaresi ARTIK KABUL (Google red metninde alıntılıyor → impersonation sorunu çözülmüş). Yeni sorun: uygulama **resmî kanun metinleri** gösteriyor ama orijinal kaynağa (.gov, mevzuat.gov.tr) **net ve erişilebilir URL/bağlantı yoktu**. Flag'lenen yer: **Tam açıklama (tr-TR)**.
>
> **YAPILDI (kod + docs, 18 Haz):** Google'ın "düzeltme yöntemi" maddelerine birebir:
> 1. **`config.ts`** → `MEVZUAT_KAYNAK_URL = 'https://www.mevzuat.gov.tr'` (tek kaynak sabiti).
> 2. **`madde-metni-sheet.tsx`** → resmî metin panelinin altına **sabit, tıklanabilir** "Kaynak: mevzuat.gov.tr (T.C. resmî mevzuat veritabanı)" satırı (`Linking.openURL`; ScrollView flex:1 → kaynak şeridi altta sabit). "net ve erişilebilir URL/bağlantı" şartını birebir karşılar.
> 3. **`yasal-metin.ts`** (Şartlar) → yeni "2) RESMÎ KAYNAK" maddesi (URL'le); sonraki maddeler 3-7'ye kaydı.
> 4. **`docs/PLAY_MAGAZA_GIRISI.md`** → açıklamaya "KANUN METİNLERİNİN KAYNAĞI" bölümü + URL; "Orijinal kanun metni" maddesine kaynak notu. (`docs/MAGAZA_LISTELEME.md` eski taslak da aynı şekilde güçlendirildi.) tsc 0 hata.
>
> **KARAR (kullanıcı): B — SAĞLAM YOL.** In-app kaynak linkli **vCode 5** build alındı + açıklama güncellendi. (A = sadece açıklama, build yok seçeneği vardı; kullanıcı garantili olanı seçti.)
> **BUILD ALINDI ✅ (18 Haz 08:15):** versionCode 4→5 (app.json). `npx expo prebuild -p android --no-install` (config plugin imza/arm64/heap otomatik) → `gradlew :app:bundleRelease` → BUILD SUCCESSFUL (1m21s). `D:\mevzu-yerel.aab` (162.8 MB, vCode 5) hazır; **imza CN=MEVZU-JSPS doğrulandı** (keytool, vCode 4 ile aynı upload key). Madde metni panelinde tıklanabilir mevzuat.gov.tr kaynak satırı artık build'de.
> **BEKLEYEN (kullanıcı, Play Console — kod değil):** (1) `D:\mevzu-yerel.aab`'yi (vCode 5) aynı kapalı test track'ine yeni sürüm olarak yükle. (2) Mağaza açıklamasını `PLAY_MAGAZA_GIRISI.md`'den **birebir** güncelle (**KANUN METİNLERİNİN KAYNAĞI** bölümü dahil). (3) İncelemeye gönder.
>
> ---
>
> # 🔴 1. RED — KİMLİĞE BÜRÜNME (Impersonation) — 18 Haz (ÇÖZÜLDÜ)
>
> **vCode 3 kapalı test REDDEDİLDİ.** Sebep: **Google Play "Kimliğe Bürünme Politikası" ihlali** (içerik değil, marka/ikon). 17 Haz itibarıyla uygulama Play'de kullanılamaz. İki tetikleyici:
> 1. **Yanıltıcı Başlık** — mağaza adı **"JSPS"** (resmî kurum kısaltması, "resmî değil" diye ayrılmıyor).
> 2. **Yanıltıcı Simge** — ikon + özellik grafiğindeki logo **altın kılıç + askeri amblem havası** (Jandarma/devlet çağrışımı).
>
> **KARAR (kullanıcı):** Yeni ad **"Mevzu — JSPS Hazırlık"** · ikonu **kullanıcı yeniden hazırlayacak** (kılıç/askeri sembol YOK).
>
> **YAPILDI (kod, 18 Haz — commit `971c8a4`):** (1) **Yeni sivil ikon** `mevzu-icon-1024.png`+`512.png` (kılıç/askeri amblem/resmî mühür YOK → beyin+kitap+ışık; kullanıcı kare verdi, ImageMagick yok → .NET System.Drawing ile resize). (2) **Özellik grafiği** `feature-graphic-1024x500.png` yeni logoyla yeniden üretildi (.NET ile). (3) `app.json name` → "Mevzu — JSPS Hazırlık". (4) `docs/PLAY_MAGAZA_GIRISI.md` tam açıklamanın **EN BAŞINA büyük harf muafiyet ibaresi** (rakip JSPS PRO örneği gibi) + sonda tekrar. (5) **versionCode 3→4** (app.json + android/app/build.gradle).
> **BUILD ALINDI ✅ (18 Haz 01:33):** Claude kullanıcının makinesinde prebuild --clean + gradle bundleRelease çalıştırdı (config plugin tüm ayarları otomatik uyguladı). `D:\mevzu-yerel.aab` (vCode 4, yeni ikon, ad "Mevzu — JSPS Hazırlık") hazır; **imza CN=MEVZU-JSPS doğrulandı** (keytool). Yüklemeye hazır.
> **BEKLEYEN (kullanıcı, Play Console — kod değil):** (1) ~~vCode 4 AAB build~~ ✅ HAZIR (`D:\mevzu-yerel.aab`). (2) **Mağaza girişini güncelle:** yeni ad + yeni 512 ikon + yeni özellik grafiği + **tam açıklamayı `PLAY_MAGAZA_GIRISI.md`'den kopyala-yapıştır** (muafiyet ibaresi en başta — EN KRİTİK adım, döküman/kod Google'a ulaşmaz, canlı listeyi elle güncellemek şart). (3) **Aynı Kapalı test track'inde YENİ SÜRÜM oluştur** (vCode 3'ü silme, yeni track açma) → vCode 4 yükle → incelemeye gönder.
> **NOT (red analizi, kullanıcı araştırması):** "JSPS" kelimesi yasak değil (markette dolu app var); reddin 3 tetikleyicisi: muafiyet ibaresi yok + ikon resmî/devlet estetiği + isimde ayrıştırıcı kelime yok. Üçü de düzeltildi.
>
> **BUILD OTOMASYONU (18 Haz):** Yeni ikon/isim build'e ancak prebuild ile geçtiği (sadece `gradlew` eski içerik üretir), prebuild de imza/arm64/heap ayarlarını sıfırladığı için → **`plugins/withYerelBuild.js` config plugin yazıldı** (app.json plugins'e eklendi): release imza bloğu (`MEVZU_UPLOAD_*`) + `reactNativeArchitectures=arm64-v8a` + heap `-Xmx4096m` artık **her prebuild'de otomatik**. Regex dönüşümü temiz template'te test edildi (mevcut elle-yazımla birebir). **Build artık sadece:** `npx expo prebuild -p android --no-install` → `cd android` → `gradlew :app:bundleRelease`. İmza creds yine `~/.gradle/gradle.properties` (BOM'suz). `docs/YEREL_BUILD.md` sadeleştirildi.
>
> ---
>
> # 🚦 ÖNCEKİ DURUM — 17 Haz gecesi (YARIN BURADAN DEVAM)
>
> **NEREDEYİZ:** Uygulama (MEVZU-JSPS, paket `app.mevzujsps.android`) **Google Play kapalı teste GÖNDERİLDİ → şu an "İncelemede" (in review).** Gönderim kapsamı: Kapalı test (Alpha) + Mağaza Girişi + Uygulama İçeriği + Mağaza ayarları. Yüklenen sürüm: **versionCode 3**.
>
> **YARIN İLK BAKILACAK:** İnceleme onaylandı mı? (Play Console → Test ve yayınla → Kapalı test). Onaylanınca uygulama testçilere kurulabilir olur.
>
> **14 GÜN / 12 TESTÇİ ŞARTI (yeni hesap):** Üretime geçmek için en az **12 testçi**, **14 gün kesintisiz** kapalı testte olmalı. Sayaç inceleme bitip 12 testçi opt-in olunca işler. İlerleme: "üretime erişim / production access" bölümünde "X/12 testçi, Y/14 gün". **→ Asıl iş yarın: 12 gerçek kişi (Gmail) ayarlayıp test davetini kabul ettirmek, uygulamayı silmemelerini sağlamak.**
>
> ### Bu oturumda yapılanlar (commit'li, master'da)
> 1. **Sentry KALDIRILDI** (`99d4f45`) → %100 offline → Play "Veri Güvenliği" = **veri toplanmıyor** (doğru, işaretlendi).
> 2. **YEREL BUILD'E GEÇİLDİ** — EAS kuyruğu (20+ dk, hatta saatlerce IN_QUEUE) yerine kendi PC'de gradle build. Süreç: `docs/YEREL_BUILD.md`.
> 3. **Gereksiz/hassas izinler kaldırıldı** (config plugin `plugins/withRemovedPermissions.js`): READ_MEDIA_IMAGES/VIDEO, READ/WRITE_EXTERNAL_STORAGE, RECORD_AUDIO, SYSTEM_ALERT_WINDOW. (Play "foto/video izni beyanı" hatasının sebebiydi.) Kalan izinler masum: INTERNET, ses çalma, titreşim.
> 4. **Mağaza metinleri + grafikler hazır**: `docs/PLAY_MAGAZA_GIRISI.md` (ad/kısa/tam açıklama) + simge `assets/images/mevzu-icon-512.png` + özellik grafiği `assets/store/feature-graphic-1024x500.png`.
>
> ### YEREL BUILD — nasıl (özet, detay docs/YEREL_BUILD.md)
> ```
> cd android
> $env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
> $env:ANDROID_HOME="$env:LOCALAPPDATA\Android\Sdk"
> .\gradlew.bat :app:bundleRelease --no-daemon
> # AAB: android/app/build/outputs/bundle/release/app-release.aab → D:\mevzu-yerel.aab
> ```
> - **İmza:** upload key `credentials/upload-keystore.jks` (git-dışı, şifre `credentials/KEYSTORE_BILGI.txt`). Creds `~/.gradle/gradle.properties`'te (**BOM'suz UTF-8 olmalı**, yoksa debug'a düşer → Play reddeder). AAB `CN=MEVZU-JSPS` ile imzalı olmalı (keytool -printcert ile doğrula).
> - **OOM fix:** `android/gradle.properties` → `reactNativeArchitectures=arm64-v8a` (4 ABI birden = daemon OOM) + heap `-Xmx4096m`.
> - **DİKKAT versionCode:** Her yeni Play yüklemesinde ARTMALI. Sıradaki build **versionCode 4** olacak. Kaynak: `app.json` → `android.versionCode` (şu an 3) + `android/app/build.gradle` (prebuild app.json'dan üretir; android/ elle düzenlendiyse ikisini eşitle).
> - **prebuild --clean YAPMA gereksiz yere:** android/ içindeki ayarları (signing, gradle.properties, local.properties) sıfırlar. İzin plugin'i artık app.json'da olduğu için prebuild sonrası izinler otomatik gelir; ama signing/arch/heap'i tekrar uygulamak gerekir.
> - **YÜKLEME:** Play'e `D:\mevzu-yerel.aab` yüklenir. EAS'in `D:\mevzu.aab`'si FARKLI anahtarla imzalı → ASLA yüklenmez (yerel upload key'e bağlı kaldık).
>
> ### versionCode geçmişi (Play'de her biri benzersiz olmalı)
> - vCode 1 → dahili teste (internal) yüklendi (test edildi).
> - vCode 2 → kapalı teste denendi; "foto/video izni" hatası + izin temizliği gerekti.
> - **vCode 3 → kapalı teste GÖNDERİLDİ, ŞU AN İNCELEMEDE.** ← buradayız.
>
> ### Backlog (sonra)
> - **Mağaza ekran görüntüleri:** telefona kurulunca 4 ekran (Karargah, Mevzuat, kart, Sicil) çekilip mağaza girişine eklenecek (zorunlu, henüz eksikse).
> - **Kart görselini sağa/sola kaydırarak değiştirme** (kullanıcı "sonra" dedi).
> - Genel yayında istenirse `armeabi-v7a` ABI'sini de ekle (RAM uygunken).
> - R8/proguard "kod gösterme dosyası" uyarısı = ZARARSIZ, görmezden gel (kod karartma yok).

---

## 1. Nerede kaldık (özet)
Çalışan bir iskelet var: 4 sekmeli navigasyon, gerçek SRS döngüsü, gerçek TCK görselleri, kanun seçip çalışma, branş sistemi + onboarding + DB migration. **Mevzuat artık 66 gerçek kanun başlığıyla dolu (25 müşterek + 41 jandarma, `51ab4bb`); kartlar TCK hariç boş ("yakında").** Android EAS build config de hazır (`e256f95`, profiller: preview/development/production). Sırada: içerik maratonu (49 TCK görselini gerçek kartlara bağlamak).

Geliştirme web'de yürüyor (`npx expo start --web` → localhost). **iPhone Expo Go artık çalışıyor** — bunun için proje SDK 54'e indirildi (`88983ca`; Apple App Store'daki Expo Go en fazla SDK 54 destekliyor). VirtualView/onModeChange hatası gitti.

---

## 2. Tamamlanan fazlar (commit geçmişi, master)
- `284c73d` Initial commit (create-expo-app)
- `14bc0e3` İlk iskelet: tema, 4 sekme, Kart Akışı, SQLite + Leitner SRS
- `17317ad` Fix: web SQLite wasm çözümleme — veri katmanı .web/.native ayrımı
- `9affa4c` Günlük çalışma döngüsü: due-kart kuyruğu (getDailyQueue), UPSERT SRS, Karargah canlı sayılar, Bugünlük bitti
- `ebe279b` Kart Akışı: getDailyQueue akışı + ortalı telefon kolonu (maxWidth 460), ScrollView + pinli buton bandı
- `32af324` Görsel sistemi: 49 TCK görseli temiz şemaya, codegen registry, StudyCard tek-görsel + fallback, m1 seed
- `aecb4f0` Mevzuat ekranı: kanun listesi (Müşterek/Branş + kart sayısı), kanun modu (getCardsByLaw), akış başlığında madde no + ad
- `d95b129` Branş sistemi: branches/law_branches (M2M), AsyncStorage branş kaydı, `PRAGMA user_version` migration runner (SRS korunur, referans veri idempotent), onboarding + branş değiştirme, `getLaws(bransSlug)` + `getBranches()`, Mevzuat branş filtresi; hafıza dosyaları (CLAUDE.md, PROJE_DURUM.md)
- `e256f95` Android EAS build config: eas.json (3 profil), android.package com.mayibey.jsps, expo-dev-client, name/slug düzeltme, EAS projectId
- `87b4d91` docs: JSPS mevzuat listesi PDF (seed kaynağı)
- `51ab4bb` Mevzuat seed: gerçek JSPS listesi (25 müşterek + 41 jandarma = 66 kanun), TCK id1 pinli, law_branches jandarma eşlemesi, MEBS/Mali placeholder kaldırıldı, v2 migration (referans veri re-seed, srs korunur)
- `88983ca` **SDK 56 → SDK 54 tam downgrade**: iPhone Expo Go uyumu (Apple App Store'daki Expo Go en fazla SDK 54). RN 0.81.5, expo 54.0.35, router 6, worklets 0.5.1, reanimated 4.1, typescript 5.9.2. node_modules+lock sıfırdan kuruldu (tek RN 0.81.5, ERESOLVE'suz). 4 şablon dosyada tip uyumu düzeltmesi (external-link Href, collapsible SymbolView, use-theme ColorSchemeName, animated-icon absoluteFillObject) — çekirdek mantık (database/srs/queue/kanun-kartlari/ekranlar) dokunulmadı. Web + iPhone test edildi: VirtualView hatası gitti.
- `f1962a6` docs: SDK sabiti 56 → 54 (CLAUDE.md sürüm pinleri + PROJE_DURUM downgrade notu)
- `947fd1c` **İstatistik Faz A**: `lib/stats.ts` (yeni saf dosya — `hesaplaIstatistik`: çalışılan/öğrenilen/hazırlık%/kutu dağılımı; öğrenildi = kutu≥4), `getCardCount` (4-dosya senkron). Karargah hazırlık % gerçeğe bağlandı, nöbet serisi geçici `—` (streak Faz B'de). Sicil: İLERLEME + KUTU DAĞILIMI kartları (placeholder kalktı). Web/native parite: stats yalnız kutu≥1 (çalışılmış) kart üzerinden. external-link typedRoutes regresyonu (`Extract<…,string>`) düzeltildi. Şema değişmedi (SCHEMA_VERSION=2).
- `2f5b84f` docs: istatistik Faz A tamamlandı notu
- `6ca939a` **İstatistik Faz B (streak)**: `study_days (gun TEXT PK)` tablosu + **migration v3** (tamamen eklemeli — yalnız `CREATE TABLE IF NOT EXISTS`, srs/laws/cards/branches dokunulmaz; v2 telefonlar otomatik v3 alır, ilerleme korunur). `markStudyDay`/`getStudyDays` (4-dosya senkron); `recordReview` her cevapta `bugunISO()` gününü işaretler. `lib/stats.ts`'e saf `hesaplaStreak` + `oncekiGun` (UTC; çapa bugün-veya-dün → geriye kesintisiz say → yoksa 0). Karargah nöbet serisi gerçek (0/yükleniyor → `—`). Web bellek-içi → yenilemede streak sıfırlanır (srs davranışıyla tutarlı). **SCHEMA_VERSION 2→3.**
- `36a90d7` **UX cila turu**: ortak durum bileşenleri `components/ui/` → `<Loading>`, `<EmptyState>`, `<ErrorBoundary>`. Global error boundary (`_layout` Stack'i sarar) + her ekranda `.catch()` ekran-içi "Tekrar dene". Mevzuat yüklemede artık `<Loading>` (eski yanıltıcı "Kanun yok" gitti); gerçekten boşsa "Bu branşta kanun yok". akis: "yakında" kanun → temiz "Yakında" EmptyState (çökme yok, "bitti" demiyor), boş-başlangıç ↔ bitiş ("Bu turu tamamladın") ayrı mesaj, yükleme retry, `cevapla` try/catch (buton kilitlenmez). AudioBar gizlendi (Faz 4'e saklı). `Screen`'e tab-bar `paddingBottom` (son satır görünür). Çekirdek mantık + şema (SCHEMA_VERSION=3) dokunulmadı.
- `1937020` docs: UX cila turu tamamlandı notu
- `c36eb36` **Tatbikat quiz iskeleti**: `lib/quiz.ts` (yeni saf dosya — `quizUret`: tip A "madde→başlık" + tip B "başlık→madde" karışık, çeldirici havuzdaki başka kartlardan, `rastgele` enjekte; `puanla`; `MIN_HAVUZ=4`). Yeni `/quiz?lawId=` rotası (akis ile simetri): state makinesi, şık seçince anlık doğru/yanlış geri bildirim (yeşil/kırmızı), bitince skor `<EmptyState>` (Tekrar/Bitir). Tatbikat sekmesi placeholder → başlatıcı (kanun listesi, kart sayısı ≥4 olanlar tıklanabilir, altı "yetersiz" kilitli). `EmptyState`'e opsiyonel `ikincilButon`. **Kanun-bazlı v1** (mevcut `getCardsByLaw`, yeni DB fn yok). **SRS'e DOKUNULMAZ** (salt ölçüm — recordReview/saveSrs yok). Şema sabit (SCHEMA_VERSION=3). TCK 4 gerçek kartla canlı demo (sahte soru yok).
- `d40fa5d` docs: Tatbikat quiz iskeleti tamamlandı notu
- `7df2b3c` **Akıllı öğrenme Katman 1 (veri toplama)**: `kart_performans (id PK AUTOINCREMENT, card_id, kaynak, sonuc, tarih)` tablosu + **migration v4** (tamamen eklemeli — yalnız `CREATE TABLE IF NOT EXISTS`; srs/study_days/laws/cards/branches dokunulmaz; v3 telefonlar otomatik v4 alır, ilerleme korunur). `kaydetPerformans`/`getPerformans` (4-dosya senkron). **LOG modeli** (her cevap 1 satır, sonuc ham): çalışma cevapları `recordReview` public wrapper'ından (`'calisma'` + biliyorum/tekrar/zor), quiz cevapları `quiz.tsx > sec()`'ten (`'quiz'` + dogru/yanlis, ateşle-unut). **SRS'e DOKUNULMADI** (ayrı katman); `quiz.ts` saf kaldı; `akis.tsx` değişmedi (recordReview üzerinden otomatik). **SCHEMA_VERSION 3→4.** Kullanıcıya görünen UI yok (sadece veri akışı).
- `f60138c` docs: akıllı öğrenme Katman 1 tamamlandı notu
- `0e2ebb0` **Forensic filigran (Faz 1)**: StudyCard'a (akış kartı) kullanıcıya özel çapraz (-30°) tiled watermark — `JSPS • {cihaz-id} • {tarih}`. Kimlik = AsyncStorage `jsps.cihaz` (bir kez üretilen kısa hex, modül-cache; **yeni paket YOK**); `useCihazKimlik` hook. Yeni saf `Watermark` bileşeni (`absoluteFill` + `pointerEvents="none"`, flex merkezleme, opaklık 0.15, ince açık kontur → koyu görselde de okunur); iki kart modunu da kapsar, `overflow:'hidden'` köşeleri kırpar. Tema sabitleri `FiligranOpaklik`/`FiligranAci`. **Çekirdek/şema/SRS dokunulmadı**, DB/4-dosya etkisi yok.
- `b637b85` docs: forensic filigran Faz 1 tamamlandı notu
- `c6b15f8` **Ses altyapısı (Faz 2)**: `expo-audio ~1.1.1` (expo install; app.json'a `expo-audio` config plugin, izin yok; expo-av deprecated → kullanılmadı). Ses registry+codegen (`scripts/ses-registry-uret.mjs` → `src/assets/kart-sesleri.ts`, görsel sistemi analoğu; **0 dosyada boş başlar**, `npm run ses:uret`). `useKartSesi` hook (expo-audio `useAudioPlayer`/`useAudioPlayerStatus` sarmalayıcı): `{ varMi, oynuyor, yukleniyor, calistirDurdur }`, **auto-play YOK**. `AudioBar` placeholder → gerçek hook (sessiz kart → soluk "Sesli anlatım yakında"). `akis.tsx`'te kartın altında `<AudioBar key={kart.id}>` (kart değişince remount → önceki ses durur). **Tek-kart oynat/duraklat**; "Sesli Nöbet" (kuyruk + kilit ekranı) sonraki faz. `ses_yolu` zaten şemada → **şema/4-dosya/çekirdek/study-card dokunulmadı.**
- `c51f678` docs: ses altyapısı Faz 2 tamamlandı notu
- `7161f1e` **Sesli Nöbet v1 (foreground kuyruk)**: `lib/sesli-nobet.ts` (saf — `sesliKartlar` ses filtresi, `sonrakiIndex`/`oncekiIndex`, son kartta dur). `useSesliNobet` hook (tek `useAudioPlayer`; `status.didJustFinish` → otomatik sonraki kart; **auto-start YOK**, oturum aktifken kart değişince devam). Yeni `/sesli-nobet?lawId` rotası: aktif kart (StudyCard, filigran dahil) + önceki/oynat-duraklat/sonraki + "n / m" ilerleme; ses yoksa empty-state; Loading + hata-retry. Mevzuat'ta kanun satırında **kulaklık girişi** (akış navigasyonu bozulmadan ayrı `Pressable`). **`useKartSesi` (tek-kart) DOKUNULMADI** (ayrı hook). **Arka plan/kilit ekranı = v2 (dev build) — app.json'a UIBackgroundModes EKLENMEDİ, yeni paket yok.** Çekirdek/şema/study-card dokunulmadı.
- `d992685` docs: Sesli Nöbet v1 tamamlandı notu
- `d1570ae` **Akıllı sistem KATMAN 2 (analiz + gösterim)**: `lib/performans.ts` (saf — `kotuMu`, `zayifKartlar` [SON-deneme recency: son deneme zor/yanlış ise zayıf, iyi ise toparlandı], `kanunPerformans`, `eksikOzet`). Yeni `getAllCards(): CardWithLaw[]` (4-dosya senkron; **srs JOIN'siz → quiz-only kartların metadata'sını da kapsar**; şema değişmez). Sicil'de KUTU DAĞILIMI altına **"ZAYIF MEVZİLER"**: top-5 zayıf kart (`madde_no — baslik` + "N yanlış") + "En zayıf: {kanun}" + veri yok/temiz durumları; **ayrı state+catch** (İLERLEME'yi bozmaz). **Çekirdek/şema/Katman 1 yazma/study-card dokunulmadı.**
- `8da137b` docs: akıllı sistem Katman 2 tamamlandı notu
- `e4132d0` **4733 m.8 İÇERİĞİ (İÇERİK MARATONU BAŞLADI)**: `assets/kartlar/4733/` 10 panel PNG + `assets/sesler/4733/` ilk ses (m8-6, mp3). `SEED_CARDS`'a 10 kart (id **100-109**, law_id **49**, madde_no '4733 m.8', özet en son); id 105 sesli (`ses_yolu '4733_m8_6'`). 2 codegen (`gorsel:uret`/`ses:uret`) registry'leri üretti. **Migration v5** (eklemeli `seedReference()` re-seed; **DELETE/DROP yok, srs korunur**; v4 telefonlar otomatik alır). **SCHEMA_VERSION 4→5.** **Panel başlıkları GEÇİCİ** ("1. BÖLÜM" vb.) — gerçek başlıklar sonra. İkinci kanun → quiz/istatistik/zayıf mevzi/Sesli Nöbet sistemleri otomatik kapsadı (TCK-özel kod yok). 4733 jandarma branşında görünür.
- `5bd24bc` **Ses sızıntısı fix**: `use-kart-sesi.ts` + `use-sesli-nobet.ts` unmount cleanup (`player.pause()` try/catch, `[player]` dep) — ekran kapanınca/kart değişince ses durur (web HTML5 Audio sızıntısı düzeldi; native ekstra garanti).
- `d754f0e` **Görsel zoom**: kart görseline dokun → tam ekran yakınlaştırma (`@likashefqet/react-native-image-zoom` ^4.3.0, pure-JS; pinch/pan/double-tap). `_layout`'a **`GestureHandlerRootView`**; yeni `gorsel-zoom.tsx` (`absoluteFill` overlay, **Modal değil** → ekran unmount olmaz → **ses kesilmez**); `study-card` görselli modda `<Image>` → `Pressable` (tap→zoom) + overlay. Watermark/placeholder modu korundu. Çekirdek/şema/seed dokunulmadı.
- `62eea37` **Patika Faz 1 (bölüm omurgası)**: `bolumler` + `bolum_kartlari` AYRI tablolar (cards DOKUNULMADI), **migration v6** (eklemeli CREATE+INSERT OR IGNORE, srs korunur), `getBolumler`/`getCardsByBolum` (4-dosya), `lib/patika.ts` (saf `bolumIlerleme`). Mevzuat→**/patika**→bölüm→/akis?bolumId. 4733 demo 2 bölüm, bölümsüz kanun (TCK)→tek "Tüm Kartlar" düğümü→/akis?lawId. quiz/istatistik/zayıf mevzi/Sesli Nöbet (law-bazlı) ETKİLENMEDİ. **SCHEMA_VERSION 5→6.**
- `ba146e1` **Patika Faz 2 (Duolingo zikzak görünüm)**: yalnız `patika.tsx` — düğümler sola-sağa zikzak (`alignSelf` parite), soluk merkez omurga, büyük yuvarlak düğümler (numara + sıraya-döngüsel MaterialCommunityIcons), durum renkleri (yeşil tamam / lacivert başlanmış / soluk başlanmamış), ilk tamamlanmamışa **altın aktif vurgu**, N/M ilerleme (dış border halka; SVG yok). Navigasyon aynen, yeni paket yok.
- `05c8832` **Patika geri butonu**: `Screen`'e opsiyonel `onGeri` prop'u (başlık şeridinde geri oku → `router.back`); patika Mevzuat'a döner. Sekme ekranları + akis/quiz/sesli-nobet etkilenmedi.
- `48fef78` **Geri bildirim sistemi (Formspree, backend'siz)**: akis'te kart altında "Hata/öneri bildir" → yeni `/geri-bildirim` formu (Hata/Öneri/Diğer toggle + çok satırlı mesaj). `src/constants/config.ts > FORMSPREE_ENDPOINT` (placeholder; **boşsa DEMO-success**, doluysa gerçek `fetch` POST). `lib/geri-bildirim.ts` (izole — fetch+endpoint, DB yok). Kart bilgisi (`card_id/madde_no/baslik/kanun`) route param + cihaz kimliği OTOMATİK gömülür. Anlık "Teşekkürler 🙏" + auto-geri; çift-gönderim kilidi; hata→tekrar. **Yeni paket YOK** (fetch built-in), **şema/çekirdek dokunulmadı**.
- `dd569a1` **Madde metni özelliği**: akis'te kart altında "Madde Metni" butonu (sıra: Dinle / Madde Metni / Hata bildir) → yeni `/madde-metni` rotası (`Screen`+`onGeri`, kaydırılabilir tam metin). Metin kaynağı **kod-içi `src/db/madde-metinleri.ts > MADDE_METINLERI: Record<madde_no, metin>`** (BOŞ başlar; **madde bazında** — 4733 m.8'in 10 kartı tek metni paylaşır). `maddeMetni(maddeNo)` saf lookup. **Şema/migration/4-dosya YOK** (kod-içi map → metin eklemek migration gerektirmez). Metin yoksa buton soluk "Madde metni yakında" + ekranda EmptyState. **Yeni paket yok, çekirdek/cards/srs dokunulmadı.**
- **4733 m.8 metni dolduruldu** (`b8b5ca7`): `MADDE_METINLERI['4733 m.8']` = "Cezai hükümler" resmî tam gövde metni (birebir; mülga fıkra işaretleri korundu, YTL/TL orijinal; dipnot/AYM notu gövdede yok). m.8'in 10 kartında buton aktif. **Gelecek bakım:** 8. fıkra 2.-6. cümleler AYM ile **9/9/2026**, m.8/A "iadesi ile" **20/10/2026** yürürlükten kalkacak → metin güncellenmeli.
- `51dff20` **TCK madde metinleri Partİ 2b (5 madde)**: `MADDE_METINLERI`'ne Kamu İdaresine Karşı Suçlar'dan `'TCK m.250/252/255/257/266'` (irtikap, rüşvet [10 fıkra], nüfuz ticareti [7 fıkra], görevi kötüye kullanma, araç/gereç suçta kullanma). Bu kartların "Madde Metni" sheet'i aktif. YALNIZ `madde-metinleri.ts`. **GÖZLEM:** "Parti 2a" dosyası (docs) henüz yok → bu grubun kalan kartları (m.247, 251, 256, 258-262, 264) hâlâ metinsiz ("yakında"). Ayrıca Milli Savunma (317-325) ve m.1/özet/ayırt kartları da metinsiz. Toplam metin: 25 anahtar (4733 m.8 + 24 TCK).
- `5848aa3` **TCK Genel Hükümler madde metinleri (Partİ 1 — 19 madde)**: `MADDE_METINLERI`'ne `'TCK m.2'..'TCK m.45'` (m.2-5, 20-23, 35-45) resmî metinleri eklendi (mevzuat.gov.tr konsolide; m.35 4/6/2025-7550 ile güncel). Bu maddelerin kartlarında "Madde Metni" sheet'i artık AKTİF. **YALNIZ `madde-metinleri.ts`** (salt veri, migration/sürüm bump YOK). '4733 m.8' dokunulmadı. **Kalan TCK metinleri:** Kamu İdaresine Karşı Suçlar (247-266) + Milli Savunma (317-325) ayrı parti; ayrıca m.1/özet/ayırt kartları metinsiz ("yakında").
- `0788b72` **TCK görsel kartları bağlandı (İÇERİK MARATONU — 2. büyük parti)**: `assets/kartlar/tck/` 49 PNG (zaten projede, registry zaten üretik) → `SEED_CARDS`'a **48 yeni kart** (id **200-299** bloğu; m.1/id5 zaten vardı). Maddeler: Genel Hükümler (m.1-5), Ceza Sorumluluğu (m.20-23), Teşebbüs/İştirak/İçtima/Cezalar (m.35-45), Kamu İdaresine Karşı Suçlar (m.247-266), Milli Savunmaya Karşı Suçlar (m.317-325) + 5 özet + 1 ayırt (m.21-22) + Genel Özet. `gorsel_yolu` = registry key (49↔49 birebir). `madde_no` 'TCK m.X'/'TCK m.A-B'/'TCK Genel Özet'; `baslik` = resmî madde başlığı; `anlatim_metni` placeholder (NOT NULL); `ses_yolu` null. **TCK patikası: 5 bölüm** (`SEED_BOLUMLER` id 3-7 + `SEED_BOLUM_KARTLARI` 49 eşleme; özet/ayırt kartları grup sonunda). **Migration v7** (additive `seedReference()`+`seedBolumler()`, INSERT OR IGNORE; mevcut veri/srs/4733 korunur, DELETE yok). **SCHEMA_VERSION 6→7.** Eski görselsiz m.86/106/125 (id 1/2/3) KORUNDU. Web MemoryBackend otomatik alır. **Registry/MADDE_METINLERI/çekirdek DOKUNULMADI**, yeni paket/tablo yok. **YAPILACAK:** gerçek `anlatim_metni` + `MADDE_METINLERI['TCK m.X']` içeriği (görsel ezberini destekleyen metin) — ayrı tur.
- `dbed811` **Madde metni → ekran-içi sheet overlay** (ayrı route push KALDIRILDI): yeni `src/components/card-flow/madde-metni-sheet.tsx` — `gorsel-zoom` pattern'i (absoluteFill, RN Modal DEĞİL → ekran unmount olmaz → **ses kesilmez**). Backdrop `rgba(15,20,30,0.6)` → **arkadaki kart loş görünür**; üstte ~%12 boşluk; kart-kremi sheet (borderTopRadius 20, gölge, drag-handle, sol üst madde_no+başlık, sağ üst X); içeride `ScrollView` tam metin; metin yoksa "yakında". Kapatma: X veya backdrop (sheet'e dokunuş kapatmaz). `akis.tsx`: additive `maddeAcik` state, buton `setMaddeAcik(true)`, sheet `SafeAreaView` kökünde ScrollView+alt blokla KARDEŞ (cevap butonlarını örter → index kaymaz), kart değişince `useEffect`'le kapanır. **`maddeMetni()`/`MADDE_METINLERI`, `/madde-metni` rotası + `madde-metni.tsx` DOKUNULMADI** (derin-link/yedek; buton artık oraya push etmiyor). `_layout`'a rota/presentation EKLENMEDİ, yeni paket yok, queue/index/SRS dokunulmadı. tsc temiz.
- `dd6781d` **PATİKA = JSPS KAPSAMI (madde düğümleri, 52 kanun)**: Patika artık "bölüm bloğu" değil, resmî mevzuat listesindeki (Ek-1) **sorumlu madde** düğümlerini gösteriyor ("Madde 1 … Madde N"). `seed.ts > SEED_KAPSAM` (law_id → madde etiketleri): **'belirli'** kanunlar PDF'den birebir; **'Tamamı'** kanunlar `JSPS MESLEK KONULARI MEHAZLARI\` .doc'undan **antiword** ile çıkarıldı (`tam(N)`=1..N+Ek+Geçici; dosya sonundaki değişiklik/işlenen-kanunlar pipe-tablosu ayıklandı → Ek/Geçici gürültüsü temizlendi). `SEED_BOLUMLER`/`SEED_BOLUM_KARTLARI` artık **kapsamdan ÜRETİLİYOR** (id=`law*1000+sıra`, deterministik); mevcut kartlar `madde_no`'daki `m.<N>` ile düğüme **OTOMATİK** bağlanıyor (kapsam dışı/numarasız kart bağlanmaz, Mevzuat→kanun ile yine erişilir). **Migration v8**: `bolumler`+`bolum_kartlari` (salt referans, srs içermez) **DELETE+yeniden seed**; cards/srs/laws DOKUNULMADI → **kullanıcı ilerlemesi korunur**. **SCHEMA_VERSION 7→8.** `patika.tsx`: düğüm = madde (jenerik ikon kaldırıldı, daire içinde madde no; "Ek 7"/"Geç.2"); kartı olan düğüm dolu/çalışılabilir (yeşil tamam/lacivert), kartsız düğüm soluk **kapsam-iskeleti**; aktif altın halka = ilk çalışılabilir-bitmemiş madde. **TCK 5 tematik bölüm → 48 madde düğümü** oldu (müşterek kapsam; m.86/106/125 kapsam dışı → patikada yok, getCardsByLaw'da var). Web MemoryBackend üretik dizileri otomatik kullanır (parite). **HARİÇ (kaynaksız 'Tamamı' → kapsamsız, tek "Tüm Kartlar" düğümü):** müşterek 2(2803)/6(5326)/9(5816)/11(2893)/15/17/21/22/23/25(6136) + antiword'le AÇILMAYAN yönetmelikler 50(Kimlik Bild.Uyg)/55(Adli Kolluk)/57(Suç Eşyası)/60(Çocuk Tedbir) — gerçek metin/madde sayısı kullanıcıdan gelecek. **NOT:** auto-çıkarılan 'Tamamı' madde sayıları (CMK 344, KTK 185 düğüm vb.) yaklaşık — telefonda gözle teyit + mülga maddeler şimdilik düğüm olarak duruyor; ~340 düğümlü patikalarda ileride sanal liste (virtualization) gerekebilir.

- `25416c0` **Patika kapsamı: 13 eksik 'Tamamı' kanun eklendi (52→65)**: kullanıcı müşterek kaynak `.doc`'larını (`1.2803`…`25.6136`) klasöre ekledi. Çıkarıldı → `SEED_KAPSAM`: müşterek 2(2803 `tam27+ek1-20+geç1-12`)/6(5326 `45+ek1+geç1-3`)/9(5816 `5`)/11(2893 `12`)/15(Resmî Yaz. `39`)/17(Jand.Teşk.Yön `82`)/22(JGK Pers. `64`)/23(Hizmet Esas. `35`, **.docx→unzip XML**)/25(6136 `18+ek1-12+geç1-10`); branş yönetmelik 50/55/57/60 — bunlar `.doc` DEĞİL **HTML(UTF-16-LE)** imiş (`file`→"HTML document") → antiword açamıyordu, **python `utf-16` decode + tag-strip** ile çıkarıldı (Kimlik Bild.Uyg `35+ek1-4+geç1`, Adli Kolluk `15`, Suç Eşyası `32+geç1`, Çocuk Tedbir `31`). 'Tamamı' .doc'larda dosya-sonu değişiklik/işlenen-kanun pipe-tablosu yine ayıklandı (2803'ün Ek1-20/Geç1-12'si gerçek başlık olarak doğrulandı). **Migration v9** (bolumler/bolum_kartlari sıfırla+yeniden seed; srs/cards korunur). **SCHEMA_VERSION 8→9.** **Kalan TEK eksik: id 21 (6284 Uyg Yön)** — kaynak `.doc` bozuk (`file`→"data", ne OLE2 ne zip ne HTML). 65/66 kanun kapsamlı. tsc temiz.

- `b914330` **Patika kapsamı TAMAM — id 21 eklendi (66/66 kanun)**: son eksik 6284 Uygulama Yönetmeliği kullanıcıdan **taranmış PDF** olarak geldi (`231020201320586284KanunYönetmeligi.pdf`). `pdftotext -layout` (taranmış katman seyrekti ama başlıklar çıktı) → yönetmelik **m.1-48** (m.47 yürürlük, m.48 yürütme; OCR "MADDE47/48"yi boşluksuz yazıp regex'ten kaçmıştı, son madde 46 değil 48). Ek/Geçici yok → `21: tam(48)`. **Migration v10** (bolumler re-seed, srs/cards korunur). **SCHEMA_VERSION 9→10.** Artık 66/66 kanun kapsamlı, kapsamsız kanun kalmadı. tsc temiz.

- `b5a9682` **Patika: mülga maddeler düşürüldü (1901→1823 düğüm)**: `tam()`'a `mulga` parametresi eklendi → kaynakta **"Madde N – (Mülga...)"** işaretli ana maddeler artık düğüm OLUŞTURMUYOR (yürürlükten kalkmış maddeler patikada yok). Mülga setleri tüm 'Tamamı' kaynaklarından python ile çıkarıldı (.doc/.docx/HTML/PDF tek script; "(Mülga" başlık kalıbı). Düşenler: 2803[16,17,23,25]·JGK Personel Yön[43,44]·CMK[250,251,252,295]·1774[5,8,13,16]·KTK 2918[11,40,113,117,119,120,127,129]·5607[8,14]·Kimlik Bild.Uyg Yön[11,12,13,18,31]·**KTY[49 madde]**. **Toplam 78 mülga düştü** (müşterek 603→597, branş 1298→1226). **Migration v11** (bolumler re-seed, srs korunur). **SCHEMA_VERSION 10→11.** **Güncel düğüm sayıları: MÜŞTEREK 597 · BRANŞ 1226 · GENEL 1823.** NOT: mülga taraması ana maddeler için; Ek/Geçici mülgaları taranmadı (kaynakta nadir) — gerekirse ileride. tsc temiz.

- `ef2199c` **TCK + Kabahatler görselleri patikaya bağlandı (129 kart)**: kullanıcı yeni görselleri ekledi (tck 59 + Kabahatler 70 PNG), 4733 görsellerini boşalttı. Dosyalar konvansiyon-DIŞI adlandırılmıştı (`5237 MADDE 35-1.png`, klasör `Kabahatler Kanunu`) → python ile **ASCII konvansiyona yeniden adlandırıldı** (`tck_m35_1.png`, klasör→`kabahatler`), 1 Kopya silindi, codegen registry yenilendi (129 anahtar; eski `tck_m*`/`4733_*` düştü → kırık require yok). **`SEED_CARDS` artık registry'den ÜRETİLİYOR** (`gorselKartlari()`): her PNG=1 kart, çok-panel aynı `madde_no` (düğümde sıralı, 4733 m.8 gibi), özet/ayırt **ilk maddenin** düğümüne bağlanır, genel özet (tutar/yetki) bağsız (kanun akışında). Eski resmî TCK başlıkları `TCK_BASLIK` map'inde korundu; eksik maddeler + Kabahatler placeholder başlık. 4733 kartları (id 100-109) placeholder olarak kaldı (görsel gelince registry'ye düşer). **127/129 kart patika düğümüne bağlandı** (doğrulandı). Madde metni (MADDE_METINLERI) madde_no eşleşmesiyle çalışmaya devam. **Migration v12** (cards+bolum_kartlari+bolumler DELETE+yeniden tohumla; **srs KORUNUR** — card_id ayrı tablo). **SCHEMA_VERSION 11→12.** **Uyumsuzluk özeti:** Kabahatler kapsamında 7 madde hâlâ görselsiz (6,10,12,19,31,44,45 → placeholder düğüm); 42A/43A/43B alt-maddeleri base madde düğümüne (m.42/m.43) bağlandı; genel özetler (tutar/yetki) patika düğümsüz. tsc temiz. **YAPILACAK:** yeni 4733 görselleri gelince `assets/kartlar/4733/`'e ASCII isimle koy → `npm run gorsel:uret` → otomatik kart üretilir.

- `862305b` **SİCİL: Ödül/Ceza sistemi (geri-bes ceza merdiveni + başarı belgeleri)** — *Başkan'ın 4 büyük fikrinden 1.+2.si*. Zayıf mevzilerden askeri-jargonlu disiplin + ödül. **CEZA:** ≥3 zayıf mevzi → "Geri Besleme Eğitim Emri" (3 gün pencere); pencerede toparlanırsa **beraat** (kademe sıfır), pencere dolar+hâlâ zayıfsa kademe ilerler ve sicile işlenir: **Yazılı İkaz→Uyarı→Kınama→Aylıktan Kesme** (cap 4). **ÖDÜL:** kanun bitince (o kanunun tüm kartları kutu≥4) **Takdir**; 5 Takdir→**Başarı Belgesi**; 3 Başarı→**Üstün Başarı**. Her kayıt Cüneyt imzalı temsili resmi yazı. **Yeni tablolar:** `sicil_kayitlari` + `geri_bes_durum`, **migration v13** (eklemeli, srs/cards korunur), **SCHEMA_VERSION 12→13**. **Saf:** `lib/sicil.ts` (degerlendirGeriBes+odulDegerlendir+temsili metinler, senaryo testli); **orkestratör** `lib/sicil-servis.ts` (degerlendirSicil — odağa gelince çalışır). **4-dosya senkron:** getSicilKayitlari/ekleSicilKaydi/getGeriBesDurum/setGeriBesDurum. Sicil ekranında **"ÖDÜL-CEZA SİCİLİ"** bölümü + aktif emir kırmızı uyarı kartı + kayıt defteri (tıkla→temsili metin). tsc temiz. **Kararlar (kullanıcı):** ≥3 zayıf/3 gün · 5→Başarı/3→Üstün · beraat-sıfırla. **YAPILACAK:** "geri-bes'e başla" şimdilik /akis (günlük kuyruk) → özel **zayıf-kart kuyruğu (KATMAN 3)** sonraki tur; emir/ceza duyurusu şimdilik uygulama içi (bildirim = faz 3).

- `da7f2c8` **Akıllı öğrenme KATMAN 3: zayıf-kart kuyruğu** — *Başkan rötuşu*. `getZayifKuyruk()` (4-dosya): son denemede zor/yanlış kartları SRS'iyle kuyruk olarak döndürür. akış'a **`/akis?mod=zayif`** = geri-bes oturumu (cevaplar recordReview ile SRS'i güncelleyince kart zayıf havuzdan çıkar → emir kapanır/beraat). Sicil'de "EĞİTİME BAŞLA" + ZAYIF MEVZİLER "Zayıfları çalış" butonları artık bu moda gider. Şema değişmedi. Ödül/Ceza'yı (862305b) gerçek oturuma bağlar.
- `976a02e` **Bildirimler: Sabah İçtiması / Gece Eğitimi / Fırsat Eğitimi** — *Başkan'ın 3. fikri*. `expo-notifications ~0.32.17` (SDK 54; çekirdek pinler korundu). `lib/bildirim.ts`: ayar (AsyncStorage) + `planla()` (izin→cancelAll→Sabah/Gece **DAILY** + Fırsat **TIME_INTERVAL** rastgele). Yeni **/egitim-plani** ekranı (Sicil'den giriş): bildirim aç/kapa, sabah/gece saati, fırsat toggle, **oturum başına kart (5..50)**. akış günlük kuyruğu bu hedefe göre sınırlar (`gunlukSinirli`). `_layout`: bildirime dokununca /akis. **Web/Expo Go'da guard'lı no-op → tam test DEVELOPMENT BUILD ister** (backlog'da). tsc temiz.
- **İÇTİMA ALANI (4. fikir) — PLANLANDI, kod başlamadı** (`docs/ICTIMA_ALANI_PLAN.md`). Sohbet+arkadaş+DM **backend ZORUNLU** (offline app → sosyal ağ); sahte/yarım yapılmadı. Önerilen yığın **Supabase** (Auth+Postgres+Realtime+RLS). **Kullanıcı kararı bekliyor:** backend onayı · üyelik modeli · v1 kapsamı (öneri: Auth+genel sohbet, DM/arkadaş v2) · KVKK/moderasyon metinleri · bütçe. Karar gelince faz faz uygulanır; diğer 3 fikirden bağımsız.

- **İÇTİMA ALANI v1 (4. fikir) — KOD YAZILDI, Supabase kurulumu bekliyor** (`e8962f8` temel + `d1934ae` giriş kapısı + `b05b42f` sohbet). İlk backend'li özellik. **Kararlar (kullanıcı):** v1=Auth+genel sohbet · e-posta/şifre · **giriş app-wide**. **Yığın:** Supabase (@supabase/supabase-js + react-native-url-polyfill; çekirdek pinler korundu). `lib/supabase.ts` (env'den; **anahtar yoksa `supabaseHazir=false` → backend uykuda, uygulama offline çalışır, KİLİTLENME yok**), `lib/auth-context.tsx` (oturum), `lib/auth.ts` (giriş/kayıt/çıkış), `lib/ictima.ts` (mesaj CRUD + realtime). Ekranlar: **/giris** (e-posta/şifre toggle), **5. sekme /(tabs)/ictima** (canlı sohbet, balonlar, uzun-bas sil/raporla, çıkış). `_layout` guard: hazır+oturum yok→/giris, sonra branş→onboarding. **AKTİFLEŞTİRME (kullanıcı):** `docs/SUPABASE_KURULUM.md` → proje aç + SQL çalıştır (profiles/ictima_mesaj/rapor + RLS + realtime + trigger) + `.env`'e URL/anon key. **v2:** DM + arkadaşlık (`docs/ICTIMA_ALANI_PLAN.md`). **NOT:** SRS ilerlemesi şimdilik LOKAL kalıyor (buluta sync ayrı iş); KVKK metinleri canlıdan önce gerekli. tsc temiz.

- **MAĞAZA HAZIRLIĞI başladı** (kararlar: v1 **İçtima'lı**, isim **Mevzu**). Yayına girmeden önceki kod/evrak işleri (build + developer hesabı kullanıcıda):
  - `e433864` **İsim → "Mevzu"** (app.json name + scheme 'mevzu'; slug korundu/EAS bağı; giriş ekranına MEVZU wordmark).
  - `e545222` **Yayın evrakları** (`docs/`): GIZLILIK_POLITIKASI (KVKK, gerçek veri akışı) · KULLANIM_SARTLARI (sınav garantisi reddi + İçtima UGC kuralları) · MAGAZA_LISTELEME (ad/açıklama/anahtar kelime/yaş/veri beyanı) · **YAYIN_HAZIRLIK.md** (tam checklist; bende/sende/bekleme ayrımı).
  - `2861238` **Apple/Google zorunlu maddeler:** **Hesap silme** (/hesap ekranı → `rpc('hesabi_sil')`, cascade) + **Kullanıcı engelleme** (İçtima uzun-bas → Engelle, mesaj gizleme; `engellenenler` tablosu) + **yasal linkler** (giriş onay satırı + /hesap; `config.GIZLILIK_URL/SARTLAR_URL`). SUPABASE_KURULUM.md'ye `hesabi_sil()` + `engellenenler` SQL eklendi. Sicil'e "Hesap" girişi.
  - `d93fc52` **Review-risk:** Karargah "Günün Maddesi" sabit placeholder ("Yer tutucu özet metni") → gerçek günlük karta bağlandı (getAllCards'tan güne göre, tıkla→patika). Reviewer ana ekranda placeholder görmez.
  - `cafed40` **Yasal metinler uygulama içine gömüldü:** `constants/yasal-metin.ts` + **/yasal** ekranı; giriş onay satırı + /hesap linkleri artık URL boşken de çalışıyor (Apple "çalışan gizlilik linki" şartı). Config URL doluysa "web'de aç" linki.
  - Hepsi `supabaseHazir`/`__DEV__` guard'lı; web export temiz (SSR), tsc temiz. **Bağımsız (build/Supabase/içerik gerektirmeyen) iş kuyruğu büyük ölçüde BİTTİ.** **Açık karar:** Sesli Nöbet (0 ses dosyası → hep "yakında") v1'de gizlensin mi? **Parked:** build config (kullanıcı: en son), Supabase kurulumu (en son), içerik maratonu (ayrı). **Sende:** developer hesapları, EAS build, Supabase, legal yer tutucuları doldur+yayınla, ekran görüntüleri, demo hesap. Asıl takvim: hesap onayı + Google 14 gün test + review.

- **İÇERİK MARATONU — 23 MÜŞTEREK KANUN GÖRSELLERİ + TTS (gece otonom, kullanıcı uykuda)** `06015ec` (içerik) + `f2284ad` (TTS).
  - **Kaynak:** `D:\JSPS Fabrika\...\MUSTEREK\{kanun}\uretilen_gorseller\*.png` (sadece üst seviye) + `ses_metinleri\*.txt`. Yerleştirici: **`scripts/icerik-yerlestir.py`** (yeniden çalıştırılabilir; kaynaktan ASCII registry anahtarı + kapsam üretir, ses metnini toplar).
  - **23 kanun eklendi** (~406 kart): law 2-5,7-25 (TCK id1 ve Kabahatler id6 HARİÇ — onlar eski görsellerle zaten bağlıydı, DOKUNULMADI). `KANUN_BILGI` 25 kanun; `gorselKartlari()` registry'den üretir.
  - **WebP:** tüm kart görselleri (eski TCK/Kabahatler dahil) PNG→WebP q82, **1186MB → 117MB**. Codegen png+webp tarar.
  - **Patika trim (kullanıcı isteği):** `SEED_KAPSAM` müşterek kanunlar için `Object.assign` ile **yalnız GÖRSELİ OLAN maddelerle** override → görselsiz maddeler patikadan düştü. **EK/Geçici/harfli madde (42A) + isimli özet (SUC-AYIRT/KUME/MERCI vb.) kartları "genel-özet"** → patika düğümü YOK, **Mevzuat→kanun→Tüm Kartlar (getCardsByLaw)** ile erişilir. Genel-özet sayıları: 2803 Jandarma 11, 6136 Ateşli 4, 5442 İl İdaresi 4 vb.
  - **TTS (Başkan'ın fikri):** `expo-speech` — kayıtlı ses dosyası YOK, `ses_metinleri` metnini **cihaz Türkçe sesiyle okur** (çevrimdışı, ücretsiz). `src/assets/kart-ses-metinleri.ts` (406 metin, anahtar=gorsel_yolu) + `TtsBar` akış kartı altında ("Sesli Anlatım", metni olan kartta görünür). Gizlenen sesli bölüm geri açıldı.
  - **Migration v14** (cards/bolumler DELETE+re-seed; **srs KORUNUR**). SCHEMA_VERSION 14. tsc + web export temiz (535 görsel).
  - **SABAH KONTROL (telefonda):** Mevzuat→müşterek kanunlar artık "N kart" dolu → patika→madde→kart akışı; kart altında "Sesli Anlatım"a bas → anlatım okunur. **KARAR BEKLEYEN:** (1) TCK/Kabahatler yeni kaynak görselleriyle tazelensin mi (şu an eski)? (2) genel-özete düşen EK/Geçici/harfli maddeler için ayrı patika düğümü istenirse `gorselKartlari` çekirdek revizyonu gerekir. (3) 117MB repo → ileride Git LFS/CDN. (4) Tatbikat/quiz artık içerik var → v1'de geri açılsın mı?
- **İSİM → MEVZU-JSPS** (kullanıcı kararı, eski "Mevzu" yerine). `app.json name` + yasal metin (`yasal-metin.ts`) + tüm yayın evrakları (gizlilik/şartlar md + GitHub Pages HTML + mağaza listeleme) marka olarak güncellendi. **"Mevzuat" (sekme adı) ve "mevzuat" (kelime) DOKUNULMADI** — sadece marka. `scheme:'mevzu'` (deep link) + `slug`/`android.package` (EAS/Play kimliği) DEĞİŞMEDİ. Yayın evrakları yer tutucuları da dolduruldu: geliştirici **MEVZU-JSPS** · e-posta **dev.ademyilmaz@gmail.com** · şehir **Osmaniye**. Gizlilik/şartlar GitHub Pages için hazır (`docs/index.html` + `sartlar.html`); `config.ts` URL'leri Pages adresine bağlı (Pages açılınca canlı). tsc 0 hata.
- **MAĞAZA YAYIN-HAZIRLIK DÜZELTME TURU (denetim → A/B/C blokları)** — tam tarama (2 denetçi ajan; tsc+web export temiz) sonrası bulgular `docs/YAYIN_DENETIM_GUVENLIK.md`'ye kalıcı kaydedildi (bir daha baştan tarama yok; hafıza: [[yayin-denetim-guvenlik]]). Yapılan kod düzeltmeleri (her biri ayrı commit, kullanıcı onayıyla): **A1** geri bildirim sahte "gönderildi" → `FORMSPREE_ENDPOINT` boşken giriş gizli (`akis.tsx`). **A2** Tatbikat komple gizlendi → tek "yakında" ekranı (`tatbikat.tsx`; quiz altyapısı kodda park). **A3** Karargah sahte "Mini Tatbikat: 1" kaldırıldı (`index.tsx`). **A4** "resmî kurumla bağlantılı değildir" ibaresi (`yasal-metin.ts > RESMI_BAGLANTI_YOK` + Şartlar md.1 + Sicil altı görünür satır). **A5** `app.json`'a `expo-notifications` plugin (bildirim production build'de çalışır). **B1** `expo-screen-capture ~8.0.9` + `akis.tsx` `usePreventScreenCapture()` (kart akışında ekran görüntüsü/kaydı engeli; web/Expo Go no-op). **C1** ölü rota `madde-metni.tsx` silindi (sheet overlay çalışıyor). tsc 0 hata, web export 20 rota temiz. **Karar (kullanıcı):** Pro üyelik/2-cihaz limiti/korsan koruması = v2 (onaydan sonra, RevenueCat+Play Billing+Play Integrity). **SENDE (kod değil, D bloğu):** gizlilik metnini yayınla→`config.ts` URL · `docs/` yasal/mağaza dökümanlarını İçtima'sız güncelle · maskot ambleminin stilize edilmesi + mağaza açıklamasına ibare · developer hesabı+EAS build+20 test/14 gün+ekran görüntüleri.
- **MAĞAZA v1 SADELEŞTİRME (placeholder gizleme)** — reviewer'a "yarım uygulama" hissi veren boş "yakında" yüzeyleri kaldırıldı (kullanıcı kararı). **(1) Sesli özellik GİZLENDİ (silinmedi):** 0 ses dosyası → her kart altında "Sesli anlatım yakında" + Mevzuat'ta kulaklık girişi vardı. Giriş noktaları kaldırıldı: `akis.tsx` AudioBar (import + kullanım) çıktı, `mevzuat.tsx` kulaklık ikonu + `sesliNobete` çıktı. **Makine durdu KORUNDU** (`sesli-nobet.tsx` rota + `use-sesli-nobet`/`use-kart-sesi`/`sesli-nobet.ts`/`audio-bar.tsx` dosyaları + `_layout` Stack.Screen) → ses dosyaları gelince giriş noktalarını geri koyarak tek hamlede açılır. **(2) Müşterek: yalnız dolu kanunlar** — `getLaws` filtresi `blok==='müşterek' && kartSayisi>0`; içeriği olmayan müşterek kanunlar gizli (artık "yakında" rozetli satır yok). **(3) Branş topluca "yakında"** — tek tek boş branş satırları yerine tek `<BransYakinda>` bilgi kartı ("Branş konuları yakında — hazırlanıyor"). `KanunSatir`/`Bolum`'dan `onSes` + boş-satır (`satirBos`/`rozetBos`) mantığı temizlendi. **İçerik/şema/SRS/seed DOKUNULMADI** — yalnız gösterim katmanı; içerik gelince filtreler otomatik dolu gösterir. tsc 0 hata. **NOT (içerik akışı):** müşterek görseller editör düzeltmeleri bitince güncellenecek → yeni build (veya ileride expo-updates/OTA kurulursa mağaza incelemesiz). Branş içeriği gelince `BransYakinda` yerine tekrar `Bolum baslik="BRANŞ"` konur.
- **İÇTİMA ALANI TAMAMEN KALDIRILDI** (kullanıcı kararı: "o olmayacak, vazgeçtim" — mağaza onayını hızlandırmak için). Uygulama artık **%100 offline**, hesap/giriş YOK. **Silinen 7 dosya:** `lib/ictima.ts` · `lib/auth.ts` · `lib/auth-context.tsx` · `lib/supabase.ts` · `app/(tabs)/ictima.tsx` · `app/giris.tsx` · `app/hesap.tsx`. **Düzenlenen:** `_layout.tsx` (AuthProvider + giriş guard çıktı, sadece branş/onboarding guard kaldı; bildirim listener KORUNDU) · `(tabs)/_layout.tsx` (5→**4 sekme**, İçtima sekmesi gitti) · `(tabs)/sicil.tsx` (Hesap girişi → **"Gizlilik & Kullanım Şartları"** girişi, useAuth kaldırıldı) · `yasal.tsx` (gizlilik↔şartlar geçiş linki) · `constants/yasal-metin.ts` (hesap/sohbet/Supabase/hesap-silme maddeleri çıkarıldı → cihaz-içi veri + geri bildirim odaklı) · `.env.example` silindi · `package.json` (**@supabase/supabase-js + react-native-url-polyfill kaldırıldı**, `npm install` → 9 paket prune, çekirdek pinler değişmedi) · docs `ICTIMA_ALANI_PLAN.md`+`SUPABASE_KURULUM.md` silindi. **DOKUNULMADI (yanlış pozitif — askeri/hukuki terim):** `bildirim.ts` "Sabah İçtiması", `seed.ts`/`madde-metinleri.ts` "Fikri içtima" (TCK m.44). tsc 0 hata, kalan referans yok. **YAPILACAK (mağaza öncesi):** `docs/GIZLILIK_POLITIKASI.md` · `docs/KULLANIM_SARTLARI.md` · `docs/MAGAZA_LISTELEME.md` · `docs/YAYIN_HAZIRLIK.md` hâlâ hesap/sohbet/Supabase'den bahsediyor → barındırılan gizlilik metni yayınlanmadan bunlar da uygulama-içi metne göre güncellenmeli (canonical = `yasal-metin.ts`).
- `6438ac8` **Kart akışı 3 UX düzeltmesi** (kullanıcı bildirdi): **(1) Görsel sığma** — StudyCard `aspectRatio:1`(kare)+contain → kare olmayan görseller küçülüyordu; artık görselin gerçek oranı `onLoad`'dan ölçülüp uygulanıyor (tam dolar). **(2) Patika gir-çık** — madde düğümü tek bölüm açıp bitince geri dönüyordu; yeni `getCardsByBolumChain` (4-dosya) girilen maddeden kanun sonuna kadar TÜM maddeleri sırayla akıtır → tek girişte sürekli ileri çalışma. **(3) Butonlar** — 'Biliyorum/Tekrar/Zor' belirsizdi → **'Bilemedim / Zorlandım / Biliyordum' + "kaç gün sonra"** alt metni (SRS mantığı/`srs.ts` DEĞİŞMEDİ, sadece etiket). tsc + web export temiz.

- **LOGLANMAMIŞ 6 COMMIT (DURUM.md `6438ac8`'de kalmıştı):** `06f819a` TCK görselleri yeni kaynaktan tazelendi (eski 59 → yeni 54 kart) · `5842490` ekran-görüntüsü engeli web'de çöküyordu, Platform guard · `feeb6d4` 4733 m.8 placeholder kartları kaldırıldı (görselsiz fallback) · `4dc66e3` çalışma akışı UX (2 buton + anlatım-bitti "sıradakine geç" mesajı) · `feb8cef` Kabahatler görselleri yeni kaynaktan tazelendi (+ TTS sesi). Ayrıca masaya gelen ama bağlanmamış **karakter sistemi (14 meslek PNG + envanter docx)** kullanıcı kararıyla **silindi** ("o karakterlerle işimiz kalmadı"; untracked'di, kodda bağlı değildi).

- **MADDE METNİ İÇERİĞİ — MASTER md'lerinden otomatik dolduruldu (`madde:uret` codegen).** Kullanıcı bildirdi: kart altındaki "Madde Metni" butonu çoğu kanunda pasifti (sebep: `MADDE_METINLERI`'nde yalnız ~24 TCK + 4733 vardı; 23 müşterek + Kabahatler'de hiç metin yoktu → kod doğru, içerik eksik). **Kaynak (kullanıcı ipucu): `D:\JSPS Fabrika\...\KANUN_MASTER_DOSYALARI\MUSTEREK\*/​*MASTER*.md`** içindeki `📜 Metin/Birebir metin/Kanun Metni` blokları. Yeni **`scripts/madde-metni-uret.mjs`** (görsel/ses registry analoğu, repo-DIŞI mutlak yol okur): müşterek MASTER md'lerini parse eder → `src/assets/kart-madde-metinleri.ts` (`KART_MADDE_METINLERI`, anahtar `"<etiket> m.<no>"` = kart.madde_no ile birebir). Format kanundan kanuna değişiyordu → parser 3 yapıyı kapsar: (a) müşterek `📜 **Metin:** "(1) …"` satır-içi tırnak, (b) Kabahatler/branş `**📜 Kanun Metni:**` + `>` blok-alıntı, (c) **Jandarma v2** (ID yok, madde no metinde `"Madde N –"`, bir blok m.4+5+6 birleşik → `"Madde N –"` sınırlarında bölünür). Editör notu filtresi (`✅ teyit/_txt/sınavda` → atla); 📜 yalnız satır-başı işaret sayılır (cümle-içi `📜 alıntılar` tetiklemez). **`maddeMetni()`** önce el-yazımı `MADDE_METINLERI` (TCK/4733 birebir, öncelikli), sonra üretilen registry'ye bakar → eski yüksek-kalite metinler korunur. **Sonuç: 325 madde üretildi; 272/346 kart-maddesi (%79) artık aktif** (önceden ~24). TCK 48/48, Kabahatler 38/38, İl İdaresi 12/12, Tebligat 5/5, Atatürk 3/3 **TAM**. **Şema/migration/SRS/seed/görsel DOKUNULMADI** (kod-içi map, sürüm bump yok). `package.json`'a `madde:uret`. tsc 0 hata. **KALAN BOŞLUKLAR (%21, içerik tarafı — parser değil):** (1) master'da o maddenin 📜 metni yok; (2) **görsel madde-numarası ≠ master numarası** — örn. 6136 Ateşli görselleri m.6/7/9/14 ama master m.4/5/12/13/15 (master "madde 20 yok" uyarısı veriyor), Bilgi Edinme benzer → 0/4. Bu görsellerin yeniden numaralanması ayrı iş. (3) bazı master metinleri kendi içinde `(...)` ile kısaltılmış (master'ın tercihi, kabul edildi). **Yenileme:** MASTER md güncellenince `npm run madde:uret`.

- **Kart akışı: ileri/geri gezinme okları** (kullanıcı bildirdi: "kartların sağında ileri, solunda geri buton yok"). `akis.tsx`'te StudyCard'ı saran `kartSar` wrapper + dikey-ortada sol/sağ kenarda yuvarlak chevron butonları (lacivert daire, beyaz ok). **Saf görünüm gezinmesi:** yalnız `index` değiştirir (`setIndex max(0,i-1)` / `min(len-1,i+1)`), **SRS/recordReview'a DOKUNMAZ** — cevap butonları (Biliyorum/Tekrar) SRS'in asıl aksiyonu olarak kalır. İlk kartta sol, son kartta sağ **pasif** (opacity 0.25, disabled). Geri gidince `index` effect'i anlatım-bitti + madde sheet'i sıfırlar. Görsel-zoom tap'i korunur (oklar kenarda, zIndex 2). tsc 0 hata.

- **ÜYELİK ALTYAPISI v1 — Gmail (Google OAuth) ile giriş, OPSİYONEL** (kullanıcı kararı: "üyelik kuralım, Gmail ile kayıt"; amaç ileride satın alma). İçtima'yla kaldırılan auth'un yerine, **bu sefer sosyal alan YOK, sadece kimlik/giriş**. **Kararlar:** backend **Supabase** · giriş **opsiyonel** (misafir çalışır, offline felsefesi korunur, mağaza riski yok) · yöntem **OAuth-web** (`expo-auth-session`+`expo-web-browser` → tarayıcı → `mevzu://` dönüş; **native modül yok → Expo Go'da test edilebilir**, ileride dev build'de native butona yükseltilebilir). **Paketler** (`expo install`, çekirdek pinler korundu): `@supabase/supabase-js` `react-native-url-polyfill` `expo-auth-session` `expo-web-browser` (+ app.json'a expo-web-browser plugin). **Yeni dosyalar:** `lib/supabase.ts` (GUARDED — `SUPABASE_URL/ANON_KEY` boşsa `supabaseHazir=false`, client null → **uygulama %100 offline çalışır, çökmez**; PKCE + AsyncStorage oturum), `lib/auth.ts` (`gmailIleGiris`/`cikisYap`; OAuth URL→WebBrowser→`exchangeCodeForSession`), `lib/auth-context.tsx` (`AuthProvider`/`useAuth`: kullanıcı/oturum, `onAuthStateChange`; BransProvider deseni), `app/giris.tsx` (giriş ekranı: yapılandırılmamış→"yakında" · oturum yok→Gmail butonu+yasal onay · oturum var→e-posta+çıkış · "girişsiz devam et"). **Düzenlenen:** `_layout.tsx` (AuthProvider sarmalar, `giris` modal rota; **zorlama guard YOK**), `(tabs)/sicil.tsx` (Hesap girişi → /giris; e-posta/giriş/yakında durumu), `constants/config.ts` (`SUPABASE_URL`/`SUPABASE_ANON_KEY`, env `EXPO_PUBLIC_*` veya sabit). **`.env` gitignore'da** (anahtar sızmaz). **Kurulum:** `docs/UYELIK_KURULUM.md` (Supabase projesi + Google Cloud OAuth client + provider + redirect `mevzu://` + anahtar girişi). tsc 0 hata. **SENDE (provisioning):** Supabase projesi aç + Google OAuth client + anahtarları gir → buton aktifleşir. **SONRAKİ FAZLAR (ayrı):** (1) SRS bulut senkronu (Supabase tablo+RLS — şu an giriş yalnız kimlik, veri lokal kalır) · (2) **satın alma/freemium** (RevenueCat + Play/App Store billing + gerçek build) · (3) gizlilik/KVKK metni hesap verisini yansıtacak şekilde güncelle (canlı öncesi).

- **Sesli anlatım (TTS) ileri/geri sarma** (kullanıcı bildirdi: "bir yeri kaçırınca ileri-geri alamıyor"). **Teknik gerçek:** `expo-speech` TTS motorudur (ses dosyası değil) → saniye bazında seek YOK. **Çözüm:** `tts-bar.tsx`'te metin CÜMLELERE bölünür (`cumlelereBol` — `.!?`'da keser, <15 krk kısa parçayı öncekine ekler; 926 krk metin → ~10 temiz cümle test edildi), cümle cümle okunur, **◀ önceki / ▶ sonraki cümle** butonları + "n/m" ilerleme. Kaçırılan cümle ◀ ile tekrar dinlenir; Durdur→Oynat aynı cümleden devam. **Nesil (generation) token'ı** ile seek/stop sırasında eski okumanın geç gelen `onDone`'u zincirlemez (çakışma yok). `onBitti` (son cümle bitince "sıradakine geç" mesajı) KORUNDU. Auto-play yok; kart değişince remount→durur. tsc 0 hata.

- **Üyelik web SSR çökmesi fix** (kullanıcı `expo start`'ta gördü: `ReferenceError: window is not defined` → dev sunucu komple ölüyordu). Sebep: Expo Router web'i Node'da ön-render ediyor; orada `window` yok ama Supabase auth, AsyncStorage'ın web sürümünden oturum okumaya çalışıyor. **Fix `lib/supabase.ts`:** `ssrOrtami = Platform.OS==='web' && typeof window==='undefined'` → SSR'de **no-op storage** + `persistSession/autoRefreshToken=false`; native ve gerçek tarayıcıda AsyncStorage (kalıcı oturum) korunur. **`lib/auth.ts`:** `maybeCompleteAuthSession()` SSR'de atlanır, `makeRedirectUri` modül-yükünden çıkıp `gmailIleGiris` içine (lazy, hep istemci tarafı) alındı. **Doğrulama: web export EXIT 0, "window is not defined" 0 kez** (tüm rotalar prerender). tsc 0 hata. Anahtarlar `.env`'e girildi (Supabase URL JWT'den türetildi: vwmjrvolkbiofpkzzwef).

- **ÜYELİK AKTİF — Gmail girişi ÇALIŞIYOR** ✅ (Faz A tamam). Kullanıcı Supabase projesi (ref `vwmjrvolkbiofpkzzwef`) + Google Cloud OAuth client (Web) kurdu, anahtarlar `.env`'de. Telefonda (Expo Go) Gmail girişi başarılı, e-posta Sicil'de görünüyor. **Çözülen zincir:** (1) **web SSR çökmesi** (`window is not defined`) — supabase.ts SSR-guard'lı storage. (2) **Safari localhost** — Supabase `exp://` redirect'ini eşleştiremeyip varsayılan **Site URL (localhost:3000)**'e düşüyordu → **Site URL = `exp://<IP>:8081`** yapıldı (Redirect URLs + Site URL ikisi de exp). (3) **"belirteç yok" bug'ı** — dönüş URL'i `exp://...?code=...#` (code query'de + sonda boş `#`); `paramAyikla` "`#` varsa fragment'e bak" diyerek code'u kaçırıyordu → **query VE fragment'i toplayacak** şekilde düzeltildi (4 format test edildi). Giriş ekranında **TEŞHİS kutusu** (`__DEV__`) redirect adresini gösterir (IP değişince Supabase'i güncellemek için). **Expo Go sınırı:** `exp://<IP>:8081` IP'ye bağlı → kalıcı çözüm gerçek build (`mevzu://`, Faz C). `docs/UYELIK_KURULUM.md` gerçek adımlarla (Site URL dahil) güncellendi. tsc 0 hata. **Sonraki:** SRS bulut senkronu · satın alma (RevenueCat) · KVKK metni.

- **YAYIN ÖNCESİ DENETİM + ÜYELİK v1'DE KAPATILDI** (kullanıcı "build'den önce sınıfta kalır mıyız" dedi → kapsamlı ajan denetimi). **3 BLOCKER bulundu, hepsi Gmail girişinden:** (Ü1) gizlilik politikası "hesap yok, e-posta toplamıyoruz, sunucuya veri gitmez" diyor ama Gmail e-postayı Supabase'e yazıyordu → yanıltıcı davranış red; (Ü2) hesap silme yok (Google zorunlu); (Ü3) Data Safety uyumsuz. **ÇÖZÜM:** `config.ts > UYELIK_AKTIF=false` (derleme-zamanı ana şalter) → `supabase.ts supabaseHazir = UYELIK_AKTIF && ...` → **client hiç oluşmaz, Supabase'e bağlanılmaz** → uygulama gerçek %100 offline → mevcut gizlilik metni DOĞRU → 3 blocker da gitti. `sicil.tsx` Hesap kartı gizlendi (`hazir`), `/giris` ulaşılamaz (kod+`.env` duruyor, v2'de bayrak açılır + gizlilik/hesap-silme/Data Safety eklenir → yeni build = yeniden inceleme, meşru faz). **Küçük:** `error-boundary.tsx console.error` `__DEV__` guard'landı. Detay kanonik: `docs/YAYIN_DENETIM_GUVENLIK.md` §6 [[yayin-denetim-guvenlik]]. Önceki denetimin (15 Haz, A1-A5/B1) tüm düzeltmeleri hâlâ yürürlükte (ajan doğruladı). tsc 0 hata. **Kalan v1 riski (düşük):** "yakında" yüzeyleri (Tatbikat sekmesi) + gizlilik URL'ini canlıya alma (SENDE).

- **KRİTİK SEED BUG FIX — Mevzuat'ta 25 yerine yalnız 4 kanun görünüyordu** (kullanıcı telefonda fark etti: sadece TCK/Jandarma/Kabahatler/Disiplin). **Kök sebep:** `seed.ts gorselKartlari` id üretimi `(lawId===1?100000:110000) + kanun-başına-sayaç` → TCK dışı TÜM müşterek kanunlar aynı id'lere (110001, 110002…) biniyordu; native `INSERT OR IGNORE`'da çakışan kart düşüyor, yalnız "sayaç-maksimumunu büyüten" kanunlar (Jandarma 22 → Kabahatler 62 → Disiplin 114) + TCK hayatta kalıyordu = **tam 4 kanun**. **WEB'de görünmüyordu** çünkü web backend kartı diziden sayıyor (`SEED_CARDS.filter`), DB INSERT yok → çakışma yok → web 25 gösteriyordu (geliştirme web'de yürüdüğü için kimse fark etmemiş). **FIX:** id = `law*1000 + sıra` (her kanun kendi 1000'lik bloğu; kanun başına <1000 kart). Simülasyon: eski=4 kanun, yeni=25 kanun. **Migration v18** (cards/bolumler DELETE+yeniden tohumla; **srs eski id'lere bağlıydı → orphan olur, yayın öncesi kabul** — yeni id'lerle kartlar "yeni" sayılır, ilerleme sıfırlanır). **SCHEMA_VERSION 17→18.** tsc + web export temiz. Telefonda reload → migration v18 → 25 kanun görünür.

- **🔴→✅ BUILD ÇÖKMESİ ÇÖZÜLDÜ (kritik) — `expo-asset@56` SDK uyumsuzluğu.** İlk EAS preview/production build'leri Android'de **açılışta anında çöküyordu** (Expo Go'da sorunsuz). Belirti: native modül kayıt aşamasında, React/Sentry başlamadan → Sentry bile yakalayamadı, ADB de LG G8'de açılmadığı için logcat alınamadı. **Arkadaşın Xiaomi telefonunda (adb açan) logcat alındı → kök sebep:** `java.lang.NoClassDefFoundError: expo.modules.kotlin.types.AnyTypeCache` (`expo.modules.asset.AssetModule` yüklenirken). Sebep: **`expo-audio@1.1.1` transitif olarak `expo-asset@56.0.15` (SDK 56!) çekip üst seviyeye hoist ediyordu**; SDK 54 `expo-modules-core@3.x`'te `AnyTypeCache` YOK → ClassNotFound → çökme. `expo@54` doğru `expo-asset@12.0.13`'ü istiyordu ama hoist edilen 56'ydı. **FIX:** `package.json > overrides: { "expo-asset": "12.0.13" }` → her yerde 12.0.13. Doğrulandı: fixed APK arkadaşın telefonuna adb ile kuruldu, açıldı, MainActivity ön planda, 0 FATAL, kullanıcı kaydırıyor = ÇALIŞIYOR. **DERS:** CLAUDE.md "SDK 56'ya çıkma" pinine ek olarak transitif sürümleri de kontrol et (`npm ls expo-asset`); paket kurup kaldırınca expo paketleri kayabilir. [[expo-asset-surum-pini]]
- **Çökme teşhis altyapısı (kalıcı):** Sentry (`@sentry/react-native`, `_layout` init/wrap, DSN config.ts, `SENTRY_DISABLE_AUTO_UPLOAD=true` eas.json) — bu çökme için çok erkendi ama gelecekteki JS/native çökmeler için kalıcı panel. **expo-notifications KALDIRILDI** (çökme şüphesiyle; Firebase erken-init değildi ama v1'de zaten gereksizdi → bildirim.ts no-op, _layout dinleyici yok, plugin+paket çıktı; v2'de gerçek Firebase ile döner).

- **KANUN İÇİ ARAMA özelliği eklendi (18 Haz) — yeni "Ara" sekmesi.** Anahtar kelime → kelimenin geçtiği kanun/maddeleri bulur. **Saf, istemci-tarafı** (yeni DB/migration/4-dosya YOK): `getAllCards` + `maddeMetni` üzerinde arar. **Yeni `lib/ara.ts`** (saf): `araIndeksHazirla` (law_id+madde_no bazında tekil, tr-küçük-harf önişlem — kartlar yüklenince bir kez) + `araKanunlar` (her tuşta hızlı indexOf filtre; başlık eşleşmesi ağırlıklı skor; eşleşme civarı snippet). **Türkçe-duyarlı** (`toLocaleLowerCase('tr-TR')` → İ/ı doğru). **Yeni `(tabs)/ara.tsx`**: arama kutusu + FlatList sonuçlar (kanun · madde no — başlık · vurgulu snippet); **modül-cache `sonSorgu` → karta gidip dönünce son arama hatırlanır** (kullanıcı isteği: 3-4 sonuçtan ilkini beğenmezse geri dönüp diğerine girebilsin). Sekme: Karargah · Mevzuat · **Ara** · Sicil. **`akis.tsx`'e `kart` parametresi** (arama sonucu → `/akis?lawId&kart` → eşleşen karttan başlar; `findIndex` ile index). **Sınır:** resmî metin kapsamı %79 → metinsiz maddeler gövdede bulunmaz, başlıkta bulunur (Tier 4 içerik ilerledikçe arama zenginleşir). tsc 0 hata, web export temiz (/ara bundle'landı). **SRS/şema/seed/çekirdek DOKUNULMADI.**

- **TAM DENETİM TURU yapıldı (18 Haz, 5 paralel ajan, salt-okuma).** Sonuç: **kesin BLOCKER yok.** Temiz: 4-dosya senkron tam · migration eklemeli/srs korunuyor · id üretimi güvenli · kaynak-linki kapsamı tam · İçtima/Cüneyt/Sentry kalıntısı yok · gizlilik runtime'la tutarlı · tsc 0. **Punch-list (sıra):** **Tier 1 dürüstlük/dead-end** (kod-only): (1) Bildirim no-op ama Eğitim Planı "kaydet" diyor (`egitim-plani.tsx`+`bildirim.ts:62`) → "yakında"/gizle; (2) **branş seçimi Mevzuat'ı hiç değiştirmiyor** (`mevzuat.tsx:34`, 41 branş kanunu 0 kart) → dürüst "BRANŞ yakında"/vaadi yumuşat; (3) metinsiz kartta gri "madde metni yakında" kutusu → gizle; (4) patika kartsız düğüm tıklanabilir (`patika.tsx:138`) → soluk+pasif. **Tier 2 perf:** patika N+1 + her bölümde tüm srs taranıyor (`patika.tsx:40`+`database.native.ts:314`) → tek sorgu; 340 düğüm → FlatList. **Tier 3 temizlik:** 11 ölü Expo-template dosyası (themed-text/view, collapsible, use-theme, external-link, animated-icon…) sil · `getZayifKuyruk` srsMap tip hizalama · `YAYIN_HAZIRLIK.md` "sesli gizli" notu bayat (TTS canlı). **Tier 4 içerik (kullanıcı materyali):** 41 branş kanunu 0 içerik · 6136/Bilgi Edinme görsel madde-no ≠ master no (0/4 metin) · 74 metinsiz madde.

- **SAHA İYİLEŞTİRME TURU (18 Haz, kapalı test penceresi) — iş planı `docs/IS_PLANI.md` (14 madde).** Kullanıcı saha tespitleri + yeni istekler tek dosyada (kümeler A-F). Biten kod-only işler (her biri ayrı commit, IS_PLANI'de ✅):
  - `3b62987` **Kanun içi ARAMA** — yeni "Ara" sekmesi; `lib/ara.ts` (saf, tr-duyarlı, snippet) + `getAllCards`/`maddeMetni` üzerinde; sonuç→`/akis?lawId&kart`. Yeni DB/migration YOK.
  - `fc7a16e` **#4 kart görsel boyut tavanı** (`maxHeight=min(ekran×0.5,460)`) — TtsBar/Madde Metni hep görünür.
  - `b6fac5e` **#3 swipe** ileri/geri (Gesture.Pan, yatay-only, runOnJS; SRS'e dokunmaz).
  - `215de47` **#11 sekme "Sicil"→"Evsaf"** (etiket; rota/DB aynı).
  - `682ec25`+`b91f58c`+`ea73fae` **#9 RÜTBE** (4 resmî rütbe Sb/Asb/Uzm.J/Uzm.Erb): `rutbe-store/context/secici`, onboarding 2-adım (branş→rütbe), guard, `/rutbe-sec`. Filtre `lib/rutbe-kapsam.ts > rutbeGorur` Mevzuat'ta. **Müşterek matris KİLİTLİ** (Ek-1 `jsps konuları.pdf`'ten `pdftotext -table -enc UTF-8` ile okundu; istisna = law13 4678 + law16 Sözleşmeli S/A Yön → uzmanlarda yok; gerisi 4 rütbe ortak). **Branş rütbe matrisi branş içeriği gelince (ayrı tur).**
  - `b91f58c` **#10 AYARLAR ekranı** — Branş·Rütbe·(Hesap)·Eğitim Planı·Gizlilik girişleri tek `/ayarlar`'da; Evsaf sadeleşti (istatistik+ödül-ceza).
  - `7245d97` **#8 Karargah iyileştirme** — selamlama+branş/rütbe rozet, günlük hedef çubuğu, zayıf mevzi kısayolu, hazırlık çubuğu + nöbet 🔥.
  - **TAM DENETİM (5 ajan, salt-okuma):** BLOCKER yok; punch-list Tier 1-4 (bildirim no-op, branş filtresi vaadi, ölü kod 11 dosya, patika N+1 perf, içerik boşlukları) — `docs/IS_PLANI.md` + aşağıda. **KALAN (kullanıcı materyali/karar):** B logo/ikon (#5,#12) · C içerik (#6,#7) · A backend/üyelik+kişiye-özel filigran (#1,#2,#13 — politika ağır) · F tatbikat (#14) · branş rütbe matrisi.

## 3. Devam eden iş
- **İstatistikler BİTTİ (Faz A `947fd1c` + Faz B `6ca939a`).** Karargah (hazırlık % + nöbet serisi) ve Sicil (ilerleme + kutu dağılımı) metriklerinin tümü gerçek SRS verisine bağlı. DB şema sürümü **3** (study_days dahil).
- **UX cila turu BİTTİ (`36a90d7`).** Ortak durum bileşenleri (`<Loading>`/`<EmptyState>`/`<ErrorBoundary>`) eklendi; tüm ekranlar tutarlı yükleme/boş/hata/bitiş durumu gösteriyor; "yakında" kanun çökmüyor; global + ekran-bazlı hata yakalama (retry) var; AudioBar gizli (Faz 4).
- **Tatbikat quiz İSKELETİ BİTTİ (`c36eb36`).** Kanun-bazlı çoktan seçmeli quiz: A+B karışık soru tipi, otomatik çeldirici, skor. SRS'ten bağımsız (salt ölçüm). İçerik gelince anlamlanır (şu an yalnız TCK ≥4 kartla oynanabilir). **Kapsam dışı (sonraki):** skor/yanlış geçmişi kaydı (kalıcılık), "karışık/tüm kanunlar" quiz (`getAllCards` + 4-dosya), görsel/anlatım-tabanlı soru tipleri.
- **Akıllı öğrenme KATMAN 1 (veri toplama) BİTTİ (`7df2b3c`).** `kart_performans` tablosu + migration v4; çalışma + quiz cevapları LOG'lanıyor. SRS'ten bağımsız. DB şema **4**.
- **Akıllı öğrenme KATMAN 2 (analiz + gösterim) BİTTİ (`d1570ae`).** `lib/performans.ts` (zayıf kart = son-deneme recency) + `getAllCards` + Sicil "ZAYIF MEVZİLER" (top-5 + en zayıf kanun). **Sırada KATMAN 3: "eksikleri çalış" aksiyonu** — zayıf kartlardan (`zayifKartlar`) bir çalışma kuyruğu üretip `/akis`'e besleme (yeni akış girişi/parametresi; örn. Sicil/Karargah'ta "Zayıfları çalış" butonu → `/akis?mod=zayif`). SRS'e bu noktada dokunulup dokunulmayacağı karar verilecek (öneri: normal akış gibi recordReview ile).
- **Forensic filigran FAZ 1 BİTTİ (`0e2ebb0`).** Akış kartlarına kullanıcıya özel çapraz tiled watermark (caydırıcı + sızıntı atıfı). Kimlik şimdilik AsyncStorage cihaz-ID'si (kurulum başına; reinstall'da değişir). **Üyelik gelince** ID kaynağı gerçek user ID'ye bağlanacak (Watermark bileşeni aynı kalır). Opaklık (0.15) ileride telefonda göz kararı ince ayar yapılabilir.
- **Ses altyapısı FAZ 2 BİTTİ (`c6b15f8`).** expo-audio ile tek-kart "Dinle" (oynat/duraklat, auto-play yok); ses registry+codegen boş hazır; sessiz kartlar "Sesli anlatım yakında". AudioBar akışta görünür.
- **Sesli Nöbet v1 (foreground) BİTTİ (`7161f1e`).** Kanun-bazlı kuyruk: `/sesli-nobet` oynatıcı, didJustFinish auto-advance, önceki/oynat-duraklat/sonraki, n/m ilerleme; Mevzuat'ta kulaklık girişi; ses yoksa empty-state. **Expo Go'da test edilir.** **Kalan v2 (DEV BUILD ŞART):** arka plan oynatma (`setAudioModeAsync shouldPlayInBackground`) + kilit ekranı (`setActiveForLockScreen`+metadata) + config plugin `enableBackgroundPlayback` + app.json `UIBackgroundModes` + global player context. **Gerçek oynatma/auto-advance ≥2 ses dosyası gelince görülür** (şu an 0 dosya → her kanun empty-state).
- **İÇERİK MARATONU BAŞLADI (`e4132d0`).** İlk ikinci-kanun içeriği: **4733 m.8** (10 panel görseli = 10 kart + ilk ses dosyası). DB şema **5** (migration v5 re-seed). **Panel başlıkları GEÇİCİ** ("1. BÖLÜM" vb.) — kullanıcı gerçek başlıkları verecek (madde_no zaten '4733 m.8'). **Akış:** kullanıcı görselleri `assets/kartlar/{kanun}/{kanun}_m{no}.png` + sesi `assets/sesler/{kanun}/` ASCII isimle koyar → `npm run gorsel:uret`+`ses:uret` → `SEED_CARDS`'a kartlar → migration vN+1 re-seed. **Sırada:** 4733 gerçek başlıkları + diğer maddeler, sonra diğer kanunlar (TCK görselleri dahil — şu an TCK kartları görselsiz placeholder). İçerik arttıkça istatistik/quiz/performans/Sesli Nöbet hep gerçek dolar.
- **Ses sızıntısı düzeldi (`5bd24bc`)** + **Görsel zoom eklendi (`d754f0e`)** — kart görseline dokun → tam ekran pinch/pan/zoom (ses kesilmeden). Bilgi yoğun panelleri telefonda okumayı sağlar.
- **PATİKA Faz 1+2 BİTTİ (`62eea37` + `ba146e1`); Faz 3 = MADDE KAPSAMI BİTTİ (`dd6781d`).** Bölüm sistemi (`bolumler`/`bolum_kartlari`, Duolingo zikzak) artık **JSPS resmî kapsamı = madde düğümleri** olarak kullanılıyor (52 kanın madde haritası `SEED_KAPSAM`, migration v8, DB şema **8**). Mevzuat→kanun→/patika→**madde**→/akis?bolumId. **Sırada/karar gerektiren:**
  - **TÜM kapsam TAMAM — 66/66 kanun (`b914330`, v10):** `25416c0` ile 13 eksik kanun (9 müşterek 'Tamamı' + 4 HTML/UTF-16 branş yönetmelik python ile) eklendi; **`b914330`** ile son eksik **id 21 (6284 Uyg Yön)** de eklendi — kullanıcı taranmış PDF verdi, `pdftotext -layout` ile çıkarıldı (`tam(48)`; m.47/48 yürürlük-yürütme, OCR boşluksuz yazmıştı). **Artık kapsamsız kanun yok.**
  - **'Tamamı' auto-sayıların teyidi:** CMK/KTK/Çocuk Koruma vb. madde listeleri antiword'den; mülga maddeler düğüm olarak duruyor, telefonda gözle kontrol.
  - **TCK çift kapsam:** şu an müşterek liste (48 madde). Branş TCK kapsamı (Genel+Özel Hükümler aralıkları) daha geniş — tek law id 1'de hangisi? karar.
  - **Çok düğümlü patika (~340) performansı:** ScrollView → sanal liste (FlatList/virtualization) gerekebilir.
  - **Opsiyonel:** sıralı kilit (önceki madde bitince açılır) + SVG ilerleme halkası (`react-native-svg` — karar).
- **GERİ BİLDİRİM sistemi eklendi (`48fef78`).** Kart altında "Hata/öneri bildir" → form → Formspree (backend yok). Şu an **DEMO modu** (config'de endpoint boş → gerçek mail gitmez, "Teşekkürler" gösterir). **YAPILACAK:** kullanıcı Formspree'de form oluşturup URL'ini `src/constants/config.ts > FORMSPREE_ENDPOINT`'e yazınca gerçek mail düşmeye başlar (kod değişmez). **Gerçek cevap/2-yönlü bildirim üyelikle gelecek** (şimdilik tek yön: kullanıcı → mail).
- **MADDE METNİ özelliği eklendi (`dd569a1`).** Kart altında "Madde Metni" → resmî tam metin (offline). Metin kaynağı `src/db/madde-metinleri.ts` (kod-içi map). **YAPILACAK:** kullanıcı bu dosyaya `'madde_no': 'metin'` ekleyince (örn. `'4733 m.8': '...'`) o maddenin kartlarında buton aktifleşir. **Migration/seed/sürüm bump GEREKMEZ** (kod-içi; her açılışta taze). İçerik maratonunun parçası: görsel + ses + **madde metni** üçlüsü her madde için doldurulacak.
- **İLK MADDE METNİ DOLDURULDU — 4733 m.8 "Cezai hükümler".** Kullanıcı resmî PDF'i (mevzuat.gov.tr konsolide, 7491/2023 + 2025 AYM iptalleri dahil) verdi → `MADDE_METINLERI['4733 m.8']` **birebir resmî gövde metniyle** dolduruldu (kullanıcı kararı: tam metin birebir + YTL/TL karışık orijinal haliyle; mülga fıkra işaretleri korundu; **dipnotlar ve AYM iptal notları gövdeye DAHİL EDİLMEDİ**). Artık m.8'in 10 kartında (id 100-109) "Madde Metni" butonu **aktif**. `npx tsc --noEmit` temiz. **NOT (gelecek):** 8. fıkranın 2.-6. cümleleri AYM (E.2024/54) ile **9/9/2026'da**, m.8/A'daki "iadesi ile" ibaresi **20/10/2026'da** yürürlükten kalkacak → o tarihlerde metin güncellenmeli. Madde isimleri keşfi: m.1 Amaç · m.4/B Görevler · m.6 üretici tütünleri · m.8 Cezai hükümler · m.8/A Teminat · m.9 Yönetmelikler (diğerleri büyük ölçüde KHK-696/82 ile MÜLGA). **Kart başlıkları hâlâ jenerik** ("1. BÖLÜM" vb.) — resmî "Cezai hükümler" değil; başlık ayrı iş.
- İsteğe bağlı sonraki istatistik işi (gerekirse): "bugün çalışılan kart" için `study_days`'e `(gun, adet)` kolonu (sonraki migration v7); streak için ŞART değil.
- **Sıradaki BÜYÜK altyapı kararı:** (a) **Üyelik + ödeme** (gerçek user ID — filigranı/ilerlemeyi kişiye bağlar, paywall), (b) **Akıllı sistem KATMAN 3** ("eksikleri çalış" aksiyonu — zayıf kartları akışa besle), (c) **Sesli Nöbet v2** (arka plan/kilit ekranı — dev build), (d) **Patika Faz 3** (sıralı kilit + opsiyonel SVG halka). Paralel hat: içerik maratonu (her şeyi anlamlı kılar). Not: v2 ses + gerçek ses testi + arka plan = **development build** gerektiriyor (Expo Go ötesi).

## 4. Backlog (planlı işler, sıra kabaca)
- Tatbikat ekranı (quiz/sınav sistemi) — iskelet ✅ (`c36eb36`): kanun-bazlı A+B çoktan seçmeli, otomatik çeldirici, skor. Kalan: skor/yanlış kaydı, karışık quiz (getAllCards), SRS entegrasyonu, çıkmış sorular kategorisi.
- Sicil ekranı istatistikleri: hazırlık % + çalışılan/öğrenilen + kutu dağılımı ✅ (`947fd1c`, Faz A). Kalan: çalışılan saat (zaman ölçümü yok — büyük iş, opsiyonel).
- Ses entegrasyonu: tek-kart "Dinle" iskeleti ✅ (`c6b15f8`, Faz 2 — expo-audio, auto-play yok). Kalan: ses dosyaları (içerik) + "Sesli Nöbet" (arka arkaya kuyruk, kilit ekranı, arka plan modu).
- Karargah metrikleri: Hazırlık % ✅ (`947fd1c`) + Nöbet serisi/streak ✅ (`6ca939a`) gerçeğe bağlandı. (Mini Tatbikat + Günün Maddesi bilerek statik — kapsam dışı.)
- Dikey ekran kilidi (yatay mod kapat)
- **İçerik maratonu**: 49 TCK görselini gerçek kartlara bağla (her birine madde no + başlık + anlatım metni). Sonra 65 gerçek kanun. Önce Müşterek + Jandarma.
- Development build (Faz 6): Expo Go/ngrok derdini kökten çözer, telefonda kendi uygulaman gibi açılır
- Görseller 1100'e çıkınca: Git LFS veya CDN/release asset (repo şişmesini önle) — radarda

## 5. Bilinen sorunlar / notlar
- **Web bellek-içi**: her yenilemede SRS sıfırlanır, tüm kartlar tekrar "yeni" olur. Normal, web önizleme kısıtı. Telefonda native SQLite kalıcı.
- **Telefonda eski veri**: native SQLite ilk açılışta tohumlanıyordu, yeni seed görünmüyordu → DB migration turu bunu çözüyor.
- **ngrok tünel Windows'ta çalışmıyor** (global PATH bug). `--tunnel` "Install and try again" döngüsüne giriyor. Telefon testi için kalıcı çözüm: development build. Şimdilik web yeter.
- Görseller 112 MB (49 TCK) repoda; LFS/CDN ileride.

## 6. Mimari kararlar (neden)
- **Veri katmanı .web/.native ayrımı**: Metro derleme-zamanı import'a göre paketler; tek dosyada expo-sqlite import'u web'e de sızıp wasm hatası verir. Çözüm: ayrı platform dosyaları, ortak saf mantık.
- **"srs kaydı yok = yeni kart"** + recordReview UPSERT: Anki/Leitner standardı, init'te srs tohumlama yok.
- **İki mod ayrımı**: günlük tekrar (due+yeni) vs kanun-bütünü çalışma (due filtresiz) — kullanıcı "TCK'yı çalışayım" derken hepsini ister.
- **Görsel registry codegen**: Metro dinamik require çözmez; 1100 require elle yazılamaz → klasör taranıp registry üretilir, runtime statik kalır.
- **Branş many-to-many**: bir kanun birden çok branşta ortak olabilir → junction tablo (tek kolon yetmez).
- **Migration = user_version runner** (DB adı yükseltme DEĞİL): kullanıcının SRS ilerlemesi korunsun.
- **Branş storage = AsyncStorage** (SQLite settings değil): web'de de kalıcı, onboarding her yenilemede tekrar çıkmasın.
- **Alt sınıf (subay/astsubay) şimdilik yok**: YAGNI, sadece ana branş.
- **SDK 54'te sabit (56 değil)**: iPhone App Store'daki Expo Go en fazla SDK 54 destekliyor (Apple inceleme gecikmesi). 56'da telefonda test imkânsızdı → tüm ekosistem 54'e indirildi (`88983ca`). SDK 56'ya çıkma; çıkılırsa iPhone Expo Go bağlanamaz. Detay/pinler CLAUDE.md "SÜRÜM SABİT" maddesinde.

## 7. İş akışı kuralları (özet — detay CLAUDE.md'de)
- Her değişiklikten önce KEŞİF, sonra FIX. Keşifsiz fix yok.
- 4-dosya senkron (types/d.ts/web/native). Mantık dosyalarına (srs/queue/kanun-kartlari) dokunma.
- Claude Code otomatik build/start yapmaz; içerik üretmez (placeholder).
- Her başarılı iş → commit (açıklayıcı TR mesaj) + master'a push. Geçmiş okunaklı.
- **Her iş sonrası bu dosyayı güncelle.**

## 8. Branş listesi (16) ve kapsam
Jandarma, MEBS, Havacılık, Personel, Maliye, İstihkam, İkmal, Bakım, Bando, Tabip, Diş Tabibi, Eczacı, Sağlık, Kimyager, Veteriner, Mühendis. + Müşterek (25 mevzuat, herkese). Toplam ~65 kanun, ~950-1100 öğretim birimi.

## 9. İçerik varlığı
- TCK: 49 görsel hazır (`assets/kartlar/tck/`), 4 panelli kare format, künye görselin içinde. Şu an sadece m1 seed'e bağlı; gerisi içerik turunda bağlanacak.
- Diğer kanunlar: görsel/metin henüz yok (üretilecek).
- Maskot: Jandarma Cüneyt (mavi bere, tek yıldız, jandarma amblemi). Kart yapısı: olay → sorun → Cüneyt+kanun → sonuç.
