import { AppText } from '@/components/ui/app-text';
import { Screen } from '@/components/ui/screen';

export default function SicilScreen() {
  return (
    <Screen title="Sicil">
      <AppText color="solukMetin">Sicil (ilerleme ve istatistikler) yakında.</AppText>
    </Screen>
  );
}
