# İçtima Alanı (Supabase) — gerçek değerleri .env'e yaz (.env git'e girmez).
# Supabase → Project Settings → API → Project URL + anon public key.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
